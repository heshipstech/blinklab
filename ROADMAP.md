# ROADMAP.md

The full increment ladder. One row is one pull request with green continuous integration; the working branch is reused between rows and reset to main after each merge. If a row looks like it needs more than two hours, split it here into lettered sub-rows before starting.

## Amendments to the master plan

Accepted on 2026-07-28, before any code:

1. Increment 1.1's automated check was replaced by manual steps in `test/MANUAL.md`. Playwright (the browser testing tool) is introduced at 5.5 as planned, and a headless browser cannot click a real camera permission prompt anyway.
2. Increment 5.4 was split into 5.4a and 5.4b. The gaze target is reliable quadrant classification with the head reasonably still, not a precise gaze point. The head pose gate from 3.8 marks gaze invalid when the head moves too much.
3. Video upload mode moved from 8.1 to 7.0, so dataset videos run through the same TypeScript pipeline as the live camera. Python only loads the resulting CSV files and does statistics. Increment 7.3 is a go or no-go gate on dataset licensing.
4. Accepted 2026-07-31: increment 3.7 delivers a verification of tilt invariance instead of a tilt correction. The aperture is measured as distances between landmarks, and rotations cannot change distances, so there is nothing to correct. The synthetic tests at 0, 15 and 30 degrees prove it, and a counterfactual test shows the cos(roll) shrinkage a naive vertical measurement would have suffered.
5. Accepted 2026-08-06: the long closure detector (6.2) and its alert (6.3) moved off the blink threshold onto a dedicated shut line at 40 percent of the personal baseline. The owner's face proved the need: naturally low eyelids reading at the bottom of the screen sat below the blink line for five seconds while fully awake, and the literature's 20 percent P80 line proved unreachable because the instrument reads fully shut eyes as about a third of baseline, not zero. The 40 percent line is the measured midpoint between the shut floor and the reading droop. The band between the blink line and the shut line is a partial droop, deliberately neither event. PERCLOS shares the unreachable-line problem and has its own follow-up issue.
6. Accepted 2026-08-06: PERCLOS (6.1) adopts the same measured shut line as the long closure detector, aliased from one constant. Its original 20 percent P80 line was proven unreachable on this instrument (0.0 percent through a witnessed 12.9 second closure), so PERCLOS is now an instrument-adjusted convention, documented as such rather than presented as literal literature P80. The deliberate cost: the disagreement between PERCLOS and the long closure detector was the tripwire that exposed amendment 5's bug, and aligning them removes that tripwire; the regression tests and manual items that replaced it are named in the fix.

7. Accepted 2026-08-08: increment 7.3's gate returned NO. No public dataset clears all four of the project's requirements at once (face video, a drowsiness label, per-clip subject identity, and a licence a solo unaffiliated founder may rely on for a public MIT repository). The openly licensed drowsiness data is physiological, still-image or synthetic; the video corpora with real sleepiness labels are behind signed agreements, non-commercial clauses, or no licence at all. Full evidence in DATASETS.md. Rows 7.4 to 7.7 are therefore held for replanning, and split into two tracks. Track A needs no permission from anyone: validate blink DETECTION against the GPL3 blink-annotated benchmark set, which has ground-truth blink intervals and no gate. Track B, a drowsiness classifier on UTA-RLDD, is defensible but not clean, because that dataset carries no licence at all; it is a judgement call for the maintainer and is recorded as a recommendation rather than a decision.

8. Accepted 2026-08-10: rows 7.5 and 7.6 are HELD, not done, and not achievable as written. They assume a dataset large enough to train a classifier and hold out subjects. The only dataset this project has permission for and can measure is DROZY, which yields 20 sessions from 13 subjects once the 25 fps floor removes 16, and those 16 are systematically the sleepier ones: they average KSS 6.38 against 4.60, and every rating of 9 is among them. No feature survived correction against the sleepiness ratings. Training a classifier on that and reporting a leave one subject out score would produce a number that is mostly the noise of whichever subject was held out, which is the exact failure this project spent two days climbing out of. What would unblock them: more recordings carrying real sleepiness labels, at 25 frames per second or better, from more people. Row 7.7 is delivered inside the DROZY analysis, which shuffles the ratings 1000 times with a fixed seed. Rows 7.8 and 7.9 remain achievable and are unaffected.

9. Accepted 2026-08-09: DROZY is unblocked too. Professor Jacques Verly, senior author of the DROZY database, granted written permission on 8 August 2026 for the use this project described: computing per-second numeric features locally and publishing those numbers and the resulting evaluation metrics. His reply asked about a connection with MIT, having read "MIT licensed" as the institution rather than the software licence; the owner corrected that in writing on 9 August, which matters because term 4's non-commercial clause bit this project precisely BECAUSE the MIT licence grants downstream commercial use. The condition is citation of the database and the WACV 2016 paper wherever results appear. DROZY carries real Karolinska Sleepiness Scale ratings, which is what rows 7.5 to 7.7 need and what no other permitted dataset has, so those rows are unblocked. Full record in DATASETS.md. The safeguards are unchanged: numbers only, never a frame.

10. Accepted 2026-08-08: Track B is unblocked. Professor Vassilis Athitsos, senior author of UTA-RLDD, granted written permission on 8 August 2026 for exactly the use this project proposed: computing per-second numeric features locally and publishing those numbers and the resulting evaluation metrics. The dataset's absence of a licence, which was amendment 7's reason for holding rows 7.4 to 7.7, is therefore resolved for this use. Track A continues first because it is further along and needs nobody's permission. The safeguards recorded in DATASETS.md are not relaxed by the permission: numbers only, never a frame, pseudonymous subject identifiers, source video deleted once features are computed, and the CVPR Workshops 2019 paper cited. The reason they stand is that participants recorded themselves and their consent terms are published nowhere, so no author can grant rights over a face beyond what its owner allowed.

11. Accepted 2026-08-16: rows 7.10 to 7.12 are added, a six-person validation round on live webcams. The reason they are not on the original ladder is that the ladder assumed validation meant a corpus, and a corpus is exactly what the licensing gate at 7.3 could not deliver. Meanwhile every live-camera claim this project makes rests on ONE face through ONE camera, the owner's, because Eyeblink8 and DROZY are both recorded clips at fixed rates rather than webcams in real rooms with real lighting. That is the largest untested claim in the repository and no held row unblocks it: rows 7.5 and 7.6 want labelled sleepiness data, and this round wants six people and a scripted session. It also blocks 7.9 honestly, since "limitations stated plainly" cannot be written while the biggest limitation is unmeasured. The round is not a classifier, publishes no recall figure, and is pre-registered in `docs/validation-plan.md` before any file exists, for the same reason the DROZY plan was.

12. Accepted 2026-08-18: issue #178 is reopened off the declined list and reconciled without changing any behaviour, and the reason it may be reopened is one that did not exist on 15 August. It was declined because settling it needed a corpus run or an argument; the blink-trace harness built on 17 August for the sampling-rate question runs one closure past the real detector with every variable held still, so both sides of the contradiction can be reproduced in the same framework in milliseconds. The finding is that `MAX_BLINK_DURATION_MS` does two non-overlapping jobs and which one it is doing depends on `baseline_over_resting`. The constant is NOT moved: that is step 4 of the issue's own suggested order and it is the owner's decision. Written up in `docs/max-blink-duration.txt`. The same increment adds the gradual-descent case closed issue #126 asked for and issue #115 filed, asserted as what today's code does.

13. Accepted 2026-09-04: the Phase 9 line "pupil diameter estimation and a light response experiment using screen brightness" is taken up as rows 9.2 to 9.4. The iris ruler the aperture already uses (11.7 mm across the iris) doubles as a size reference for the pupil, so a pupil diameter in millimetres is reachable without any new calibration; the honest question the plan flagged is whether a webcam can segment the pupil from the iris at all, which is why 9.2 (the pure estimator) refuses rather than guesses when the contrast, the shape, or the plausibility is not there, and 9.4 (the light-response experiment) is pre-registered before it runs, the same discipline as DROZY and UTA-RLDD. Rows 9.2 and 9.3 build the capability and are ordinary test-driven increments (no claim, no pre-registration); 9.4 makes a claim and gets its plan first.

14. Accepted 2026-09-04: the Phase 9 line "a small learned model to replace one heuristic, with the heuristic kept as the baseline it must beat" is taken up as row 9.1, against the demo alertness score (`src/core/score.ts`). It can be taken up now because the UTA-RLDD run (7.5/7.6) produced both a labelled dataset the score can be tested against and a validated learned model to be the contender, and the README now states in the open that the demo score "has never been shown to correspond to anyone's actual sleepiness" — a claim row 9.1 tests rather than leaves standing. Pre-registered in `docs/alertness-score-plan.md` before the comparison code exists, predict-then-verify. The work is analysis-only; any change to `src/core/score.ts` is a later increment gated on the plan's Bar 2.

15. Accepted 2026-09-04: the light-response experiment (row 9.4) gets an in-app stimulus screen, so the maintainer records by pressing start and watching the screen rather than toggling monitor brightness by hand on a stopwatch. The pre-registered plan (`docs/pupil-light-plan.md`) already permits "a minimal in-app stimulus screen" as the mechanism, so this adds no claim and needs no new pre-registration; it is an instrument, like 9.2/9.3. Being an instrument that straddles the core/io fence, it is split the same way 9.3 was: 9.4a is the pure schedule (which phase the screen shows at each moment, and the export rows that log it), 9.4b is the fullscreen screen and the wiring. Row 9.4 stays the experiment itself — the run on the maintainer's camera and the analysis against the committed prediction — and once 9.4b lands it awaits only that camera.

16. Accepted 2026-09-05: the owner decided the browser prototype CONTINUES as the project's working instrument, and Phases 10 through 14 below are the new era's ladder. The goals: raise the accuracy of what is already measured to the most this class of hardware can deliver, add new measurements toward drowsiness and attention, and grow the demo surface — the prototype is the thing that can be shown, and the record it builds is part of the point. The ladder was assembled by proposing candidates from seven lenses (existing-signal accuracy, new drowsiness measurements, attention, capture and devices, the demo surface, performance, and the evidence base itself) and subjecting every candidate to two independent adversarial reviews — one against webcam physics and what the code actually contains, one against this repository's evidence standards — with two refutations killing a candidate and surviving revisions folded in. The scrutiny corrected the goals, and the corrections are accepted as part of this amendment:
    - Blink recall on Eyeblink8 is near its measured ceiling: ~49 of the 67 misses are flat on both landmark signals (docs/iris-occlusion.txt), so the reachable headroom there is about five blinks, not the fifteen points to the literature's 98% end. Eyeblink8 is henceforth a REGRESSION harness only; every future accuracy claim graduates on the never-scored GPL3 sibling corpora (Phase 11) or a pre-registered live protocol.
    - "Microsleep" is EEG-defined and cannot be validated with any data this project may use, now or in this era. The vocabulary is capped at "microsleep-RANGE closure" (12.6); the phrase "microsleep detection" is refused in UI and docs alike.
    - "Cognitive state" has no webcam-honest meter: the one channel the literature ties to load, the pupil, is measured dead at this optics (docs/pupil-light-result.txt). The era's honest versions are demonstrated task effects (read-vs-rest blink suppression, 14.11), an in-browser vigilance task (14.14), and permanently-labelled demo-only trends. Gaze events never wear the word "attention" without the unvalidated framing the demo notice set the pattern for.
    - Capability is a property of a SETUP, not a device class: the dry run measured a phone front camera out-resolving the DSLR rig at default framing (44.6 px vs 36.9 px iris). The ladder the era builds (13.6) is measured per setup — delivered rate, iris pixels, light — and device class is only the label the export already records.
    - The UTA-RLDD labels have been read twice and are read exactly ONCE more: one batched, pre-registered v2 read (12.18) carries every new signal family together. Per-feature dips into the same 148 videos are refused; that drip is how evidentiary freshness dies.
    - WebGPU is not in the vendored API (the delegate is CPU or GPU-as-WebGL2, nothing else), and optimizing the 42 µs core chain against a ~30 ms inference budget is performance theater. The real levers are asking cameras for 60 fps for the first time (13.2) and not running inference twice on the same photograph (13.8).
      Era rules, holding for every row below: instrument rows land before their claim rows; every claim row commits its prediction to docs/ before any data is read; a row bigger than one pull request splits into lettered sub-rows when taken up, as 9.3 and 9.4 did.

17. Accepted 2026-09-06: the owner asked for the era's roadmap to be re-worked from the September audit (`docs/audit/2026-09-06-audit-report.md`; 154 confirmed findings, 1 critical, 16 high, each tested by two independent skeptics) against six briefs: (1) improve what exists; (2) finish what is partial; (3) add, expand or correct the measurements, metrics and eye signals; (4) fix every confirmed defect, mismatch and inconsistency; (5) take up everything else within the audit's scope that pushes the software toward its maximum; (6) re-do the tests and measurements on public data and volunteers across devices so that validation is complete and multi-layered. Phases 10 to 14 below are re-ordered and extended, Phase 15 is new, and every confirmed finding maps to a row here (its ladder item named in brackets) or to a dated decline in `REMEDIATION-2026-09.md`, whose stages A to E remain the fix ORDER while this table holds the STATUS. This amendment is itself ladder item D12 [D12; F-041, F-072, G-Stepped-8]. The draft was read by two adversarial reviewers, one for traceability against every finding and one for feasibility against the code and webcam physics, and their 45 corrections are folded in. Where the briefs land: (1) 10.11, 10.12, 10.13, 10.8, 11.6a-c, 12.0, 12.8a, 13.8, 13.10, 14.0, 14.0b, 14.9a-b; (2) 10.5, 10.6, 10.8, 10.9, 11.7, 11.8, 11.9 (the written, unrun assessment pilot), 12.2, 12.3, 13.12, 14.9; (3) 10.16, 10.17, every Phase 12 row, 14.9, 14.10 to 14.14, behind a gate that needs the owner's cameras for one dim-room session (10.12a) and one A/B live session (13.8c); (4) Phase 10 entire, 14.0, 14.0b, 14.9a; (5) 10.1c to 10.1g, 10.2b, 11.8a, 13.0, 13.5, 14.0c; (6) 11.3, 11.4, 11.6, 11.7, 13.3 and Phase 15. The audit's corrections are accepted as part of this amendment:
    - The camera path measures processing ticks, not delivered photographs. Peak closing velocity and the amplitude-over-velocity ratio in every live export scale with display refresh over camera delivery (up to 4× on a 120 Hz display), the 25 fps refusal reads the tick rate and passes 15-24 fps cameras behind a 60 Hz display, the blink rate divides by elapsed rather than observed time, and the ruler's sample floors count ticks. Row 13.8 is therefore a CORRECTNESS row, and the "42 µs core chain" sentence of amendment 16 is withdrawn: the per-tick cost is quadratic in tick rate, about 5 ms at 120 Hz with a still gaze. New rows 10.12a-c fix the gate, the rate and the floors on the evidence rate; 13.8a-c drive `processFrame` from `requestVideoFrameCallback` with a pre-registered velocity prediction rather than an equivalence expectation. Both precede every Phase 12 signal row.
    - The frame stepper fabricated frames on variable-rate clips: a 200-frame 20 fps file measured as 399 frames at 40 fps and passed the refusal. Row 10.14 (landed 6 September 2026, `docs/stepper-honesty.txt`) makes an inexact landing a counted refusal and writes the stepping witness into every stepped export; 10.14b gives UTA-RLDD and DROZY the container cross-check Eyeblink8 already has; 10.14c lowers the calibration probe below half a period for rates above 60, where a probe landing on every other frame would calibrate at twice the period and every target would land exactly on a real frame, invisible to the inexact-landing rule. They precede 10.8a, every Phase 11 read and 12.18, whose v1-parity gate becomes conditional on the container coverage check so a corrected stepper cannot be read as a regression.
    - The regression run at HEAD (10.8a) runs BEFORE any change on the corpus-rule list and scores the stepper and guided-line predictions written against the detector as it stands; this supersedes the ladder's "D10 depends on A2", because the stepped path does not read the frame driver, so the anchor 10.8a moves is the anchor 13.8b's parity check reads. Rows that can move the Eyeblink8 count say so and carry a committed prediction: 10.8, 10.11, 10.12c, 10.13b, 12.0a, 12.6c, 12.8a and 12.3's fusion decision, if any.
    - The instrument has one personal ruler and does not use it. The guided line sits 30-50% above the passive line, its closed median is discarded while PERCLOS and long closures keep the owner's 0.4, the long-closure reducer has no hysteresis (one 6 s closure fired 3 and 2 events on two iPhones), and no export says which line produced a session's numbers. New rows 10.11 (hysteresis on the arming side, so durations do not move), 10.13a (the line travels with the number), 11.6a-c (guided calibration as a procedure, with a settle window, a verification blink, a soundness ceiling and audible cues) and 12.0a-b (one personal ruler for blinks AND closures) land before round II (11.7), which cannot otherwise read a guided session. Row 10.9's device gap is first tested as arithmetic on the existing columns under the two line placements.
    - Rows 13.2 and 13.4 move to the front of the era: the app has never asked a camera for a frame rate, and whether live accuracy is capped at 30 fps on every owned camera is decided in half a day. The cued blink-and-closure protocol (11.0a/b) replaces the two-marks protocol as the ground truth for 10.9, 11.6, 13.3, 14.5, 14.13 and Phase 15, because the round showed the marks contaminated (3 of 6 volunteers pressed Mark more than twice).
    - Gaze instrument before gaze signal: 14.9a-b (a refusing solver that carries pose, viewport and camera context; blinks excluded from the whole gaze chain; one definition of on-screen) and 14.9 move ahead of every gaze signal; 12.11 and 12.13 move to ideas because both would move with blink rate on an offset 14.9 has not measured (F-041, A25), and 12.12 waits for 14.9's error file; 12.18's look-away and gaze riders are gated on a committed 14.9 number. MODEL_CARD's "reliable near the centre, degrades at the corners" becomes "not measured" until 14.9 produces a file.
    - The blendshape witness (12.2, 12.3) runs on the regression harness before the sibling corpora are read once with whatever detector it leaves. The vendored blendshape head is a landmark-fed network over 146 mesh points, not a pixel head: the witness tests whether mesh points the aperture ignores carry closure the eyelid rim does not, and it is the only vendored route past the 49-miss ceiling; the pre-registration states that prior.
    - UTA-RLDD's labels stay read exactly ONCE more (amendment 16): the shape-feature ablation and the within-subject null are pre-registered sections of 12.18, and 12.10a's blink-excluded PERCLOS is scored on DROZY and as a 12.18 rider, never as a separate read. The sibling corpora are read once, on the final pinned build, after every corpus-rule row is ticked; the former row 11.5 is deleted because 10.8's delta is scored on Eyeblink8 by its own committed prediction.
    - Row 10.3 is DECIDED: the three DROZY shape correlations are retired rather than re-measured; drozyGuard gains its rule and a per-feature source map; the sample is re-measured only if 12.18's v2 read moves a shape feature.
    - The headline number becomes re-derivable (10.1d) and the ratchet watches the whole chain (10.1g): the anchor run's blink logs and derived tables are committed under docs/evidence/, the evaluator gains tests and a non-zero exit on a partial corpus, and the ratchet watches the scorer, the stepper, the runner, the engine and the lockfile. Row 10.1c revives the mutation runner in CI with a literal boundary probe per refusal constant, six of which survived mutation with the whole suite green. Row 10.10c puts an interval on every published number; none carried one.
    - New row 13.0 measures engine agreement on one fixed clip before 13.1, because every published detection number is WebKit-only and Phase 13's phone rows would otherwise confound engine with device; "browser binary" in the results block becomes "WebKit binary".
    - New rows 14.0a-e give the session an `ended` state; Stop currently disables export and loses the session, a superseded camera start leaves a live track the page cannot stop, and the light overlay traps a phone user with no touch exit (14.0b). Both precede any pilot session. Row 11.8 gains row zero 11.8a: a pinned build at a stable path named in every frozen plan, with an intake that refuses a differing `app_commit`.
    - Demoted to ideas, on the owner's priority (the six briefs name accuracy, completeness and validation first) and not on the audit's F-104, which both skeptics killed: 13.9, 14.7, 14.8. 13.11 is KEPT, because 13.12's arming condition needs its floor. Row 14.10 runs on raw offsets or with the smoothing filter's lag pre-registered. Row 13.6 splits into 13.6a (rate and iris-pixel verdicts, now) and 13.6b (light, after 12.16). Tablets and the Android phone appear as volunteer-supplied or ABSENT until measured. Row 12.6 gains 12.6b (closure completeness) and 12.6c (droop-then-blink timing); 12.8 is preceded by 12.8a (a quadratic peak velocity where the descent holds three samples, after 13.8); 12.10 gains 12.10a; 12.5 is preceded by 10.17 (the pose matrix is read column-major, not row-major), a Phase 10 correctness fix rather than a gated signal row.
    - Documentation is a load-bearing cost: 62% of recent file touches are prose and every false sentence the audit found lives in the hand-synchronised layer. Rows 10.0a-b are the truth pass, closed with positive pins; thereafter any number in more than one document is generated or appears once with links, and the per-row ritual is the PR body plus the one document the row's Check names. Line 3 of this file now says what is practised (one PR per row, the branch reused).
    - Phase 15 is the validation the owner asked for, in six layers: pinned fixtures and an injected face in CI; the public corpora once each on the final pinned build; repeatability across runs, engines, devices and calibrations; the cued live protocol on volunteers across device classes at 30 and 60 fps with n set by the interval it must reach; the rounds on the pinned build; and a generated validation report from which the card and the README are regenerated, behind a claim gate. Each layer names its prerequisites; none starts before every Phase 10 row except the conditional 10.7c is ticked, because re-validating an artefact validates the artefact.
      Era rules, unchanged and now enforced by order: instrument rows land before their claim rows; every claim row commits its prediction to docs/ before any data is read; a row bigger than one pull request splits into lettered sub-rows; no Phase 12 signal row starts before 10.11, 10.12a-c, 10.13a-b, 10.14, 13.2 and 13.8b are ticked; every number published from here carries an interval and its device scope; volunteers stay pseudonymous and numbers-only, raw session files never enter the repository, and no frame ever does.

18. Accepted 2026-09-07: a ladder that offers work it cannot deliver costs whoever picks the row exactly the time it takes to find that out, and then costs the next reader the same again. Five rows were tried that way in one session, and each now carries a `**BLOCKED:**` marker naming what would unblock it, verified against the code and the repository rather than remembered: 10.2b wants a WebKit run and no WebKit is installed in this container or in `ci.yml`; 12.1's Check asks for a test about two consumers amendment 17 demoted (and 12.1 was RETIRED on the owner's ruling later the same day, so its marker is gone rather than standing: see the row, and note that BLOCKED was the right reading of a row that could not proceed and the wrong verdict on one that should not); 12.4 wants `jawOpen`, which is a blendshape the landmarker is not asked for until 12.2; 10.12b is startable but its third Check clause needs DROZY and RLDD session files this repository deliberately does not keep; 10.0b's remaining half is ADR-0005, a decision about intent rather than a fact in the record; and 12.0b, which this amendment first named as the one fully startable row left, is blocked too — its Check needs the guided shut line that 12.0a introduces, and 12.0a is a corpus rule waiting on the owner's recorded droop session. That correction is recorded here rather than quietly fixed, because a wrong claim about what is available is the same defect this amendment exists to stop. Rows 12.9, 12.6b, 12.10a and 13.4 were left unmarked on the day this was written; amendment 19 records what became of that list, and it is not what this sentence expected. `tools/roadmapGuard.mjs` refuses a marker that names nothing — "BLOCKED" alone is the same silence in a louder font — and pins the one blocker that is checkable against the code: when 12.2 turns blendshapes on, the test reddens and 12.4's marker has to come off, because a blocker that outlives its blocker is the next stale sentence. This amendment records a limit of the WORKING ENVIRONMENT, not a change of intent: nothing here is declined, re-scoped or re-ordered, and every one of these rows is ready the moment its blocker lifts.
19. Accepted 2026-09-07: amendment 18 ended by naming the rows that could still be picked up, and that sentence was wrong twice inside a day. It was wrong once inside the amendment itself, about 12.0b, and it is wrong again here: of the four rows it left standing, 12.9 has since landed and the other THREE each carry a clause this working environment cannot meet. 12.6b's Check wants the committed miss table's `min_ratio` distribution reproduced from its new column, and the per-frame traces that table was joined from are deliberately not committed (`docs/evidence/2026-09-02-awake-autopsy/README.md` says so in its own provenance section), so the clause needs a corpus run on clips this container does not hold. 12.10a's Check wants the DROZY refit from the retained CSVs, and those CSVs are not in this repository: row 10.10c2b already records that `analyse_rldd.py` and `compare_alertness.py` both take a measured corpus directory that exists only on the owner's machine. 13.4's Check ends "one deliberately-loaded session in a dated note", which is a live camera under load. All three are marked now, each naming what would unblock it. **The mistake was the same all three times, and it is worth more than the three rows:** startable was judged from the row's HEADLINE rather than from every clause of its Check, and a row is only startable when the last clause is. So the judgement stays a person's and the SENTENCE becomes mechanical: `tools/roadmapGuard.mjs` reads the rows a startable claim names and holds each to the ladder, so the moment one is ticked or picks up a marker the build says so, and a ladder carrying no such sentence at all is refused rather than passed — deleting the claim is not a way to make it true. Rows 10.11, 10.12b, 10.12c, 10.13b and 13.8b remain startable and are not marked: 10.8a's completion on 8-9 September put the corpus anchor at HEAD, which is the thing every one of their corpus clauses scores against; 10.13b's decision was taken on 9 September by the owner's delegation; and 10.12b was startable by its own clauses all along. Each has a build half met here on synthetics and a corpus half scored on the owner's next twenty-minute run. See amendment 23, which records the gate state, the amendment 20 ruling and the 10.13b decision in one dated place.
20. Accepted 2026-09-07: **three Phase 12 rows were started while the gate at the head of Phase 12 was shut, and the gate is not in the rows.** That header says, from amendment 17: "no signal row here starts before 10.11, 10.12a-c, 10.13a-b, 10.14, 13.2 and 13.8b are ticked, because every signal built before them inherits a tick-rate column and an owner-referenced shut line", and it names the exemptions — 12.0a-b, 12.1, 12.2, 12.3, 12.15, 12.16 and 12.17's tool. Of those prerequisites only 10.13a and 10.14 are ticked; 10.11, 10.12a, 10.12b, 10.12c, 10.13b, 13.2 and 13.8b are all open. Rows 12.9, 12.6 and 12.14 landed on 7 September and none of them is on the exempt list. 12.15 landed the same day and IS on it, which is the only reason that one was in order. **What it cost and what it did not.** Nothing published depends on the three: none is wired to the page, the export or the score, each was deliberately shipped as a measurement with no consumer, so there is no number to retract and no file whose meaning changed. What it did cost is the thing the gate exists to protect: their constants — the two-second and fifteen-second closure bands, the fifteen-blink floors, the halving midpoint — were chosen against an instrument that 13.8b will re-time and 12.0a will re-rule, and nothing said so. Each row now carries that sentence. **The third wrong startable list in one day, and this one was wrong differently.** Amendment 18 judged startable from a row's headline. Amendment 19 fixed that, judged clause by clause, and named 12.7, 12.10, 12.14 and 12.16 — and 12.7, 12.10 and 12.14 were still not startable, because every clause of a row's Check can be satisfiable here while the PHASE forbids the row from starting at all. The Check is what a row must prove; the gate is whether it may begin. Reading one has never told anybody the other, and nothing in the guard amendment 19 built reads a gate. So the list is now 12.16 alone. **Made mechanical in 10.0b8, not here**, because the gate is prose and a guard that parses it badly is worse than none. **One question is the owner's and is asked rather than decided:** whether 12.9, 12.6 and 12.14 stand with the caveat they now carry, are reverted, or are re-opened for re-measurement when the gate lifts. Nothing is reverted in this amendment, because deleting correct, unwired, tested work to satisfy an ordering rule costs more than the rule protects — but that is a judgement about intent, and intent is not this amendment's to settle.

21. Accepted 2026-09-07: the ladder has run out of rows this working environment can start, and the sentence that says so had no form. Amendment 19 predicted this state in writing — "if nothing outside Phase 12 is genuinely startable, say so in the sentence rather than naming something that is not" — and left it unhandled: `startableClaims` throws on a ladder with no such sentence, deliberately, so the claim could never be deleted, but nothing let a reader express NONE either. The two failure modes had become one shape. **So emptiness gets a form: "NOTHING outside Phase <n> remains startable", which `startableClaims` accepts as the claim it is, returning the empty list.** A missing sentence still throws, and the refusal now teaches the form, because the alternative is the next reader deleting the sentence again and this guard passing for the wrong reason. Row 10.0b11 carries the change, watched failing first and verified against a bend that makes silence acceptable. **What the state actually is, so it reads as a finding rather than as an ending:** every remaining row needs a camera, a corpus, a second engine or a decision. Phase 12's gate waits on 10.11, 10.12a-c, 10.13b, 13.2 and 13.8b, each of which scores against 10.8a's corpus anchor or wants a live session, and 10.8a is the owner's regression run at HEAD. 10.2b wants the WebKit run on the owner's Mac. 10.10c4d wants a citation the owner has read for `IRIS_DIAMETER_MM`. This is the same kind of record as amendments 18 and 20: a limit of the WORKING ENVIRONMENT, not a change of intent. Nothing is declined, re-scoped or re-ordered, and every marked row is ready the moment its blocker lifts.

22. Accepted 2026-09-08: amendment 21 declared nothing startable, and that was true at ONE INCREMENT SIZE. Every startability judgement on this ladder had been made over whole rows: a row died if its LAST clause needed a camera, a corpus, a second engine or a decision. Read that way the ladder was genuinely empty. Read at a smaller grain it is not, and row 10.8a is the proof — its Check opens "the replay tool committed first", and that tool needs no corpus at all: it runs on synthetic traces exactly as `miss_autopsy.py`'s tests do, which is how 10.8a1 shipped the same day this amendment was written. **The correction is to the JUDGEMENT, not to the record.** Amendments 18, 20 and 21 each recorded a limit of the working environment and each was honest about what it could see; none of them was wrong about the rows as they were written. What none of them asked was whether a blocked row's FIRST slice is blocked, and the answer is often no: a pure-core module, a guard, a committed prediction and a fixture are all reachable here, while the measurement that scores them is not. **So the rule gains a clause.** A row is startable when its every clause is satisfiable here, AND a row whose later clauses are blocked may still be SPLIT, provided each slice carries its own verifiable Check and the slice does not pre-judge a measurement the owner still owes. That proviso is the whole safeguard: splitting is not a way to claim progress on a blocked row, and a slice that picks constants against an instrument 13.8b or 12.0a will change is the mistake amendment 20 already recorded. 10.8a2 declines a verdict column for exactly that reason. **What this does not change:** the Phase 12 gate stands, the corpus rule stands, and the six rows waiting on the owner still wait. What it changes is that the ladder stops reading as finished when it is only blocked at the far end.
23. Accepted 2026-09-09, on the owner's delegation to the recommendation: three rulings in one dated record. **Amendment 20's question is ruled: 12.9, 12.6 and 12.14 STAND, caveats kept.** The 9 September regression run reproduced the anchor digit for digit with all three in the tree, which is the measurement that their code cost the corpus nothing; the caveat each row carries — constants chosen against an instrument 13.8b will re-time and 12.0a will re-rule — is about their CONSTANTS, stays truthful, and is revisited when those rows land. Nothing is reverted and nothing re-opened. **The 10.13b decision is taken: keep the fixed line for the learning window and label it.** Feeding null while learning would discard blinks the fixed line demonstrably catches (the anchor's found blinks include the pre-freeze window on every clip) and would blank the demo's first thirty seconds; counting on the live learning line is the moving ruler this project rejected when it froze the baseline. So the count stands and the CONDITION becomes visible: the page and the record say the first ~30 seconds are counted against the fixed 4 mm line, the per-row `blinkLineSource` column already says it per frame, and the predicted corpus delta is ZERO because no counting behaviour changes — a prediction the next corpus run scores. The row builds here, pre-registration document first. **The gate state: five rows are startable**, named in the sentence amendment 19 built, each with its build half here and its corpus half on the owner's next run. The Phase 12 gate itself still waits on 10.12a (a dim-room session) and 13.2 (the owner's cameras) beside the rows this amendment starts.

## Phase 0. Foundations (no eye code at all)

- [x] 0.1 The four working documents, plus LICENSE, .gitignore, README skeleton. Done when the repo has docs and nothing else.
- [x] 0.2 Vite plus TypeScript project that prints "blinklab" to the page. Done when `npm run dev` shows a page locally. Check: `npm run build` succeeds.
- [x] 0.3 ESLint, Prettier, strict tsconfig, and the rule that `core` cannot import `io` or `ui`. Check: lint and typecheck scripts pass.
- [x] 0.4 Vitest with one real test of a trivial pure function in `core`. Check: `npm test` shows 1 passing test.
- [x] 0.5 GitHub Actions CI (continuous integration) running install, lint, typecheck, test, build on every pull request. Check: green check on a pull request.
- [x] 0.6 Branch protection on `main`, pull request template, two issue templates. Done when direct pushes to `main` are blocked.
- [x] 0.7 GitHub Pages deployment workflow. Done when a public URL shows the page.
- [x] 0.8 ADR-0001 recording the stack decision and its alternatives.

## Phase 1. Pixels

- [x] 1.1 Request webcam permission, show the live video element. Done when your face appears. Check: manual steps in `test/MANUAL.md` (amended, see above).
- [x] 1.2 Permission denied and no camera states with readable messages. Check: unit test on the state machine.
- [x] 1.3 Measured frames per second, displayed. Check: unit test of the fps calculator with fake timestamps.
- [x] 1.4 Draw the video into a canvas instead of showing the video element. Check: visual check documented in `test/MANUAL.md`.
- [x] 1.5 Camera device picker when more than one camera exists. Check: unit test on the device list mapper.
- [x] 1.6 Mirror toggle and resolution readout. Check: unit test on the transform matrix.

## Phase 2. Landmarks

- [x] 2.1 Load FaceLandmarker, log face detected true or false. Check: unit test on the face present predicate using a fixture.
- [x] 2.2 Draw all 478 landmarks as dots on the canvas. Check: fixture based test of the point projector.
- [x] 2.3 Move landmark index groups into `core/constants.ts`, draw only the eye region. Check: index sets do not overlap and are in range.
- [x] 2.4 Draw the iris landmarks in a second colour. Check: iris indices form a closed ring.
- [x] 2.5 Guard against a model returning 468 landmarks instead of 478. Check: unit test with a 468 point fixture.
- [x] 2.6 Display per frame inference time in milliseconds. Check: test on the timing helper.
- [x] 2.7 Record 300 frames of landmarks to a JSON fixture file for tests. Done when `test/fixtures/session-01.json` exists and is used by a test.

## Phase 3. First real measurement

- [x] 3.1 Eye aspect ratio (EAR) per eye, displayed. Check: unit test with hand built synthetic eye shapes.
- [x] 3.2 Rolling sparkline of EAR over the last 10 seconds. Check: unit test of the ring buffer.
- [x] 3.3 Synthetic face generator in `test/fixtures/syntheticFace.ts`, landmark sets at known distances and angles. Check: whole new test file.
- [x] 3.4 Iris width normalisation, pixels to millimetres via the fixed 11.7 mm human iris diameter. Check: mm output stable while px output varies across synthetic distances.
- [x] 3.5 Lean in and lean out validation, coefficient of variation for px and mm. Check: CV(mm) < CV(px) on synthetic data.
- [x] 3.6 Head pose estimate: pitch, yaw, roll. Check: three separate synthetic tests, one per axis.
- [x] 3.7 Tilt invariance of aperture, verified instead of corrected (amended, see above). Check: synthetic test at 0, 15, 30 degrees roll, plus the cos(roll) counterfactual.
- [x] 3.8 Reject frames outside a head pose range, mark them invalid instead of guessing. Check: unit test of the validity gate.

## Phase 4. Blinks

- [x] 4.1 Fixed threshold blink detector, blink counter. Check: unit test on a synthetic aperture time series.
- [x] 4.2 Personal baseline learned over 30 seconds, visible countdown. Check: baseline rises but never falls.
- [x] 4.3 Blink duration in milliseconds. Check: synthetic series with known durations.
- [x] 4.4 Blink rate per minute, rolling window. Check: ring buffer and window edge tests.
- [x] 4.5 Blink closing velocity and the amplitude over velocity ratio. Check: unit tests on velocity extraction.
- [x] 4.6 Frame rate honesty gate, null instead of a number when fps is too low. Check: null, not zero, below threshold.
- [x] 4.7 Squint versus blink separation. Check: synthetic squint plateau counts no blinks.
- [x] 4.8 Blink event log with timestamps in a side panel. Check: test on the event reducer.

## Phase 5. Gaze and attention

- [x] 5.1 Iris centre offset relative to the eye corners, per eye. Check: synthetic tests at known offsets.
- [x] 5.2 Screen quadrant classification, four regions. Check: test on labelled fixture frames.
- [x] 5.3 On screen versus off screen classification. Check: threshold test.
- [x] 5.4a Calibration capture screen: nine points shown, samples collected and stored (amended, split from 5.4). Check: test on the sample collector.
- [x] 5.4b Calibration solver producing a stored profile. Done when quadrant classification is reliably correct with the head reasonably still (amended target). Check: solver test with synthetic points.
- [x] 5.5 Introduce Playwright, first end to end test of the calibration flow. Check: headless test passes in CI.
- [x] 5.6 Gaze point smoothing filter, raw and smoothed traces both drawn. Check: filter preserves step response within tolerance.
- [x] 5.7 Fixation and saccade separation using a dispersion threshold algorithm (I-DT). Check: synthetic scanpath of known structure.
- [x] 5.8 Fixation duration statistics panel. Check: aggregation tests.
- [x] 5.9 Gaze heatmap over a static image. Check: test on the accumulation grid.
- [x] 5.10 Scanpath replay with a time slider. Check: test on the replay index lookup.

## Phase 6. Rolling state and the demo score

- [x] 6.1 PERCLOS (percentage of eye closure) over a rolling 60 second window. Check: synthetic series with known closure fraction.
- [x] 6.2 Long closure detector, eyes shut beyond a threshold triggers an event. Check: boundary case included.
- [x] 6.3 Alert with debounce so it cannot fire repeatedly. Check: repeated triggers within the debounce window.
- [x] 6.4 Feature vector assembled into one typed FeatureRecord per second. Check: schema test.
- [x] 6.5 Explainable 0 to 100 demo score using a documented weighted heuristic. Check: contribution numbers sum to the score.
- [x] 6.6 Contribution panel showing the top three drivers of the score. Check: snapshot test.
- [x] 6.7 Session recorder, export CSV (comma separated values) to a local download. Check: serialiser tests including comma and header edge cases.
- [x] 6.8 KSS (Karolinska Sleepiness Scale, 1 to 9) self report before and after a session, written into the CSV. Check: metadata writer test.
- [x] 6.9 Prominent, permanent "demo, not a safety or medical device" notice in UI and README. Check: Playwright assertion.

## Phase 7. Honest evaluation (the Python track)

- [x] 7.0 Video file upload mode so recorded clips run through the same TypeScript pipeline (amended, moved from 8.1). Check: end to end test with a sample clip.
- [x] 7.1 `/analysis` folder, pinned Python environment, ruff, pytest, second CI job. Check: Python CI job green with one real test.
- [x] 7.2 CSV loader plus a first plot of one recorded session. Check: loader tests on malformed CSV.
- [x] 7.3 DATASETS.md, choose one openly licensed public dataset, record licence and access terms. Go or no-go gate: if no suitable openly licensed dataset exists, replan 7.4 to 7.7 before starting them (amended). Gate returned NO, see amendment 7.
- [x] 7.4 Batch runner computing features over dataset videos into feature CSVs, via the 7.0 upload pipeline (amended). Check: two frame sample test. Delivered as a stepped measurement mode, which fixes #145: the clip waits for the instrument on every frame, so the output is a property of the file rather than of the machine.
- [x] 7.5 Baseline classifier with a clearly stated train and test split, DONE 3 September 2026. Check: reproducibility with a fixed seed — met, `analysis/blinklab/rldd.py` is a deterministic numpy multinomial logistic regression (zero-init, fixed step and count, standardised and median-imputed inside each fold) that reproduces bit for bit, with the label-shuffle control seeded from one recorded value. Pre-registered in `docs/uta-rldd-plan.md`, run on the full dataset (54 usable subjects, 148 videos) by `analysis/tools/analyse_rldd.py`, result in `docs/uta-rldd-result.txt`. The fold-1 pilot was a null at DROZY's scale; the full sample DETECTS drowsiness (three-class balanced accuracy 0.498, binary alert-vs-drowsy 0.732, both past the shuffle control at p 0.001), which is the surprising outcome the plan named and it is recorded as such with its caveats.
- [x] 7.6 Leave one subject out evaluation, per subject scores reported, DONE 3 September 2026. Check: no subject in both splits — met and verified empirically on the 148-video matrix (54 folds, every video held out exactly once, none in both splits). Same pre-registered plan, `docs/uta-rldd-plan.md`: per-subject balanced accuracies reported for all 54 subjects, pooled balanced accuracy against the 1/3 floor, and the 1000x label-shuffle control whose null sits at chance (0.310) — the proof identity leakage was blocked. Both tasks clear the pre-registered two-bar decision rule. Recorded in `docs/uta-rldd-result.txt`.
- [x] 7.7 Negative control: shuffle labels, confirm collapse to chance. Delivered inside the DROZY analysis: the ratings are shuffled 1000 times with a fixed seed and the strongest chance correlation is printed beside the observed one. Check: test asserting the collapse.
- [x] 7.8 Latency measurement, DONE 21 August 2026: time from eye closure to alert is deterministic, 500 ms plus at most one frame period (520.0 ms at 25 Hz, 533.3 at 30, 516.7 at 60), proven sensitive by mutation; the core chain costs about 42 microseconds per frame on one measured machine, asserted under a 2 ms ceiling so CI noise cannot redden it while per-frame growth still would. The debounce regime is stated beside it: within 5 s of a firing a new closure is counted, not told. Record: docs/latency.txt. Check: timing test with tolerance, test/core/alertLatency.test.ts.
- [x] 7.9 Results section in README generated from analysis output, DONE 21 August 2026: a "Results at a glance" block between markers, built by tools/resultsBlock.mjs from docs/eyeblink8-result.txt, docs/drozy-result.txt and docs/validation-round.txt, with the limitations stated plainly in the block and the DROZY citation required by its permission carried in it, refused if missing. Regenerate with npm run results:write. Check: a test regenerates the block and fails when the committed README drifts from it, and no TODO remains in the block. AMENDED 4 September 2026: the sleepiness bullet now carries the UTA-RLDD detection alongside the DROZY null, generated from docs/uta-rldd-result.txt via parseUtaResult, with both required citations carried and a verdict guard that reddens the build if the result ever stops reading "detecting drowsiness" — the mirror of the DROZY null guard.
- [x] 7.10 The six-person validation round, pre-registered and read (amendment 11). `docs/validation-plan.md` written before any session file exists, and `analysis/blinklab/validation.py` pairing a folder of exports with a named refusal for every way it can be handed something untrustworthy, including the camera blink log that `blink_log.py` is correct to refuse. No check is computed here, deliberately: a reader and its checks arriving together is how a refusal softens into a default. Check: a mutation per refusal turns its own test red, and a session whose blink log does not exist is KEPT, because that is a result and not an accident.
- [x] 7.11 The checks and the published table: blinks between the two marks with their boundary counts, the long closure, the baseline's readiness and drift, face-detected share, iris width per device with its measurement frame, and declared against processing frame rate. `validation_checks.py` judges, `tools/validation_report.py` prints, and the report answers the plan's three failure criteria out loud rather than leaving a reader to count rows. Check: a fixture per verdict, a mutation per rule, and a refused participant gets a named row plus a non-zero exit rather than a silence. NOT YET RUN ON A REAL FILE, which is 7.12.
- [x] 7.12 The dry run, DONE 16 and 17 August. FIVE sessions on three devices, at the owner's suggestion of three rather than two: iPhone 14 Pro Max twice, MacBook Air twice, and the Sony A7 IV through a Cam Link 4K. The second MacBook run is the test that separates the processing rate from the device, repeating the first at the same rate with a baseline that lands correctly. All eight files read with no refusals and the pairing coped with device names in the filenames. The drift threshold is SET at 15 percent from the measured 0.0, 0.0, 0.0 and 5.0, recorded in `docs/validation-plan.md` before any volunteer file existed. Full write-up in `docs/validation-dry-run.txt`. It found two things no fixture could: a baseline 41 percent longer than its own session's median aperture passing BOTH halves of the pre-registered baseline check, which produced the plan's second correction and a fifth check; and the instrument losing deliberate blinks at the rate a four core machine produces, 3 of 10 on the phone and 1 of 10 on the laptop, each miss a hole a whole number of blinks wide in a metronome cadence, with the 25 fps gate never firing. It also refuted one of its own first conclusions, that blink duration moves with the processing rate, which the fifth session ordered the wrong way. Check: the threshold was recorded before any volunteer file was opened, and the session that disagreed with the protocol was re-run rather than deleted.

## Phase 8. Making it public and durable

- [ ] 8.1 (moved to 7.0, see amendments)
- [x] 8.2 ARCHITECTURE.md with a module diagram and the data flow. Done when a newcomer understands it in 5 minutes.
- [x] 8.3 CHANGELOG.md written 15 August covering all seven existing releases plus an Unreleased section. Tags exist to v0.7.0. The v0.8.0 tag is deliberately not cut: 8.8 is declined rather than done, and a version number is not worth a ceremony.
- [x] 8.4 MODEL_CARD.md: what it measures, what it does not, where it fails, who it fails for.
- [x] 8.5 Dependabot config, alerts on and reporting 0, SECURITY.md with a real threat model. Verified against the API 15 August.
- [x] 8.6 Coverage floor on src/core as its own CI step: statements 98, branches 95, functions 100, lines 98, against a measured 98.61 / 95.52 / 100 / 98.57. Far above the 70 this row asked for. Proven able to fail.
- [x] 8.7 Bundle size ceiling of 240 kB checked after every build, against 217.6 kB actual. Written against the one commit that bundles the 3.7 MB model rather than against creep. Inference time stays a locally recorded number, 6 ms, since CI machines have no camera.
- [~] 8.8 DECLINED 15 August. The floor is already met: keyboard operable, focus visible, all text clears WCAG contrast. What remained was polish. **AMENDED 22 August 2026, additively rather than reopened: one clause of that decline was false when it was written.** Five colour pairs failed AA on the committed stylesheet, and the worst of them was `.rate-warning` at 2.80:1 — the processing-rate limitation shipped the very next day, which made "limitations belong in the open" untrue on the page while this row sat closed. The palette is corrected and `tools/contrastGuard.mjs` now computes every pair, on both grounds, and checks that the page uses the legible tokens rather than merely declaring them. The decline itself STANDS: what remained after this really is polish. What did not stand was checking a claim like that by eye.

## Phase 9. Pupillometry, the light response, and the learned-model question

Taken up from the ideas list (now at the end of this file) and tracked as rows, because each had the data and the baseline it needed (amendments 13-15):

- [x] 9.2 Pupil diameter, the pure estimator, DONE (amendment 14). A `core/pupil.ts` that reads a luminance field of the eye region plus the iris centre and radius and returns a pupil diameter in millimetres through the same 11.7 mm iris ruler the aperture uses, or `null` when the contrast, the shape consistency, or the physiological plausibility is not there. No `io`, no camera, no wiring — pure and synthetic-tested. Check: synthetic dark-disc fields at known radii recover the diameter; low contrast, an occluded/partial pupil, an implausible size, and a malformed field each return null. **The tick means the code exists and passes its synthetic Check; it does NOT mean pupillometry works — the real-world 9.4 run resolved the pupil in under 1% of webcam seconds, so the estimator is effectively non-functional on this hardware (docs/pupil-light-result.txt).** (Box left unticked by oversight when the estimator shipped ahead of 9.3a; reconciled 5 September 2026.)
- [x] 9.3a Pupil diameter, the pure pixels-to-field half, DONE 4 September 2026. `core/pupil.ts` gains `luminanceField(rgba, frameWidth, frameHeight, box)`: crop a rectangle of an RGBA frame to a grayscale (Rec. 601) luminance field in [0,1], the pure half of reading the eye off the canvas, refusing a box off the frame or a pixel array that does not match the frame. Check: RGBA converts to luminance, crops correctly, feeds the estimator end to end from raw pixels to a recovered diameter, and refuses the malformed cases. (Split from 9.3 so the camera-free, testable half lands before the io wiring.)
- [x] 9.3b Pupil diameter wired, DONE 4 September 2026. The io layer (`readVideoPixels`) reads the iris-region pixels from an OFFSCREEN, full-resolution, unmirrored copy of the clean video (not the visible 640px canvas, which is downscaled and may carry overlays), a pure `irisSampleRegion` boxes the iris and translates its centre, and `luminanceField` + `pupilDiameterMm` turn that into a `pupilDiameterMm` column in the per-second export (blank, not zero, on refusal) and an "Eyes" readout. Check: the column's presence and its null-is-blank serialization are unit-tested (`csvHeader`/`serializeRecords` against the record's own keys), `irisSampleRegion` is pinned end to end from raw pixels to a recovered diameter, and the coverage floor holds; the pupil number's real-eye behaviour is 9.4's job (a headless clip has no resolvable pupil, so an e2e could only re-assert the header the unit tests already pin). No measurement the detector or score reads changes.
- [x] 9.4a Light-response stimulus, the pure schedule, DONE 4 September 2026 (amendment 15). `src/core/lightSchedule.ts`: the stimulus fixed as timing, not pixels — a 20 s settle the analysis discards, then six cycles of 20 s dark then 20 s bright (260 s total). `lightPhaseAt(elapsedMs)` returns the phase the screen should show at any moment from the stimulus start; `lightScheduleTransitions()` lists every boundary; `lightStimulusMetadataRows(startMs)` (in `sessionMetadata.ts`) writes the schedule and the stimulus's moment-zero into the export in the `timestampMs` clock, so the analysis recovers a row's phase as `lightPhaseAt(row.timestampMs - startMs)`, or writes nothing when there was no stimulus. Nothing paints yet. Check: every phase boundary, the transition list, the agreement between the two, and the exact metadata rows are unit-tested; the loader already reads arbitrary `# key: value` metadata, so no analysis change. (Split from 9.4 at the core/io fence, like 9.3a before 9.3b.)
- [x] 9.4b Light-response stimulus, wired, DONE 4 September 2026 (amendment 15). A "Light response" button (enabled once a live camera session is recording) raises a fullscreen overlay that paints the 9.4a schedule — near-black through settle and dark, near-white through bright — driven by `lightPhaseAt` off `performance.now()`, the clock the per-second records share. The camera keeps recording underneath; at export `lightStimulusMetadataRows` writes the schedule and the stimulus start, so the analysis recovers each row's phase by subtraction. A pure `lightPhaseBackground` makes the colour-per-phase a tested decision. Esc (or leaving fullscreen) ends it; the start stays logged. Check: three unit tests pin the colours and one Playwright test drives the button, asserts the overlay paints the settle phase, and reads the schedule plus start back out of the exported file (the 260s run is not waited out — the phase logic is unit-pinned). The overlay carries no inline `display`, so `hidden` alone controls it.
- [x] 9.4 The light-response experiment, PRE-REGISTERED before it runs — first run INCONCLUSIVE, 4 September 2026. Plan committed in `docs/pupil-light-plan.md` before the instrument (9.3b) was even wired: a screen-brightness stimulus (six 20 s dark/bright cycles), the prediction that the pupil constricts to the bright screen committed in writing first, then measured on the maintainer's camera. The maintainer ran it behind the 9.4b stimulus screen; `analysis/blinklab/light_response.py` + `analysis/tools/pupil_light.py` recover each second's phase from the logged stimulus start, apply the usable-data gate first, and only then compare. Result in `docs/pupil-light-result.txt`: the estimator resolved the pupil in 1 of 239 in-window seconds (0.4%, 0 dark / 1 bright), so the gate did not clear and the verdict is **inconclusive** — the plan's committed most-likely-non-detection branch, recorded as plainly as a detection. Face and pose tracking were flawless, localising the limit to the small (43 px) webcam iris interior; eyeglass reflections are a likely aggravator. The plan invites a closer/no-glasses/darker re-run, appended to the result page; the analysis method is fixed and untouched. Check: the plan was committed before any measurement, the committed tool is tested on synthetic detected/null/inconclusive cases and pins the schedule to the browser's, and the result page carries the prediction beside the outcome.
- [x] 9.1 Does a learned weighting beat the demo alertness heuristic? DONE 4 September 2026. Pre-registered in `docs/alertness-score-plan.md`; an analysis-only head-to-head on the UTA-RLDD usable set (threshold-free alert-vs-drowsy AUC, `analysis/blinklab/alertness.py` + `tools/compare_alertness.py`, two shuffle controls, a two-bar rule). Result in `docs/alertness-score-result.txt`: on 95 alert/drowsy videos from 52 subjects the demo heuristic separates drowsiness at AUC 0.70 (p 0.001, Bar 1 CLEARED — the score the README calls unchecked is above chance across strangers), while the learned model's higher 0.78 does not beat it beyond the shuffle (+0.083, p 0.099, Bar 2 NOT cleared). Verdict: KEEP the heuristic; `src/core/score.ts` is unchanged. The committed prediction was half wrong (Bar 1 right, Bar 2 predicted to hold and did not), recorded as such. Adopting a learned score would have been a separate increment gated on Bar 2, which was not met.

## Phase 10. Pay the ledger, then make the live instrument honest

Nothing here needs a volunteer. It closes published results' own loose ends, installs the guards, and fixes the seams the September audit found under the live camera path and the personal ruler, before any new signal is built on them. Ladder items in `REMEDIATION-2026-09.md` are named in brackets; the ladder holds the reasoning and the fix order, this table holds the status. Rows that can move the Eyeblink8 count say so; they carry a committed prediction and land after 10.8a's regression run, never before it.

- [x] 10.1a Detector-change measurement ratchet, DONE 5 September 2026: docs/eyeblink8-result.txt carries a "Built from commit" anchor (the 2 September awake run), and tools/detectorRatchet.mjs reddens CI when any of 16 named detector sources gains a commit past the anchor without a dated DETECTOR CHANGED caveat naming it — and reddens from the other side when a caveat still reads "not yet re-measured" on a fresh anchor. Check: 12 tests, watched failing first — the verdict truth table both ways, a disk-existence pin so a rename cannot silently drop a file from the watch, and the live repo held coherent. The ratchet found a real staleness in its first hour: the guided blink-line commits (1a6c88f and kin) postdate the anchor, and the honest caveat now stands in the result file until 10.8a's regression run re-measures.
- [x] 10.1b MODEL_CARD's measurement-uncertainty section wired into resultGuard, DONE 5 September 2026: the card states the two measured conditions — precision is preparation-conditioned (the 65 invented are a property of the committed remux; re-encoded derivatives measured 12.4 points lower, one clip's false alarms collapsing 19 to 3 — the planning note's "65 vs 13" conflated this with the re-arm gate's 13 removed and is corrected here from the committed record), and blink duration is device-conditioned (iPhones ~96 ms, Macs 149-166 ms, mechanism open until 10.9). resultGuard's uncertaintySection() holds the section to the result file: the quoted invented count must equal the committed one, and the condition figures are pinned. Check: 5 tests, watched failing first, including the drift case (a re-measure that moves the count reddens the card until it moves too).
- [x] 10.0a1 Truth pass, the page and the card [B1, B3, B6's dating half, B7, B13], DONE 6 September 2026: MODEL_CARD carries the UTA-RLDD alertness result (AUC 0.70, p 0.001, cohort-level, per person unvalidated) and its tested-on table names 54 subjects for the LOSO read and 52 for the alertness read, and tablets as untested; the notice and its six echoes say the model tries to send and this page intercepts it, quoted verbatim into README and MODEL_CARD; "reliable near the centre" becomes "not measured" until 14.9; the cross-engine table is dated and says WebKit (its re-measurement is 13.0); SECURITY.md names a channel that exists. Check: 13 tests, watched failing first — the alertness headline and both subject counts parsed out of their own records and held to the card, the notice quoted verbatim in both documents, the retired gaze claim held out of both, the table's measurement date and commit, and the reporting channel. (10.0a was split into three sub-rows on 6 September 2026 under this file's two-hour rule; no item moved, and 10.0a2 and 10.0a3 carry the rest.)
- [x] 10.0a2 What is kept and what is exported, said in the open [B2, B18], DONE 6 September 2026: README's Privacy section is generated from STORED_ITEMS, names what a CSV contains, and offers a reduced user agent unless the full string is needed; the third-party notice carries the shipped MediaPipe version and the OFL text. Check: every storedData key found in README's Privacy section, from a generator whose committed output the test rebuilds; `lockfileVision().version` quoted in the notice; the OFL preamble and every font family `src/styles.css` imports. Check met by 24 tests, watched failing first, plus one Playwright sequence proving the disclosure and the browser-string checkbox stand in the Session box rather than existing only as tested strings.
- [x] 10.0a3 The generators stop freezing prose [B5], DONE 6 September 2026: the miss mechanism reads as a measured ceiling everywhere the generators print it, every generated sentence a parse or an `assertQuote`, and README's duplicated miss paragraph is dropped. Check: cannotSee.ts regenerated with the ceiling sentence held to docs/iris-occlusion.txt; a test greps the generators for unpinned sentence literals. Check met by 6 tests, watched failing first, and two mutations: a claim stripped of its pins and the limitations bullet returned to a typed literal each reddened their test.
- [x] 10.0b1 The published unit count is what the runner collects, DONE 7 September 2026 [B15]: `actualUnitTestCount` counted `it(` and `test(` calls and vitest ran 1238 cases against 1213 calls, because a table written as a loop around one call is one call and many cases. The number is read from `test/collected-tests.txt`, and `npm run counts:check` asks `vitest list` for its own listing and holds the file to it, in continuous integration beside the suite because a test cannot count the run it is inside. The old grep is kept as `unitTestCallCount` and a test asserts the collected count exceeds it. Check: a hand-edited count in README, STATE or ARCHITECTURE reddens the build; putting the grep's figure back into the file reddens both the guard test and the build step.
- [x] 10.0b2 The published figures come from the current run, not the first match, DONE 7 September 2026 [B14's parsing half]: `parseResultFile` took the first headline that matched anywhere in `docs/eyeblink8-result.txt`, a file that deliberately keeps superseded runs below the current one in the same shape. Row 10.10c1 adds an interval clause inside the recall and precision parentheses, so at the next regeneration the pattern would have skipped the current block and read the 20 August one: precision 81.4% and 78 invented in place of 84.0% and 65, with recall staying right because that block carries an identical recall line, and every byte comparison green because both sides read the same wrong line. The file gains an explicit `--- END OF THE CURRENT RUN ---` boundary, the generated figures are read only above it, the parser refuses a file without it rather than guessing, and the patterns tolerate extra text inside the parentheses so the break does not happen in the first place. Check: a fixture with a new-format current block above an old-format superseded one parses to the current figures; removing the boundary throws by name; both mutations redden.
- [x] 10.0b3 The summaries are held to the files they summarise, DONE 7 September 2026 [B14, D13]: two sentences restated what a machine-readable file already knew and were left to age. CONTRIBUTING hands a contributor one command line and calls it the gates; `.github/workflows/ci.yml` decides what the gates are, and `npm run counts:check` went into continuous integration without following into the document, so the line passed locally and the pull request failed on a step nobody had been told to run. README says which phases are complete; ROADMAP decides that, and README was a phase behind, because Phase 9 finished and the sentence did not move. `tools/gateGuard.mjs` holds both. Counting complete phases meant encoding what this file means by complete: three settled markers, `[x]` done, `[~]` declined, and a `[ ]` row whose text opens `(moved to`, matched narrowly at the head of the row so ordinary prose cannot settle a row nobody did. Check met: 13 tests watched failing first; three mutations — a gate dropped from the command line, a README claiming one phase too many, and a new gate added to continuous integration alone — each redden their own test. One hole was dug and filled inside the row: the first reader also took inline code spans, so the paragraph explaining the drift satisfied the pin by naming the gate while explaining it, and deleting the gate from the command line left the suite green. A gate counts as documented where it is RUN, not where it is discussed. The file-count pin the parent row asked for is dropped: no document in this repository states a file count, so there is nothing to hold.
- [x] 10.0b4 CHANGELOG joins the guarded documents, DONE 7 September 2026 [B17]: the Unreleased section had published "87.7% recall, 83.3% precision, 85.4% F1" since 15 August, in the present tense, in the section that says what is about to ship. That is the THIRD of the five Eyeblink8 figures this project has published, superseded twice since, and nothing in the file said so. README keeps all five in a table and marks which is current; the result file keeps superseded runs below a boundary line; the changelog did neither. `tools/changelogGuard.mjs` reads the three numbers back from the current run through `parseResultFile`, which 10.0b2 had already anchored to the block above `--- END OF THE CURRENT RUN ---`, so the pin follows the headline rather than the first match in a file of history. `statedEyeblink8` takes all three or refuses by name, because a headline missing its F1 is a sentence somebody edited and two thirds of a headline leaves the third drifting unwatched. Check met: 7 tests watched failing first; three mutations — the superseded headline restored, the pointer to the record dropped, and the result file's current run moved — each redden their own test, and the third is the one that matters, because it proves the changelog now follows the measurement rather than sitting beside it.
- [x] 10.0b5 One generated block that says where the project stands, DONE 7 September 2026 [D14]: the September audit found eight documents disagreeing about the state of this project — two August trackers still listing work that had shipped, one of them filing a done thing under deliberately-not-doing, a build log weeks behind, and a state file whose top entry is whatever landed last rather than a summary. The answer is not a ninth hand-written summary. `tools/statusBlock.mjs` builds a block at the top of STATE.md in which every figure is READ from the file that owns it: the phase count from ROADMAP's own rows through `phasesComplete`, the Eyeblink8 headline from the current run through `parseResultFile`, the suite sizes from the files the runners write, the bundle ceiling imported from the constant rather than parsed out of its source. `node tools/writeStatusBlock.mjs` rewrites it and a test rebuilds and compares byte for byte, so nothing in it can be edited into agreement with a document that has drifted. `REMEDIATION.md`, `NEEDS-REVIEW.md` and `docs/log.md` are retired at their own FIRST content line, not further down, because a notice below a page of prose is one the reader reaches after believing the page; each points at REMEDIATION-2026-09.md and each is kept rather than deleted, the rule this project already applies to a superseded measurement. Check met: the block regenerates byte-identically; 11 tests watched failing first; three mutations — a number hand-edited inside the block, a retirement notice moved below the prose, and a roadmap row reopened so the phase count falls — each redden their own test.
- [x] 10.0b6 The stamp that says a document was READ, not touched, DONE 7 September 2026 [B14's stamp half]: three documents already carried a written-or-revised stamp, and a test reddened when a stamped file changed without it moving. That catches a document edited and left dated wrongly. It cannot catch what the September audit actually found — sentences true when written and false when read, in files edited many times since, every edit bumping the stamp. The missing act is not editing but READING. `tools/readGuard.mjs` holds a "Read in full on <date>, claims <digest>" stamp to the document's CLAIM TEXT: the prose with its generated blocks, its count figures and its written-or-revised dates normalised away, so a number the machine updated is not a changed claim and a gate that fires on every count bump does not become a rubber stamp inside a week. Six current-description documents carry it; STATE.md and LEARNING.md are deliberately excluded as append-only logs, where an old entry does not become false when a new one lands. THE FIRST READ FOUND NINE THINGS. SECURITY.md said the absence of a Content-Security-Policy was "not a defect this repository can fix", which is false — a policy can be delivered as a meta tag and roadmap 10.2b is that work — and it pointed at the retired REMEDIATION.md as where open items live. ARCHITECTURE.md carried a third, unpinned copy of the gate list, missing four gates and attributed to STATE.md which never held it, and said the README prints three figures where it prints five. CONTRIBUTING.md's document table listed all three retired trackers as current and omitted the live one. README.md said "the current run has 50 misses" where it has 67 — a correction that had itself gone stale, three runs out of date — and pointed at the retired tracker too. MODEL_CARD.md said the published figure had been "wrong twice" where it has been corrected four times. Three of those drifts were created by row 10.0b5 one hour earlier, which retired the trackers without updating the documents that point at them. All nine are fixed here. PROJECT.md's two intent lines are NOT fixed: they are flagged in the file itself as under ADR-0005 and left for the owner. Check met: 11 tests watched failing first; three mutations — a claim changed without the stamp moving, a count moved which must NOT go stale, and a stamp stripped of its digest — each behave as designed. `guardsArmed` caught that the tool was first named outside the repository's own `*Guard|Block|Ratchet` convention, which is what that check is for; it is `readGuard.mjs` now and the count goes 18 to 19.
- [x] 10.0b7 The guard on the guards reads the import, not the filename, DONE 7 September 2026: found while row 12.6 looked for somewhere honest to publish a guard count. `tools/guardsArmed.mjs` answered BOTH halves of its question from filenames — a module was a guard if it was CALLED one (`*(Guard|Block|Ratchet).mjs`, the convention 10.0b6 renamed `readInFull.mjs` to satisfy) and it was armed if a test file was NAMED after it — and never once asked whether that test imports that module. Both halves were holed, and the second one in the live tree: the hand-kept `NAMED_DIFFERENTLY` map sent `bundleGuard` to `bundleBudget.test.ts`, a file that imports `bundleBudget.mjs` and nothing else, so `bundleGuard.mjs`'s own real test was watched by nothing. Both halves read what a module IS now. A hand-written `.d.mts` sibling is the mark of a rule-carrier, because that file exists exactly when a type-checked caller imports the module, and it brings in the six the name pattern missed: `modelProvenance`, `bundleBudget`, `metadataKeys`, `wilson`, `writeVerdictFixtures` and `guardsArmed` itself. Armed means a test IMPORTS it, anchored on `from "..."` rather than the path anywhere in the text, because `claimGuard.test.ts` lists `tools/claimGuard.mjs` among its exemptions and a looser reader would have counted an exemption list as an import list — the guard making its own mistake one layer down. `NAMED_DIFFERENTLY` is deleted rather than extended; there is nothing left to keep by hand. Check met: 20 modules watched became 26; the bundleGuard case was watched failing first EMPIRICALLY, by moving `test/tools/bundleGuard.test.ts` out of the tree and seeing `unarmedGuards` name it where the old rule returned an empty list; a stated count of 26 keeps adding a module a conscious act; `orphanDeclarations` reports a `.d.mts` whose module is gone, which is a type that lies. The changelog half is done with it: CHANGELOG.md's Unreleased section had said "Six checks" since 15 August while the count reached 26, in the section that describes what is about to ship, and `tools/changelogGuard.mjs` now reads that number back from the declarations on disk. It THROWS on a section stating no count, because reporting "nothing claimed" would make the check satisfiable with a delete. One residue, stated rather than hidden: a static reader can prove a test imports a module and cannot prove it reaches one, so the stated count is still the moment to ask that by hand.
- [x] 10.0b8 The startable sentence reads the phase gate, not only the row, DONE 7 September 2026: amendment 19's guard holds a claimed row to its own LINE — open, unmarked — and a row passes that while the header of its PHASE forbids it from beginning at all, which is how three rows started with the Phase 12 gate shut (amendment 20). `tools/roadmapGuard.mjs` reads the gate out of the header now: `phaseGates` finds every phase whose header carries one, `expandRowRange` turns the prose runs `10.12a-c` and `10.13a-b` into rows, and `gatedStartables` refuses a claimed row whose phase is gated, that the header does not exempt by name, and whose prerequisites are not all ticked — naming the FIRST one still open, because one thing to go and do is more use than seven. Check met on every clause. An item the reader cannot parse THROWS rather than returning an empty list, because a gate parsed to nothing permits everything and looks exactly like a gate nothing is violating; `12.17's tool` is the live case, an exemption for a TOOL and not the row, kept in `exemptOther` and pinned by test rather than silently dropped, since reading it as row 12.17 would open the gate for a row nobody exempted. Watched failing first against the REAL ladder: claiming 12.7, whose every Check clause is satisfiable here, passes `staleStartables` and is named by `gatedStartables`, which is precisely the gap that cost three rows. **A defect of the row's own kind was made and caught on the way:** the first two patterns excluded dots, and every row number contains one, so they matched no gate at all and the guard reported a clean ladder — the exact failure this row exists to prevent, arriving inside the fix for it. The tests caught it because they pin the parsed prerequisite list rather than only the verdict, and the comment now says why both patterns terminate on their sentence instead.
- [x] 10.0b9 The gate caveats retire themselves, DONE 7 September 2026: amendment 20 left three rows carrying a caveat — started with the gate shut, unwired so nothing published depends on them, constants chosen against an instrument 13.8b will re-time and 12.0a will re-rule, re-look when those land. That last clause was prose, and a sentence kept true by somebody remembering is the single thing this repository has watched fail over and over: a count in a changelog, a list in the ladder, a header nobody re-reads. Each caveat NAMES the rows it waits on now, and `ripeCaveats` reddens the build the moment one is ticked, on the self-retiring pattern `drozyGuard` and the detector ratchet already use — the reminder arrives when the instrument moves rather than when a person happens to look. Check met: a caveat naming nothing to wait for throws, the same refusal `blockedRows` makes; the FIRST row to land fires it rather than the last, because waiting for all of them would let the earliest change pass unexamined and the earliest is the one that moves the instrument under a constant nobody has re-read; the ladder is quiet today because both rows are open; and flipping 13.8b to ticked in a scratch copy names all three, watched failing first. **The same defect as 10.0b8's, made twenty minutes after writing that row up.** The first draft of the pattern excluded dots and captured `13` out of `13.8b, 12.0a`. Twice in an hour is a shape rather than a lapse: a sentence-ending dot and a version-numbering dot are the same character, and this ladder's identifiers are full of the second kind. Both patterns terminate on their own punctuation now and the comment says why. Recorded rather than quietly fixed, because the second occurrence is the evidence that the first write-up was a memory and not a mechanism.
- [x] 10.2c The blocked list says which KIND of stuck each row is, DONE 7 September 2026: two corrections from one finding, and a rule so the pair cannot recur. Row 12.1 is RETIRED on the owner's ruling (see its row) and its BLOCKED marker goes with it, because for a day it carried both — a line saying the work waits on something and a line saying there is nothing to wait for. BLOCKED and RETIRED are different verdicts and the difference is the marker's whole use: BLOCKED promises a row is ready the moment its blocker lifts, which is what amendment 18 exists to say, and RETIRED says do not pick this up at all. `retiredWithBlocker` refuses a row claiming both, watched failing first. Row 10.2b's marker is CORRECTED: it said only that no WebKit is installed here or in `ci.yml`, which is true and reads like an accident — and reading it that way is exactly what happened, when the next reader proposed adding WebKit to continuous integration and had to withdraw it on opening `playwright.config.ts`, which carries a WebKit project excluded from CI by a RECORDED DECISION: an earlier attempt produced "three failures in a row that were about the platform, not the code", and the file concludes that a test failing for unrelated reasons teaches people to ignore red. The marker now names that decision, points at the file, and says where the protection does live: `npm run e2e` on a Mac covers both engines, and Safari proper is manual check 58. **The other six markers were swept for the same shape and are clean** — 10.12b names the numbers-only permission, 12.6b the traces the evidence README records as deliberately uncommitted, 12.10a the corpus directory row 10.10c2b places on the owner's machine, 12.0b and 12.4 name rows, and 13.4 names a camera this container does not have, which is irreducible. A blocker that states a surface fact where a decision exists sends the next reader to reverse the decision, and that cost was paid once today before it was written down.
- [x] 10.0b10 An exemption follows the row, not its spelling, DONE 7 September 2026: found the moment 12.16 was split. Phase 12's gate exempts `12.16` by name, this ladder splits a row that outgrows one pull request into lettered halves because an era rule tells it to, and `gatedStartables` then refused 12.16b — a row that is 12.16, split for SIZE and for no other reason. The two available fixes were not equal. Editing amendment 17's exemption list to add each new letter would mean a dated amendment gets rewritten every time somebody splits a row, which is a gate widened by a rename and by whoever happens to be doing the renaming. So the RULE moves instead: `rowAncestry` walks a row up its lettered parents (12.16b to 12.16, 10.0b9 to 10.0b to 10.0) and an exemption anywhere on that chain carries down. Only the chain counts, never a neighbour — 9.4 is not a part of 9.5 and no reader of the gate would think it was, and a test pins that separately. Watched failing first, on the live ladder rather than a fixture: the guard named 12.16b before the rule existed, which is the guard from 10.0b8 doing exactly its job on the person who wrote it, one row later.
- [x] 10.0b Truth pass, the last of it, DONE 7 September 2026 [D13; B15 as 10.0b1, the result parser as 10.0b2, the summary pins as 10.0b3, the changelog as 10.0b4, the status block and retired trackers as 10.0b5, the read stamp as 10.0b6, the guard on the guards as 10.0b7, the phase gate as 10.0b8, the self-retiring caveats as 10.0b9]: what remained after those nine was ADR-0005, and it was the only thing in the pass that no amount of reading could settle, because it was a decision about intent rather than a fact in the record. The owner ruled on both halves. EXPLAINABILITY KEEPS PRIORITY and the tie-break is written down rather than left to the moment it arrives: an accuracy gain is taken when it can be explained, and one that cannot is recorded and not shipped — into the result files as a measured fact about what is possible, never into the page or the score. Amendment 16's goal of raising accuracy to this hardware's ceiling is pursued through instrument work, a corrected stepper and an honest frame driver and a personal ruler, rather than through a method nobody can audit; 12.3 was already framed that way, and now the framing is a rule. MOBILE IS IN SCOPE, as a measurement surface, which brings PROJECT.md to work that shipped weeks ago: 14.0b's phone surface is merged, a 375-wide viewport runs in the end-to-end suite on every pull request, and MODEL_CARD's device list already records phone sessions behind the published numbers. Capability stays per SETUP and not per device class, which amendment 16 settled and which this project's own dry run demanded when a phone out-resolved the DSLR rig. Both lines in PROJECT.md are corrected, the open-decision note that stood over them since the morning is retired, and the file that says what this project is for now agrees with what it ships — which is the standard every other document here is held to by test.
- [x] 10.1c Refusal constants pinned at their boundaries and the mutation runner revived [D2, D16], DONE 6 September 2026: one literal probe per refusal constant in its owning test (24.9/25/25.1; 59.9/60/64.9/65; 14 999/15 000 ms; 99/100 samples; last+2000/2001; 29/30 guided samples), tools/mutationCheck.mjs repaired (RISE entries dropped, these constants added, each entry runs only the test it expects to redden) and run in CI; claimGuard matches a regex family and PROJECT.md's one-word-away restatement is reworded while README's "of ours" scope is stated; exportGuard matches both quote styles and `session-*.json`; a guardsArmed test asserts a sibling test per tools/*Guard|Block|Ratchet.mjs; `npm ci --ignore-scripts` in both workflows. Check: the runner is green on main and red for `MIN_BLINK_FPS = 24` and `= 26`; six of the audit's surviving mutations redden. All 27 entries are caught in about two minutes. Narrowing each entry to its owning test file turned 10 of them from caught into SURVIVED first — nine pose-gate mutations and the blink refractory, whose owning tests also derived their probes — and those two files carry literals now.
- [ ] 10.1d The headline becomes re-derivable [D1, D3, A12]: the anchor run's eight blink logs, the 408-row annotation table and the 65-row false-alarm table committed under docs/evidence/ (numbers only, the class DATASETS.md already rules on); `analysis/tests/test_evaluate_eyeblink8.py` reproduces "341 of 408", 65, 83.6/84.0/83.8 and every per-clip row; the evaluator exits non-zero on a partial corpus, prints its skips into the report body, refuses a clip whose coverage gap or missing `frames_measured` header it cannot explain, and says "not computable" for a glasses split it cannot form; a dated paragraph names which candidate produces the published "+1" on two clips or that it is unresolved. Check: a pytest binds the committed miss CSV to the published miss column (67 rows, 47/67 = 70.1%); a watched-mode log, a >1% coverage gap and a missing header each turn a test red.
- [x] 10.1e Pages waits for CI [D5], DONE 6 September 2026: deploy on `workflow_run` of CI, guarded on success, with `head_sha` passed to checkout and `GITHUB_SHA` set for the provenance stamp. Check: 8 tests, watched failing first, in a new `tools/deployGuard.mjs` that reads both workflow files — the trigger set with no `push` in it, the CI workflow's own name held to the string the deploy watches for, the success condition, the checked-out ref and the stamped `GITHUB_SHA`, and that `vite.config.ts` still reads that variable. The reader refuses a file whose `on:` block it cannot parse rather than reporting no triggers. The live half of the check, that a red push does not publish and that the deployed commit line matches the green run, is observable only after this merges.
- [x] 10.1f1 The metadata contract, written down and asserted [D6's contract half, B16], DONE 6 September 2026: `analysis/tests/test_metadata_contract.py` in the shape of `test_csv_contract.py`, reading every key out of the seven TypeScript writers — `sessionMetadata.ts`, `blinkLog.ts`, `csv.ts`, `frameClock.ts`, `frameTrace.ts`, `kss.ts`, `stepCalibration.ts` — including the multi-line `line(` calls a one-line regex misses; a hand-written EXPECTED_WRITTEN, a READ_BY_PYTHON list, and a grep that every `metadata.get("...")` literal is in it. SPEC.md gains "### The session metadata block" as a table (key, when written, value format, which reader consumes it) and its ts block and state table are brought current. Check met by 18 tests, 11 in Python and 7 in TypeScript, and by the rename mutation run for real: `sampled_fps` renamed reddens 3 Python tests and 2 TypeScript tests. Renaming it reddened Python ONLY until the TypeScript reader was added, which is why there are two readers rather than one. Each reader is fed a case it must find and holds a floor on how many keys it finds in the real repository. SPEC.md also gains the two FeatureRecord fields and the two CameraStates it was behind on, and the record block is held to the CSV columns.
- [x] 10.1f2 The keys nothing read, now read [D6's honesty half], DONE 6 September 2026: `records_dropped`, `app_commit` and `protocol` are carried on `Session`, an unreadable dropped-row count is refused at load rather than defaulted to zero, and `cohort_commits`/`cohort_commit_line` say whether a table describes one instrument, three or a set of files that predate the build stamp. The round report prints that line above its tables. Check: 16 tests, 10 new in `test_loader.py` and a new `test_validation_report.py` (that module had no test file at all, so nothing could go red when the report's own assembly changed); the byte-frozen round I report was regenerated and carries the new line. One correction to the ladder's list of four cohort tools: `analyse_drozy.py` and `analyse_rldd.py` analyse DROZY and UTA-RLDD feature records, not browser exports, so they carry no `app_commit` and a commit line there would promise a field that cannot exist; only `validation_report.py` and the pilot path read sessions this browser wrote.
- [x] 10.1f3 When each key is written, exercised rather than described [D6's presence half], DONE 6 September 2026: the 57 keys split into the 21 every export carries and the 36 written only when the thing they describe happened, each with its condition in words, in `analysis/tests/test_metadata_contract.py`; SPEC.md's when-written column becomes a promise a reader can act on, and `test/core/metadataPresence.test.ts` holds it to the real row builders called with the arguments three sessions supply. The builder list is read out of `exportSession` rather than copied, so a builder added to the export cannot leave its keys unclassified. Check: 14 tests, watched failing first on seven real drifts; one mutation reddens both sides, and a writer that stops writing a row unconditionally reddens three. Absent and unknown are different claims and the column had merged them: `line()` writes `unknown` rather than dropping a row, so six keys described as sometimes-absent are always written, and `feature_records_dropped` was described as unconditional while the writer omits it when nothing was dropped. Two value formats were wrong with them: `median_iris_width_px` carries one decimal and `clip_duration_s` three.
- [x] 10.1f4 The absence policies at the read sites [D6's reading half], DONE 7 September 2026: `analysis/tests/test_absence_policy.py` states, for every key this folder reads, which reader consults it and what that reader does without it, and exercises both — the reader is called with a block it can read, then with the same block minus one key. Nineteen read sites, three kinds of answer (a default, an unknown, a refusal), each with the judgement in words. `round2._number` and `verdict._finite` each now name the other and say why one defaults and the other refuses: a verdict is a claim about one session, so an unreadable input means the claim cannot be made, while a round-2 table row survives a blank cell and refusing there would throw away a participant over one absent rate. Check: 24 tests; two mutations run rather than assumed — a refusal turned into a default and a default turned into a different default each redden exactly the site they belong to. One reader's stated policy was the opposite of the writer's: `Session.records_dropped` said the row was on every export so a missing key meant an older build, and 10.1f3 had just exercised the writer and found it written only when rows were dropped. Two read sites merge an absent row with an unreadable one; both are now named as choices rather than left silent.
- [ ] 10.1f4b The loader's floor, if there is to be one [D6's refusal, deferred to the owner]: the ladder asks for `loader.py` to refuse a session missing a key every export writes. It cannot be done as written without deciding to abandon older files: `analysis/tests/fixtures/session-fixture.csv`, which `test_loader.py` and `test_plot.py` both load, carries two metadata keys of 57, and the validation round's own six files predate most of the block. Which generations of file the analysis track still accepts is the owner's call, not a test's. Check: once a floor is chosen, a file below it is refused by name and every committed fixture either clears the floor or is regenerated.
- [x] 10.1f5 The verdict fixtures generated rather than written [D6's fixture half], DONE 7 September 2026: `test/support/verdictFixtures.ts` describes each fixture session once and derives both sides of the pin from it — the CSV through the twelve row builders `exportSession` calls, in the order read out of `main.ts`, and the page-state inputs through the real `asExported` and a replay of the real `rulerFitStep` over the same records. The hand-typed CSVs and the hand-typed literals beside them are both gone. Check: 13 tests; the mutation run both ways — renaming `pose_valid_fraction` reddens all four fixture byte comparisons where before it left the whole pin green, and regenerating the fixtures under that rename then reddens five Python tests, which is the mirror refusing to reproduce the committed verdict from bytes that lost a key. Three drifts the hand-typed files had already accumulated: `kss_before: 3` where the writer can only emit `3 (Alert)`, bare newlines where the exporter writes CRLF, and a header two columns behind. The 59.96 boundary is 10.1f6.
- [x] 10.1f6 The other rounding boundary [D6's fixture half, remainder], DONE 7 September 2026: a `risk-edge` fixture at a measured 59.96, which the file carries as 60.0. The refusal floor at 25 has been pinned since 10.15; the risk threshold at 60 was the same defect one step up and was pinned nowhere, so a session could read "above the 60 risk band" on one side and "quick or shallow blinks can be missed below 60" on the other. Check met: 5 tests, watched failing first, and the mutation run — handing the page the raw double instead of the exported one reddens both boundary fixtures where before it reddened one. The Python byte pin now finds its fixtures on disk rather than naming four of them in four tests, so a fixture added on the generating side cannot go unpinned on the deriving side.
- [x] 10.1g2 The published Python count means test functions, not tests, DONE 7 September 2026: `actualPythonTestCount` counts `def test_` and the runner collects 455, because parametrised tests expand. The gap was 38 before 10.1f4 added a parametrised table and is 42 now, and every document says "N Python tests ... all green", which reads as a claim about the run. Check: the counter and the runner agree, or the sentence says which number it is and a test holds it to that.
- [x] 10.1g3 The match tolerance is pinned by what it does, DONE 7 September 2026 [D4's tolerance half]: `DEFAULT_TOLERANCE_FRAMES` decides every published recall and precision and nothing held it. A pair of tests calls `match_blinks` without a tolerance argument, so the shipped default is under test rather than a literal repeated in the test: four frames apart matches, five frames apart does not, and a third holds the constant to the duration the module's own comment claims. Check: the pin reddens at 3, at 5 and at 6, and is green at 4.
- [x] 10.1g4 The ratchet watches the whole chain, and reads declarations where they are written, DONE 7 September 2026 [D4]: `DETECTOR_SOURCES` gains the stepper (`videoStepper.ts`, `stepCalibration.ts`), the runner (`measure_corpus.mjs`), the Python scorer (`blink_match.py`, `blink_log.py`, `eyeblink8.py`, `evaluate_eyeblink8.py`) and `package-lock.json`, so all four links of the chain that produces a published recall are watched rather than one. The declaration search is scoped to the caveat block: it read the whole file, and the whole file is mostly a table, so any seven-character hexadecimal run in the data counted as naming a commit. Every commit since the anchor that touches a newly watched file is named in the caveat with an argument at a stated strength, and the one that CAN move the numbers says so and points at 10.8a. Check: a short sha or a subject appearing only above the caveat is refused; the four commits are named; the ratchet is green.
- [x] 10.1g The ratchet watches the whole chain, DONE 7 September 2026 [D4; the watch list and the caveat scoping as 10.1g4, the tolerance pin as 10.1g3, the provenance half here]: MODEL_CARD pinned the model, the runtime and the landmarker options and said nothing about the browser, though every published Eyeblink8 number was produced by stepping real clips in a real browser that Playwright launched. A new instrument section states the engine the runner actually launches, the `@playwright/test` version the lockfile pins, and the WebKit and Chromium versions that Playwright bundles, all three read back from the committed lockfile and from the manifest inside the pinned `playwright-core` rather than from a comment. The section says plainly that these describe the instrument as it stands and not as it stood, because the browser that produced the 25 and 26 August runs was never written down and cannot now be recovered; the record only says the second machine had "a different WebKit binary". `runnerEngine` refuses rather than guesses when a source launches nothing, two engines, or one it never imported, because a card naming a browser no measurement ran in is worse than one naming none. Dependabot excludes Playwright from its grouped minor-and-patch updates, so a bump that can move a published measurement arrives as its own pull request instead of under one title with nine others. Check met: a Playwright bump that leaves MODEL_CARD behind reddens the build, and it reddens twice, because `package-lock.json` is under the detector ratchet from 10.1g4 and the bump must also be declared or re-measured. 11 tests watched failing first; three mutations — a bumped lockfile version, a runner switched to Chromium, and Playwright put back inside the group — each redden their own test.
- [x] 10.2 Vendored-model provenance joins the ratchet, DONE 5 September 2026: MODEL_CARD gains a "Model provenance" section pinning the model file's sha256, the runtime's lockfile version and integrity, and the landmarker options (blendshapes stated `false` explicitly, the pin 12.2 flips); tools/modelProvenance.mjs recomputes every line from the committed artifacts, and the model file plus src/io/landmarker.ts join the detector ratchet's watch, so a swap without a re-measure or caveat is a red build twice over. The WASM bundle is pinned through the lockfile integrity rather than hashed from disk — public/mediapipe-wasm is generated at build time from the locked package, so the lockfile IS its committed provenance. Check: 9 tests, watched failing first — the card parsed and recomputed (hash, version, integrity prefix, option-for-option against the real source), absent-blendshapes read as disabled never unknown, and the ratchet list containing both new paths.
- [ ] 10.2b A Content-Security-Policy closes the telemetry residual at the network layer [D9]: `connect-src 'self'` (plus `blob:` only if the model loader needs it) as a meta policy, the wrapper kept as the second layer, SECURITY.md and ADR-0004 corrected. Check: the full e2e suite including the WebKit stepped clip passes under the policy; the 70-second telemetry e2e still proves the wire is live before asserting silence. **BLOCKED: on one WebKit run the owner makes, and NOT on an accident of tooling. This Check wants the full end-to-end suite including the WebKit stepped clip to pass under the policy. `playwright.config.ts` carries a WebKit project and runs it LOCALLY ONLY, by a recorded decision rather than by omission: an earlier attempt to keep WebKit green in continuous integration produced, in that file's own words, "three failures in a row that were about the platform, not the code: no MediaRecorder at all, and a stepped clip stopping at 27 frames of 60", and it concludes that a test failing for reasons unrelated to the change under review teaches people to ignore red. So `npm run e2e` on a Mac covers both engines and is where this protection lives; continuous integration covers Chromium; Safari proper is manual check 58, because Playwright's WebKit is not Safari either. A Content-Security-Policy verified in one engine and shipped to a page real Safari users open is the kind of unverified change this repository refuses, so the policy waits rather than being guessed at. **The first version of this marker said only that no WebKit is installed here or in `ci.yml`** — true, and it reads like an accident, which is exactly why the next reader proposed adding WebKit to CI and had to withdraw it on finding the config. A blocker that names a surface fact where a recorded decision exists sends people to reverse the decision. Corrected 7 September 2026.**
- [ ] 10.3 DROZY shape rows, DECIDED by amendment 17 [B4]: the three shape correlations are retired rather than re-measured, because the ~3-hour re-measure would re-run a null on a sample missing its sleepiest sessions, on a rate the stepper had not verified, to defend verdicts that pass by chance at p 0.5 (10.10c appends that chance rate). drozyGuard gains a per-feature source map so the caveat must name every feature whose sources moved since bd2a98d, and README's DROZY paragraph says which rows are stale (all seven). Check: guard map test failing first on an unmapped feature; the ruling dated in docs/drozy-result.txt and DATASETS.md; the sample is re-measured only if 12.18's v2 read moves a shape feature.
- [x] 10.4 Long-session export honesty, DONE 5 September 2026: the drop site counts every per-second row the 3600-row buffer discards, and a nonzero count writes `feature_records_dropped` plus a plain-sentence note into the export (featureRecordOverrunRows in sessionMetadata.ts, on the blink log's WARNING precedent); the participant report's truncation line carries the same count. Absent when nothing dropped, never a zero row. Check: under-cap emits nothing at all; an over-cap count emits both rows with the exact count; the note cites the cap from the one shared constant.
- [ ] 10.5 Iris-signal positive control: the caveat docs/iris-occlusion.txt names in its own last paragraph, closed with a per-blink table over the caught blinks. Check: dated prediction appended before the tool reads real spans; tool tests watched failing first on synthetic tables. The cheapest claim row on the board. (Prediction, decision rule and the committed tool — analysis/blinklab/iris_control.py, analysis/tools/iris_positive_control.py, 17 tests — shipped 5 September 2026; the tick waits on the run, which needs the caught-blink span extraction from the machine that holds the measured folder.)
- [ ] 10.6 False-alarm autopsy: the 65 false alarms get the mechanism treatment the 67 misses got, preparation-sensitivity as a droppable sub-question. Check: current tables regenerated and committed first; numeric predictions committed before any real table is read; verdict CSV under docs/evidence/.
- [x] 10.7a Aperture-mm noise floor, DONE 5 September 2026: the prediction committed first (four numeric bounds, its own commit), then src/core/apertureNoise.ts measured the fixture through the real pipeline — median frame-to-frame wobble 0.020/0.019 mm per eye (an order of magnitude under the 0.4 mm shallow-blink band, so temporal landmark noise is not what hides shallow blinks), p95 0.286/0.242 mm (the fatter tail any per-frame threshold must respect), cross-eye disagreement 0.200 mm median and 0.860 mm p95 (ten times the temporal floor). All four predictions held, two exactly at their boundaries. Result in docs/aperture-noise-floor.txt, recomputed by test on every run so it cannot drift. Check: pure stats pinned on hand-built series (a delta never bridges an excluded blink, the mask uses each series' own median), and the committed numbers held to the fixture recomputation.
- [ ] 10.7b Cross-iris disagreement refusal, threshold DERIVED from 10.7a's measured cross-eye distribution within the pose gate, not chosen. Check: the constant held at or above the p95 parsed from docs/aperture-noise-floor.txt (the 10.10b pattern), so a threshold below it reddens the build unless the doc carries the stated reason.
- [ ] 10.7c Ruler scheme identity-invariance check, only if a pooled-ruler change is ever proposed: prediction with a committed adopt-or-revert rule, gated on 10.7a's floor proving the current scheme noisier. Check: a ratchet entry — a commit touching rulerFit.ts, baseline.ts or calibrationWindow.ts requires a dated docs line naming the floor comparison. Conditional; it never ticks on its own and does not gate Phase 15.
- [ ] 10.7d Iris ruler physical check, owner [the scale-bar half of B12]: a printed scale bar at three distances on the owner's setup. Check: prediction committed first (ruler within ±5% of the bar at all three distances); absolute-mm thresholds kept out of the measurement path; the result written beside the ±4% population spread 10.10c already states.
- [x] 10.13a The line travels with the number [A8, A15's columns; G-Guided b-11], DONE 7 September 2026: `blinkLineMm`, `blinkLineSource` (none | fixed | passive | guided), `shutLineMm` and `shutLineSource` appended to CSV_COLUMNS and FeatureRecord by the trailing-append discipline, so every older header stays an exact prefix and every file already exported still loads. `src/core/lineProvenance.ts` decides nothing: the wiring already picks the line and this reports the pick, because a second opinion about the line would be a second line. The refusal now withholds what `docs/calibration-refusal.txt` always promised — durations, amplitude and velocity went with the rate, all four on the one `blinksWithheld` the readout rides, a decision that used to be made separately in four places. A stored line is refused outright on a clip and the calibrate button is gated on a live camera, closing both directions of the same mistake: three seconds of a recorded stranger's eye could become the visitor's line, and the visitor's line could measure a stranger. The stored shape now carries the conditions it was measured under, and the live camera is checked against them; that check is a FLAG rather than a refusal, with the margin taken from `poseBiasSpan()` (16.369 percent) because a condition change smaller than a scale error the validity gate already tolerates is not worth a flag. Eleven `guided_*` metadata rows, `plot.py` reading the exported line instead of redrawing half the baseline, and a dated section in `docs/validation-plan-round2.md` on reading a guided session. Check: 26 tests plus 4 Python, watched failing first; a targeted Playwright run proves a seeded line reaches the export with its conditions; the refusal's withholding is pinned by `tools/withholdGuard.mjs` because a refusal needs a real face at a real camera and the end-to-end suite cannot reach one, and that pin was watched reddening on the exact one-line mutation. One correction to the ladder's text: `parseBlinkCalibration` staying strict means an entry stored before today is refused whole, so a person who calibrated earlier calibrates once more — deliberate, because a line whose conditions nobody recorded is exactly the line this row exists to stop being used unexamined. The Eyeblink8 byte-identity prediction is committed (`docs/line-in-the-export.txt`) and is 10.8a's to score.
- [ ] 10.14b The corpus exclusion cross-checked against the container, owner [A11; G-Stepped-9]: analysis/tools/prepare_rldd.py writes one ffprobe line per clip (`r_frame_rate`, `avg_frame_rate`, `nb_read_packets`) into a manifest; rldd.py gains the coverage check Eyeblink8 has, refusing above a stated tolerance; docs/uta-rldd-plan.md restated as "the rate this instrument measured"; the 30 exclusion reason lines pasted from a re-run of analyse_rldd.py over the retained CSVs and reconciled with the count; the same manifest attached to the 20 retained DROZY sessions and the corroboration sentence (DROZY's README states its rate) written into drozy-result.txt. Check: a synthetic manifest with one wrong frame count reddens the check; no re-measurement.
- [x] 10.14c The stepper's probe comes from the clip, DONE 7 September 2026, PREMISE CORRECTED: the probe step was a constant 10 ms and now derives from the landings, a quarter of the smallest gap seen, floored at 2 ms; the stale "4 ms" comment is gone. The row predicted that a 120 fps clip would be measured at half its rate. It was not: measured with clip length held constant, 100, 120, 150 and 200 all calibrated correctly before this change, because ladder A1's whole-multiple rule recovers the true period from a mixture of one and two period gaps. What was actually wrong is 240 and 300, which measured ZERO frames and returned no interval, so the instrument failed to measure a fast clip rather than publishing a wrong rate for one. Prediction committed first (docs/stepper-probe-rate.txt) and scored there, P1 failed. Check: 240 and 300 fps fixtures measure every frame with zero inexact landings, red on the old stepper; 30 and 60 fps unchanged; pre-registered as 11.4's voiding condition.
- [x] 10.14d The probe budget at ordinary rates, DONE 7 September 2026 [from 10.14c's measurement]: calibration may spend a fixed number of seeks to gather twelve frames, and the row asked whether the budget rises, the floor changes, or the rule adapts. All three were measured before any was chosen. `summary.calibrationAttempts` reports the cost, because a number nobody can see is a number nobody notices moving. The swept arithmetic floor is committed as a table in docs/stepper-probe-budget.txt: the cost FALLS with rate rather than rising, 51 attempts at 24 frames per second against 20 at 300, which failed P1, P2 and P3 together. The finding the predictions missed is that sixty attempts could not gather twelve frames at 20 frames per second or below — eleven at 20, eight at 15 — so the twelve-frame protection that makes one bad gap a minority, the September audit's critical finding, was thinning out at exactly the rates where the reproduced defect lived. The rule now halves a REDUCED period once the landings reduce, keeping the quarter of the smallest raw gap for landings that do not, and the budget rises to 120, justified by the floor at 15 frames per second rather than rounded up. Lifting the probe cap was measured and REJECTED: it fixes the slow end and switches off the variable-rate refusal, so a 201 frame clip with one frame 25 ms out of place reports 200 frames at a confident 25 frames per second. Check met: the table is committed, the stated margin is more attempts spare than spent at every supported rate, and three mutations redden their own tests. Every rate from 15 to 300 calibrates to an identical interval before and after, to the last digit a double carries, which is why Eyeblink8 at 30 cannot move.
- [x] 10.8a1 The replay tool, the full-clip half [D10, 10.8a's first slice], DONE 8 September 2026: `src/core/blinkReplay.ts` drives the REAL detector over a clip's committed per-frame trace and keeps the state it was in at every frame, both sides of it. **Why it is not the miss autopsy:** `analysis/tools/miss_autopsy.py` already asks what the aperture did during a missed blink, and one of its four verdicts is `crossed_line` — in its own words, "the detector's own state machine (re-arm, refractory) swallowed it". That verdict names TWO mechanisms and cannot tell them apart, because it reads the trace and not the detector. The re-arm gate and the refractory window have different causes and different fixes, so a run reporting "the state machine did it" on forty misses tells nobody which one to open. **Why TypeScript rather than Python, where the rest of the analysis lives:** `tools/measure_corpus.mjs` already says it for the corpus run and it holds here — a reimplementation of the measurement would be evaluating the reimplementation. The reducer is `blinkStep`, and a replay that re-derives its rules in another language is a second detector wearing the first one's name. **What makes the replay faithful rather than a reconstruction:** the trace carries `blinkLineMm`, the EFFECTIVE line the detector compared against, recorded at the time by row 10.13a for exactly this purpose; without it a replay would guess the line, and "did it dip below the line" is the whole question. Both the before and after state are kept, which is not redundancy: a miss is explained by what the detector was HOLDING when the aperture crossed, and the after state has already been changed by that crossing. Check: 11 tests, watched failing first, each verified against one of four bends of the module (a frame counter for the clip clock, the before state dropped, a substituted line where the trace has none, the order refusal removed). No corpus needed: it runs on synthetic traces, the same way the autopsy's tests do.
- [x] 10.8a2 The replay tool, the per-miss half [D10, 10.8a's second slice], DONE 8 September 2026: `missFacts` in `src/core/blinkReplay.ts` reports, for each blink a human marked and the detector did not count, the three quantities row 10.8a names — the crossing frame and the frame the lid cleared the re-arm line, that span on the clip's own clock, the distance from the end of the last COUNTED blink, and the re-arm flag as the detector held it entering the crossing. Together they separate the two mechanisms `miss_autopsy.py`'s `crossed_line` verdict names and cannot tell apart: the re-arm gate, which shuts after a counted blink until the lid clears the line by the hysteresis gap, and the refractory window, which drops a closure finishing within 150 ms of the last count. Different causes, different fixes. **NO VERDICT COLUMN, and a test pins the shape so one cannot be added quietly:** naming which mechanism swallowed a miss is a conclusion about data that does not exist yet, and a column asserting it now would pre-register the answer instead of the question. Three boundary decisions worth their reasons. The crossing is hunted only inside the annotation's own span, matching the autopsy so the two tables join row for row. The REOPENING is followed past that span, because the span is the human's judgement of the blink while the lid clearing the re-arm line is the detector's business and routinely happens later; stopping at `endFrame` would report "never reopened" for an eye that plainly did. And every state-machine quantity is null when there was no crossing at all — the detector was never given a closure to suppress, which is a different thing from a zero, and a zero would read as "suppressed by nothing". Milliseconds throughout rather than frames, because the thresholds these are read against are in milliseconds and a clip at another rate would otherwise report a different quantity under the same name. Check: 14 tests, watched failing first, each verified against one of four bends (the crossing hunted over the whole trace, the reopening stopped at `endFrame`, a null distance turned into zero, the reopening measured at the raw line rather than the re-arm line). No corpus needed.
- [x] 10.8a6 The run procedure is written down, not remembered [D10, 10.8a's fifth slice], DONE 8 September 2026: `docs/regression-run-procedure.txt` records the three things that were nowhere and the one contradiction that was in two places. **WebKit must be installed and nothing said so:** `measure_corpus.mjs` hard-wires `webkit.launch()`, README's setup line names chromium only, and CI installs chromium alone because it has no corpus — so a clone prepared by following the documented steps dies at the launch call before measuring a frame. **The output folder's name is parsed by a guard:** `resultGuard.mjs` matches `eyeblink8-measured-[a-z0-9-]+` against the whole file, first match wins, so a name with a dot in it does not fail — the regex walks on, matches an OLDER folder, and the published reproduction command names a run that produced different numbers with every check green. **Nothing may touch a detector source during the run,** Dependabot included, because `package-lock.json` is on the ratchet's watch list for the reason that file states. And the layout contradiction — the preparation command writes to `eyeblink8-mp4` while every reproduce command passes the corpus root — is settled not by choosing but by READING which directory holds the eight clips, because it is a fact about a machine rather than a decision to take again. Also records what an archived machine will not have: measured folders may survive a move, per-frame traces will not, and a prediction that loses its comparand is recorded as unscoreable rather than quietly dropped. **No path from anyone's machine is in the file** — it uses the `$DATASETS` and `$BLINKLAB` convention STATE.md already established. Check: the file names only committed tools and guards, each verifiable by reading the source it cites; a grep for a personal path or username returns nothing.
- [x] 10.8a7 The replay anchors on the closure that ARMED [D10, a correctness fix to 10.8a2], DONE 8 September 2026: `missFacts` took the FIRST dip below the line inside a miss's span as the crossing. A shallow wobble can cross the line and return without ever reaching arm depth, and the detector treats that as nothing; if a real closure follows inside the same annotation span, THAT is the one the refractory window judged. The tool was therefore reporting a real measurement of a closure the detector never evaluated — 66.7 ms where the decision used 133.3 — and both numbers look entirely ordinary in a table. A lid that wobbles before it blinks is exactly the behaviour the re-arm gate was added for, so the case is not contrived. It anchors on the armed closure now, falling back to the first dip when nothing in the span armed, which is itself the answer: the quantities then describe a closure that never got close enough to count. **Found by an adversarial review rather than by a failing test, and that is the point of having run one:** every test written for this module passed, because each was written from the same understanding the defect came from. Check: 2 tests, watched failing first, verified against the bend that restores the first-dip anchor.
- [x] 10.8a8 A damaged cell is refused, not read as NaN [D10, a correctness fix to 10.8a3], DONE 8 September 2026: `numberOrNull` returned `Number(text)` for any non-empty cell, so an unreadable one became NaN — and NaN is neither null nor below the line. `blinkStep` takes the OPEN branch on it and `missFacts` reports the crossing as never happening, so a damaged frame would arrive in the table as the autopsy's above-line story: the unexplained core, which is the population this project most wants to be honest about. **The Python half already refuses the same cell:** `miss_autopsy.py`'s `float()` raises, so absorbing it here would have made two tools disagree about one file, quietly, in the direction that inflates the mystery. `frameIndex` and `mediaTimeSeconds` are required outright, because a row without them is truncated rather than unmeasured and a NaN frame number joins to nothing. Found by an adversarial review, which had also edited the source directly; the change was reverted and re-made under the ritual rather than shipped as an unreviewed working-tree edit, because a correct diff arriving the wrong way is still a diff nobody tested. **Coverage forced a second correction:** the branch floor went red at 94.85 percent on six defensive fallbacks TypeScript demands and no input can reach. Four were real cases worth covering — a row cut short mid-write, a miss row cut short, an empty table. Two were a Map lookup that could never miss, and those were removed rather than tested: a fallback nobody can reach is a branch nobody can honestly test, and writing a contrived test to cover one is how a coverage number stops meaning anything. Check: 5 tests, watched failing first, verified against two bends (NaN passing through, a missing required cell allowed); branches back to 95.20 percent.
- [x] 10.8a9 The replay is told which clip it is replaying [D10, a correctness fix to 10.8a2], DONE 8 September 2026: `missFacts` took a trace and a list of spans and measured each span against the trace, with nothing to say the two belonged together. `MissTableRow` was `MissSpan & { clip }`, so the WHOLE committed miss table — every clip's spans — typechecked as one clip's misses, because a row with an extra field is still assignable to the base the function takes. A runner that read the traces but forgot to group the table by clip would hand all of them to the first trace and not fail: every clip in the corpus is numbered from its own frame 0, so a foreign span lands on real frames of the wrong video and returns an ordinary-looking crossing, span and re-arm flag for a closure that never happened. `clip` moved onto `MissSpan` itself and `missFacts` now takes the clip it is replaying and refuses any span that names another. The types cannot tell one clip's `MissSpan[]` from another's — they are the same shape — so the check is at runtime, where the identity lives; and one foreign span refuses the whole batch rather than its own row, because a partial answer measured against the wrong trace is the confident wrong answer this pipeline exists to avoid, not a smaller right one. **Found by the adversarial review that also found 10.8a7 and 10.8a8:** a probe fed a two-clip table to one clip's trace and watched three fully-populated rows come back. The disk runner 10.8a5 is written against this signature, so the misuse cannot be expressed. Check: 4 tests, watched failing first, verified against the bend that neuters the refusal — the foreign row returns a populated crossing again. No corpus needed.
- [x] 10.8a10 The trace header is pinned to the writer's columns, not a prefix [D10, a correctness fix to 10.8a3], DONE 8 September 2026: `parseTrace` found its header with `startsWith("frameIndex")` and then read four columns by POSITION, and both halves were loose. The prefix let `frameIndexSought,...` through as a trace. And nothing tied the reader's positions to the writer's ORDER: `FRAME_TRACE_COLUMNS` sat un-exported in `frameTrace.ts`, so a column inserted or reordered on the writer's side would move `apertureMm` under the reader — taken from the `irisAspectRatio` column, every frame would read eye-open and every miss never-crossed, with no error anywhere. The header is now held to `FRAME_TRACE_COLUMNS` exactly, by name and position, the constant exported from the one module that owns it; a file of only metadata with no header is refused by the same check. **The test that earns its place is the round trip:** a trace built through the real `serialiseFrameTrace`, metadata rows and all, and read back — the hand-written header strings in the other tests state the reader's assumption twice and would slip a moved column past, where the round trip makes the two files one contract that breaks the build rather than the numbers. Found by the same adversarial review as 10.8a9. Check: 3 tests, watched failing first, verified against the bend that restores the prefix match — the reordered header parses to plausible wrong numbers again. No corpus needed.
- [x] 10.8a4 The miss table's producer is archived evidence, not a tool [D10, 10.8a's fourth slice; audit F-037]: `eyeblink8_misses.csv` is what `miss_autopsy.py`, `miss_overlap.py` and 10.8a3's runner all join to. It HAS a producer — `docs/evidence/2026-08-09/scripts/tables/autopsy.py` writes it, with the six columns those consumers read — and that producer is committed as EVIDENCE rather than as a tool: it carries hardcoded `/PATH/TO/...` constants, a `sys.path.insert`, and no test. So a new corpus run cannot regenerate the file without editing an archived script, which is the shape this project retired everywhere else. **The first draft of this row said the file had no producer at all, which was false, and the correction is kept because the difference matters:** a missing tool is a gap, an archived one is a maintenance decision nobody revisited, and only the second explains why three consumers could name the file for weeks without anyone noticing. Check: the writer emits the committed 2026-08-21-rearm table digit for digit from that run's annotations and blink log, so the format is proved against a real artefact rather than invented; it refuses a clip whose blink log is missing rather than writing a shorter table, which is the opposite of `miss_autopsy.py`'s skip-and-continue. Synthetic `.tag` and `.blinks.csv` fixtures, no corpus needed. **The tool half landed 8 September 2026:** `analysis/tools/write_miss_table.py` emits the six committed columns from a run's annotations and blink logs, walks the corpus with `sorted(rglob("*.tag"))` so the row order is the corpus layout's rather than a clip-name sort's, and refuses a missing blink log and a non-stepped one; 8 tests on synthetic fixtures, the header proved byte-identical to the committed artefact, three bends verified. DONE 9 September 2026, in the stronger form: `write_miss_table.py` against the annotations and the fresh 2026-09-09 measured folder — not the archived rearm logs — reproduced the committed table byte for byte on the owner's machine (`cmp` clean), so the table is a property of the annotations and the detector rather than of the one run that first wrote it.
- [x] 10.8a3 The replay runner, the reading and writing half [D10, 10.8a's third slice], DONE 8 September 2026: `src/core/replayTables.ts` parses a corpus run's `<clip>.frames.csv`, parses the committed miss table, and writes the per-miss table, as pure string work. It is in core rather than in `tools/` for the reason every core module is — this is rules, not disk — and the disk half next door is four lines. **Both formats are real and committed, not invented here:** the trace is what `serialiseFrameTrace` writes and `measure_corpus.mjs` saves, and the miss table is the six-column shape `miss_autopsy.py` and `miss_overlap.py` already read. Parsing them loosely would invent a third dialect of a format two tools agree on, and the whole point of this pipeline is that its tables join. Three decisions, each a place a looser parser produces a confident wrong answer. The trace header is FOUND rather than assumed, because a real export opens with nine `#` metadata rows: taking line one finds no columns, and taking the first comma-separated line reads a metadata value as data. An empty cell is null and never zero, because the exporter writes empty for "no trusted face" and zero would say the eye was shut. `blink_id` stays TEXT, because it joins two tables and as a number "007" and "7" become one row here and two in the Python tools. The parser also REFUSES a file whose header is not a trace's and one with no rows below it: a wrong file parsed to an empty trace would report every miss as never-crossed, which is a confident wrong answer of exactly the kind this project's guards exist for. The output columns carry no mechanism either, pinned by test, so a verdict cannot be smuggled in by the writer after the reader refused it. Check: 14 tests, watched failing first, each verified against one of five bends (the header assumed to be line one, an empty cell read as zero, `blink_id` read as a number, a null written as the word null, the empty-trace refusal removed). No corpus needed.
- [x] 10.8a5 The replay runner, the disk half [D10], DONE 8 September 2026: the join is `joinMissFacts` in `src/core/replayTables.ts` — pure string work over the miss-table text and a map of each clip's `<clip>.frames.csv` — and the disk is `tools/replayRunner.mjs`, driven by `REPLAY_RUN` through `npm run replay:run`, following `fixtures:write`'s precedent because a `.mjs` cannot import the extensionless core. It groups by clip, replays each clip through the clip-aware `missFacts`, and REFUSES a clip the traces do not hold rather than writing a shorter table: "we did not measure it" is not "it had no misses", and a table that skipped the clip could not tell the two apart. Clips are walked in the order the table first names them, so the output's row order is the evidence's and never the map's. **The round trip is pinned as an IDENTITY rather than as digits:** the runner must equal `missFacts` over the parsed trace and spans with the clip re-attached, which is stronger than a hand-computed expected table — an identity cannot rot, and the runner cannot diverge from the tool it wraps without the test going red. Check: 5 tests, verified against the bend that drops the missing-clip refusal; the counted `tools/` modules go 26 to 27. No corpus needed.
- [x] 10.8a Regression run at HEAD, owner, BEFORE any change on the corpus-rule list [D10]: the replay tool committed first (per miss: crossing-to-reopen span, distance to the previous event, the re-arm flag from a full-clip replay), then Eyeblink8 re-measured, the anchor moved, the guided-line prediction (docs/blink-line-adoption.txt), the stepper prediction (docs/stepper-honesty.txt) and 10.13a's byte-identity prediction scored in dated sections, engine and Playwright version recorded beside "Built from commit". Check: the DETECTOR CHANGED caveat clears; every prediction's outcome section exists whichever way it lands; 10.14's `inexact_landings` row reads 0 on every clip or the design is falsified as written. The stepped path does not read the frame driver, so this anchor is the one 13.8b's parity check reads (supersedes the ladder's "D10 depends on A2"). DONE 8-9 September 2026: run at 68690bf, all eight clips stepped, `inexact_landings` 0 everywhere; the 8 September run carried a one-clip tracker transient, five re-measurements recovered it, and the 9 September full re-run reproduced the 2 September anchor digit for digit; every prediction scored in dated sections, the caveat block retired, the engine and Playwright 1.62.1 beside the built-from line (docs/eyeblink8-result.txt).
- [ ] 10.8 The ~5 armed_then_dropped misses [corpus rule]: each reproduced as a failing trace-harness test, then the re-arm mechanism fixed. Check: failing test per slice committed before any fix; Eyeblink8 regression prediction committed (5 recovered, no false-alarm identity churn, other 62 misses unchanged) with an adopt-or-revert rule. The accuracy CLAIM itself waits for Phase 11's out-of-sample gate. Follows 10.8a, so the diagnosis rests on the replay tool rather than an uncommitted table.
- [ ] 10.9 Blink-duration device anomaly, re-pointed [C2's arithmetic; the A2 candidate]: first tested as arithmetic on the existing `shutBaselineMm`/`apertureMm` columns of the dry-run exports under the two line placements (the guided line sits 30-50% above the passive one); then the re-read candidate, stated as the one pair that supports it (one Mac at about four stamps per delivered frame read 166 ms against one at about one stamp reading 149 ms, while the two iPhones read 96 and 97 ms at one and two stamps), whose observable, stamps per delivered frame, the dry-run exports do not carry, so the read is a fresh paired session on one Mac behind a 60 Hz and a 120 Hz display; and only then live, through the 11.0a/b cued protocol on phones and Macs, with the same clip stepped on each device as the device-independence control (15.4a). Check: each candidate's prediction committed before its read; disagreement beyond one frame period falsifies the camera-side hypothesis and names the next instrument row.
- [x] 10.10a PERCLOS sampling bound, DONE 5 September 2026: prediction committed first (its own commit), then a seeded deterministic sweep (src/core/samplingBounds.ts, hand-rolled LCG, seed 20260905, 2000 draws per cell) measured p95 absolute PERCLOS error at 15/25/30/60 fps and P 0.05/0.15/0.30. The answer is a rounding error with a visible mechanism: every cell's p95 is exactly two samples over the window's count (0.0022 at 15 fps down to 0.0006 at 60), boundary quantization, independent of P. All four predictions held, two by an order of magnitude. Blink RATE's sampling story stays with its own committed catch tables (docs/blink-sample-rate.txt) — shallow quick blinks are LOST there, so rate is a floor, not a count. Result in docs/sampling-bounds.txt, recomputed by test on every run. Check: sampler and generator pinned exactly on hand-built cases; the committed 12-cell table held to recomputation; monotone-in-fps property tested.
- [x] 10.10b The on-page conditions lines, DONE 5 September 2026: two static sentences built in core (samplingBounds.ts) and placed beside the numbers they scope — the Blinks box says the count is a floor, not a count, citing docs/blink-sample-rate.txt; the Eyes box says PERCLOS's sampling term is at most ±0.002 even at 15 fps and names the shut line and the landmark floor as the real conditions, citing both documents. Check: the sentence contents unit-pinned (the ±0.002 constant held to the committed table's own maximum, parsed from the doc), and one Playwright test proves both actually render — a created node nobody appended is the 9.4b lesson. Duration's line stays held behind 10.9.
- [x] 10.10c1 Every counted proportion carries its interval [B8], DONE 7 September 2026: `wilson_interval` in `analysis/blinklab/stats.py` and `wilsonInterval` in `tools/wilson.mjs`, held to one another by `test/fixtures/wilson-cases.json`, which both suites read and recompute. Wilson rather than the normal approximation because of the case that named the row: criterion 1 was cleared by 0 sessions of 3, where the normal approximation gives exactly [0, 0] and reports three observations as certainty. The README's recall and precision now carry 79.7 to 86.9 and 80.1 to 87.2, the Eyeblink8 tool prints both beside its counts, and criterion 1 states the bound its denominator supports. F1 deliberately carries none: it is a harmonic mean of two proportions, not a count over a count. Check: 33 tests, watched failing first; the README block is regenerated from the result file's own counts, so the published interval cannot disagree with the counts beside it; `evaluate_eyeblink8.py`'s report had no test file at all and now has one. The committed result files were NOT regenerated, there being no corpus here, so they gain the printed interval at the next measurement while the README carries it now, computed from the counts they already publish.
- [x] 10.10c2 A bar is worth what it is hard to clear [B9, B11], DONE 7 September 2026: `binomial_at_least` in `analysis/blinklab/stats.py`, and DROZY's within-subject bar now states its chance rate wherever it is read. At least 3 of 5 subjects agreeing on a sign is cleared 0.500 of the time by nothing at all, and that column alone granted three suggestive verdicts. Any bar added after this must state its rate and may not grant a verdict alone unless the rate is below 0.05. The alertness Bar 2 reading is corrected in the same increment: "not distinguishable from chance" and "about as good as a fitted one" both claim a finding of no difference that a control able to detect only about 0.14 AUC at this n never made, and both now read as no evidence either way. Check: 13 tests, watched failing first, recomputing the rate from the counts the committed table already publishes. Neither result file is regenerated: DROZY's would need a re-extract that recreates the derived video DATASETS.md requires destroyed, so the arithmetic is stated in prose and recomputed by test, and the tool prints it from its next run.
- [ ] 10.10c2b The subject-level bootstrap intervals [B10's remainder; NEEDS THE OWNER]: the three-class balanced accuracy 0.498 with its interval (about 0.42-0.57) and the alertness Bar 2 paired bootstrap. Both resample by SUBJECT rather than by row, because rows within a person are not independent, and both therefore need per-subject outcomes. The committed result files carry pooled numbers and a confusion matrix only, and `analyse_rldd.py` and `compare_alertness.py` both take a measured corpus directory that is not in this repository, so neither interval can be computed anywhere but the owner's machine. Check: every interval recomputed by test from a committed per-subject table, seeded so the published number reproduces, which needs 10.10c3's table to exist first.
- [ ] 10.10c3 The per-subject table and the standardised coefficients [B8; NEEDS THE OWNER, a corpus run]: the UTA-RLDD per-subject table and the standardised coefficients the analysis plan promised and never printed. Check: the table's rows sum to the pooled counts already published, and each coefficient is recomputed by test from the committed feature records.
- [x] 10.10c4a The two PERCLOS bounds [B12, audit F-029 and F-090], DONE 7 September 2026: this PERCLOS includes blink time, because the shut line it uses is the one the long-closure detector uses and every full blink crosses it, so at rest the figure is mostly blink time and inherits blink-rate variance. Stated in `perclos.ts` where it is defined, in MODEL_CARD beside the threshold caveat it is larger than, and in `drozy-result.txt` where the null is read. And the on-page conditions sentence is scoped: the sampling simulation drew closures of half a second and longer, so "at most ±0.002 even at 15 frames per second" never covered the blinks the share also counts, which are the closures a slow frame rate misjudges most. Check: 5 tests, watched failing first, including a source pin that the stated scope is the range the simulation actually drew. The bound was missing rather than wrong and the table is untouched. One correction to the ladder's text: the scope is half a second, not 0.3, because `MIN_CLOSURE_S` is 0.5 and the honest statement is what was simulated.
- [x] 10.10c4b The peak-velocity sampling bias [B12, audit F-033], DONE 7 September 2026: `src/core/velocityBias.ts` samples a raised-cosine descent with a known analytic peak through `analyzeClosing`, the page's own arithmetic, over 40 phases per cell. At 25 frames per second a 50 ms closure reads 36.8 percent low at the median phase and 60.2 percent at the worst; the ratio reads up to 1.58 times longer than the truth, which is the direction drowsiness moves it. Stated in `blinkShape.ts` where the numbers are defined and in MODEL_CARD where they are published; the bound is a floor on the true velocity at every rate and phase. The prediction was committed first in its own commit (`docs/velocity-sampling-bias.txt`) and one of its five parts was WRONG: the floor of the predicted band was 10 percent and the measurement came in at 5.4, because `analyzeClosing` takes the largest drop among all adjacent pairs and so already picks the best-placed interval, which a fixed-interval estimate does not. Recorded as wrong rather than adjusted. The audit's reproduced 12 to 28 percent does sit inside the measured spread. Check: 13 tests, one per prediction. Correction to the ladder's fix text: the sampling interval is already attached to every row as the `fps` column, so nothing needed adding there; what was missing was the bound, not the interval.
- [x] 10.10c4c Aperture is not pose invariant, and by how much [B12, audit F-086], DONE 7 September 2026: `src/core/poseBias.ts` feeds a synthetic rotated eye through the real `apertureMm` with the real index sets. The published millimetre reads 6.03 percent LOW at the gate's 20 degrees of pitch and 10.34 percent HIGH at its 25 of yaw, a span of 16.4 points across the accepted region, on a quantity whose baseline ceiling refuses a session for a 25 percent disagreement. Roll is free to nine decimals. Stated where the gate is defined, where the aperture is computed, and in SPEC.md's conventions. The prediction was committed first and TWO of its six parts moved: the combined case cancels less than cos over cos, because pitch pushes the lid chord's endpoints apart in z and yaw turns some of that into x, so the chord acquires a horizontal component and the real factor is sqrt(cos²pitch + sin²pitch·sin²yaw); and the audit's "up to +12%" for yaw is NOT reproduced, needing 26.8 degrees which the gate refuses, recorded as not reproduced rather than adopted. Check: 7 tests, one per prediction, each carrying its algebra so it can be checked by hand. This row also fixed a red main it had caused: a rebase merge rewrites a commit's sha, so a caveat written in a pull request names a commit that stops existing when the pull request lands, and the detector ratchet was green on every branch and red on main for any change touching a detector source. The ratchet now accepts a commit's SUBJECT as well as its sha, matched against whole lines because a subject that is the opening of another line would otherwise count as declaring that other commit. Found only because the shallow clone was unshallowed: the ratchet skips itself entirely on a shallow history, so it had never run in this environment at all.
- [x] 10.10c4e The fixture's storage precision beside the noise floor [B12, audit F-094], DONE 7 September 2026: `src/core/storageQuantum.ts` converts one stored rounding step into aperture millimetres through the fixture's own measurements. At 1280x720 the step is 0.072 px vertically, the lid chords run 13.2 degrees off vertical so that is the step they ride, and the fixture's median iris width is 43.3 px, making one quantum worth 0.0097 mm. The published per-eye medians are therefore 2.03 and 1.99 quanta: the floor and the grid the fixture was written on are the same size, so neither can be attributed from this file, and the noise floor document now says so beside the median it qualifies. What it does NOT mean is stated there too, because the ratio invites the wrong conclusion: rounding is deterministic, so a perfectly still eye would round to the same value every frame and show no wobble at all, and two quanta of measured change cannot be read as two quanta of storage noise. The prediction was committed first in its own commit (`docs/fixture-storage-quantum.txt`) and two of its five parts were WRONG, both from one mistake: the predicted iris band of 25 to 40 px was reasoned from the framing rather than measured from the file, which is 43.3, and the quantum therefore came in at 0.0097 rather than inside the predicted 0.010 to 0.017. Part 5 does not trigger, so the audit's phrase "two rounding quanta" is adopted as measured rather than recorded as approximate. Check: 17 tests, watched failing first, one per prediction part, with the quantum recomputed from the committed fixture over the same 270 still frames the floor used and both documents pinned to it. `FIXTURE_COORDINATE_QUANTUM` is now exported from `fixtureRecording.ts` and the rounding uses it, so the published figure cannot drift from the rounding it describes.
- [x] 10.0b11 The ladder can say it has nothing left to start [amendment 21], DONE 7 September 2026: `startableClaims` accepted a list of rows or threw, so a ladder with nothing startable had no legal sentence and the only way to write one was to delete the claim — which is the exact silence the throw exists to refuse. A declaration of nothing is a CLAIM somebody made and can be disagreed with; a missing sentence is a file nobody finished, and the two must not look alike to a reader or to a guard. The refusal now teaches the form as well as naming it, because a message that only says "wrong" gets satisfied by a delete. Found by ticking 14.0f2 and watching the ladder's own guard refuse the honest sentence, which is the guard working. Check: 5 tests, watched failing first, including one pinning that the real ladder declares nothing startable today, so picking a row up again is a deliberate edit; verified against a bend that makes silence acceptable, which reddens three.
- [ ] 10.10c4d The iris constant's citation and spread [B12, audit F-034; NEEDS THE OWNER]: `IRIS_DIAMETER_MM` has no citation and no stated spread. The plus or minus 4 percent is a population figure that must come from a source the owner has read and chosen; inventing a reference for a constant every millimetre on the page divides by would be worse than the silence it replaces. Check: the constant carries a citation the owner supplied and its spread, and the conditions sentences state it.
- [ ] 10.11 Long-closure hysteresis on the ARMING side and the bounded gap [A7, A19; corpus rule]: a long closure still ends at the shut line, so every duration is unchanged; a new closure cannot arm until the aperture has cleared the line by a noise-floor-derived fraction (blink.ts's own re-arm pattern); closure state survives a bounded run of untrusted frames; a closure beginning right after a gap does not arm unless the eye was seen open. Cadence-independent, because duplicate ticks carry the same aperture, so it may precede 13.8. Check: prediction committed first on synthetic hover traces parameterised from the dry run's per-second band (2.79-3.01 mm against a 3.04 mm line) at 30 and 120 Hz: the iPhone shapes' 3 and 2 events collapse to 1, desktop unchanged, durations byte-identical; a 6 s closure with ±0.3 mm noise counts once; test/core/longClosure.test.ts:291-307 re-pinned to the physical intent; hysteresis-not-filtering stated as the rule; Eyeblink8 through 10.8a's regression discipline. After 10.8a.
- [ ] 10.12a The blink gate reads the evidence rate [A16]: `blinkMeasurable` from min(sampledFps, fps) with 60/65-style hysteresis where delivery is reported, at the three gate sites; the report can no longer assert withholding while printing the numbers. Check: one deliberate dim-room session run BEFORE the code changes so the pre-registered rule (docs/blink-sample-rate.txt) has its observation (owner, on 13.2's day); a verdict/report fixture at sampled 20 / processing 60 whose measured rows and verdict agree; the stepped corpus unchanged by construction (no delivery on a clip).
- [ ] 10.12b Blink rate over observed time, and counting-suspended as a fact [A13, A14]: the rate divides by frames that were fed an aperture, windowed like blink times, null below the observation minimum, the observed fraction exported beside it; the rate reads null while closed longer than the maximum blink or while the re-arm gate is down, "blink counting suspended for N s" on the page and a per-record flag; vertical iris offset exported so downgaze droop is separable. Check: a face-loss gap no longer reads as calm on a synthetic; resting at 4.2 mm on a 4.0 line with five deep blinks reads suspended, not 1; the DROZY and RLDD rate columns re-derived from retained CSVs are named as changed in their caveats. Its third Check clause is open by nature rather than by blocker, recorded 7 September 2026 and restated 9 September when this row was declared startable: the first two clauses are synthetic and met here; "the DROZY and RLDD rate columns re-derived from retained CSVs" needs the owner's retained files, because no DROZY or RLDD session CSV is in this repository and none ever will be — the datasets are numbers-only under written permission. The row lands on its first two clauses, and the third ticks from the owner's machine.
- [ ] 10.12c Sample floors count face-seconds, not ticks [A20; corpus rule]: baseline and guided floors require a minimum span of trusted-face time (or distinct `deliveredCount` values) per window and per phase, face seconds shown and exported. Check: prediction committed first that the stepped corpus is byte-identical (deliveredCount does not exist on a clip and face-seconds equal elapsed seconds on every corpus clip whose face is never lost; any clip where they differ is named); a 120 Hz display no longer satisfies the floor on 25 photographs. After 10.8a.
- [ ] 10.13b The learning window, decided [A15's decision; corpus rule]: the first 30 s of every session currently count blinks against a 4 mm fixture line from one face; the decision is pre-registered and lands as its own delta, either feeding null while learning or keeping the fixture line and labelling the count provisional on the page and in eyeblink8-result.txt and MODEL_CARD's conditions. Check: the committed delta (which clips, how many blinks, in which direction) scored against the 10.8a anchor; the line source per blink is 10.13a's column either way. After 10.8a. Premise measured 9 September 2026: `blinkLineSource` reads `fixed` for the first ~29 rows of every corpus clip and `passive` after (the 10.8a run's exports), so the decision has its observation; briefly duplicated by 10.18, retired.
- [x] 10.14 Stepper honesty on variable-rate clips, code DONE 6 September 2026 [A1]: every calibration gap must be a whole multiple of the smallest within 1.5 ms or the clip is refused by name as a variable frame rate; the step is the mean period those multiples imply; an inexact landing is counted and refuses the run past 2 in 100; `frame_interval_s`, `frames_sought`, `inexact_landings` written to every stepped export; the fixture takes an explicit frame-time list and the four pre-registered cases are pinned (docs/stepper-honesty.txt, committed before the code). Check: the four fixtures were red on the old stepper; six mutations reddened; the corpus half (zero inexact landings on all eight clips, byte-identical or the quantised-branch prediction) is scored by 10.8a.
- [x] 10.15 The refusal survives the export border, DONE 6 September 2026 [A3]: `sampled_fps` handed to the verdict as the same one-decimal value the export writes, so the page and the Python mirror agree in [24.95, 25.0) and [59.95, 60.0) and pilot.py stops calling a rounding artefact an instrument defect. Check: verdict fixtures at 24.96 and 59.96 on both sides; the self-refuting "25.0 ... below the 25" sentence cannot render. (Landed: the page rounds `sampledFps` through `asExported(…, 1)` before the verdict, an `edge` fixture at 25.0 reproduces byte for byte on both sides, the literal boundary probes 24.9/25/59.9/60 are pinned, and the wiring is source-pinned.)
- [x] 10.16 Four small arithmetic fixes, one pull request [A27], DONE 6 September 2026: the two delivery rates measured over one span and worded "read all N" when sampled ≥ delivered; PERCLOS's minimum observation as a valid-sample count, exported; the permutation p-value as (k+1)/(N+1) in light_response.py; the frame trace's duplicate `frames_measured` row dropped and one `_read_metadata` shared by loader, validation and blink_log. Check: a literal test per fix; `sampled <= delivered` as a property over five delivery rates against six processing rates, plus the sped-up-camera case staged as a state, 99 and 100 PERCLOS samples across a long enough span, a p that must be 1/1001 rather than 0, one `frames_measured` line where the caller owns the key, and the blink log refusing a repeated key.
- [ ] 10.17 Head-pose matrix layout verified, owner (one frame) [A24]: one DEV frame logs the 16 numbers with the head turned (translation at 12-14 means column-major); `headPose.ts` reads `data[col*4+row]`; a real-recording fixture with a matrix pinned; MANUAL item 19 re-run for signs; the translation column kept for 14.9a's distance gate. Check: the fixture test fails on the row-major read. A correctness fix; it does not wait for the Phase 12 gate.
- [~] 10.18 The first thirty seconds run on the fixed line, and nobody decided that. 10.13a's provenance columns showed `blinkLineSource` reading `fixed` for the first ~29 rows of every corpus clip before the baseline freezes (docs/line-in-the-export.txt, the 9 September re-score): learning-period blinks are counted against the 4 mm constant, on clips and live sessions alike, and the behaviour predates the column that revealed it. Check: a written decision — count on the constant and say so, refuse to count until the baseline is ready, or count on the live learning line — recorded as a dated note or ADR, and the export's early rows matching it. RETIRED 9 September 2026 as a duplicate filed in haste: row 10.13b [A15's decision] has carried this exact decision since amendment 17, with the richer Check (a pre-registered delta scored against the anchor). The finding this row added — the provenance column measured the behaviour — is recorded on 10.13b now; decide there.

## Phase 11. Out of sample and out of house (the generalization era)

Eyeblink8 has been read to per-miss identity across five detector eras; it is exhausted as evidence. Every accuracy claim from here graduates on corpora and people no version of the detector ever saw, on a pinned build, through a cued protocol whose ground truth costs no privacy. The sibling corpora are read ONCE, on the final pinned build that 12.18 and Phase 15 also use, after every corpus-rule row is ticked and after the blendshape witness (12.2, 12.3) has been scored on the regression harness, so a fused detector does not graduate on corpora already spent.

- [x] 11.0a Cued blink-and-closure schedule, pure [C4], DONE 7 September 2026: `src/core/cueSchedule.ts` on the `lightSchedule.ts` pattern — ten blink cues, a 3 second closure, a 20 second closure and a look-away, each followed by a rest, with the settle set to `BASELINE_LEARN_MS` rather than a tidy round number because a cue delivered before the baseline is ready would be scored against a detector holding no line. `scoreCues` pairs cues with detector events into caught / missed / latency and refuses a session that ended before the schedule did, ran far past it, or carried an event from before the first cue. Two design corrections the tests forced, both before anything ran. Events are TYPED by which detector produced them: the first version took an untyped stream, so a 120 ms blink counted as answering "close your eyes for twenty seconds", and its own tally caught the conflation. And the look-away is excluded from the tally rather than scored — nothing in the blink or closure path can see a person turning away, gaze can, and this scorer does not read gaze, so scoring it would produce a number about nothing. A cued closure reports a signed `durationErrorMs` instead of a pass mark, because a 20 second closure measured at 18 is a 2 second error and not a miss, and that is the number 13.3 wants. The response window is 2 seconds, pre-registered and stated as a CHOICE rather than dressed up as a derivation, bounded below by `MAX_BLINK_DURATION_MS` and above by the rest that follows each cue; the scorer reports every latency so it can be re-chosen from real sessions by dated amendment. The adversarial case is handled honestly in both directions: a stream that ignores the cues scores 0, and a stream that blinks every 200 ms catches every blink cue and neither closure — which no scorer can prevent, so `eventsSeen` travels beside the tally and says so out loud. The two-marks protocol is retired at the top of `docs/participant-instructions.md`, with the reason and with the note that sessions already recorded stay valid. Check: 29 tests, watched failing first; the retirement notice is pinned and the pin was watched reddening when the word RETIRED was removed. No prediction document: this row measures nothing, it is the instrument 11.6 and 13.3 will measure through.
- [ ] 11.0b Cued protocol wired: overlay, tones, export rows, per-event table in the report; rows 10.9, 11.6, 13.3, 14.5, 14.13 and 15.5 re-pointed to it. Check: Playwright drives the overlay off a shortened schedule and reads the cue rows back from the export; the report's per-event table prints delivered rate, processing rate and sampled fps beside every tally.
- [ ] 11.6a Guided calibration as a procedure, the detector half [A9, A10, C3]: a settle window per phase derived from constants.ts's own rationale, so the open eye is not sampled while the person reads; the calibration's own 3 s hold fed as null to all four reducers with a marker written; a verification phase ("blink three times, normally") that stores the line only if the reducer caught at least two; a resolve-time soundness ceiling pre-registered against the open distribution's lower tail (a line at 0.85 of open is refused). Check: `calibrationSessionStep` unit-pinned on the settle and verification states; the ceiling's factor committed before any guided data is read; the verification outcome logged per calibrated person as the procedure's first measurement.
- [ ] 11.6b Guided calibration as a procedure, the interface half [B20, E3]: audio tones and vibration at phase boundaries; `role=dialog` with an assertive live region; an explicit Cancel that says nothing was stored, Escape included; refusal text conditional on what the run saw (pose rejections versus late closure versus true non-separation); n_open, n_closed and the separation ratio shown, worded "thin, consider running it again" near the floors. Check: a Playwright run of the overlay off a shortened schedule reaches every state; MANUAL item 66 rewritten to the shipped behaviour and performable with closed eyes.
- [ ] 11.6c Guided calibration as a procedure, the record: camera label, resolution and timestamp stored beside the three numbers (parse kept strict) and the outcome exported through 10.13a's rows, so round II and Phase 15 can read which procedure produced a line. Check: the stored-shape parser refuses a stale format; the export carries the procedure's numbers on a seeded line.
- [ ] 11.8a A pinned build, row zero of 11.8, 11.9 and every Phase 15 row [D11]: the commit of any run that becomes evidence is tagged and released with `dist` attached; a README "reproduce the exact page" section (`git checkout <app_commit> && npm ci && npm run build && npm run preview`); the frozen plan names the build and the intake refuses a differing `app_commit`; validation_report.py, pilot.py, analyse_drozy.py and analyse_rldd.py print the distinct commit set and refuse or state a mix. Check: the intake tool reddens on a synthetic mixed-build folder; a test asserts that every plan naming a build names a tag that exists in git, and "frozen" appears in no plan without one.
- [ ] 11.1 Row-zero corpus gate: verify the TalkingFace and Silesian5 GPL3 downloads actually contain video (ZJU joins only if its videos verifiably ship — its official page is dead, see DATASETS.md). Check: a dated docs note per corpus recording what the download contains, with the ffprobe manifest 10.14b introduced. After 12.3 has been scored and its fusion decision, if any, has landed.
- [ ] 11.2 TalkingFace prepare tool and annotation loader, remux-not-re-encode, reusing the Eyeblink8 .tag loader where the format matches. Check: prepare test verifies decoded frame counts on both sides and refuses re-encoded input; loader tests watched failing first on malformed tag files.
- [ ] 11.3 TalkingFace pre-registered frozen-instrument run, ONCE, on the final pinned build, after every corpus-rule row (10.8, 10.11, 10.12c, 10.13b, 12.0a, 12.6c, 12.8a) is ticked. Check: prediction band committed before any measured folder exists, derived from Eyeblink8's per-clip spread (recall 66.2-93.1) plus the preparation-sensitivity finding, voiding conditions named in advance; the result carries the digit-reproducible command, the container coverage check, the `app_commit` and a Wilson interval.
- [ ] 11.4 Silesian5 prepare and pre-registered run, same discipline and same build as 11.3; its higher native frame rate additionally probes the 0.4 mm shallow-blink band on real footage. Check: the pre-registered voiding condition is `frames_measured` against the ffprobe count, refused above the tolerance (10.14c's case, where a doubled period lands every target on a real frame); the high-speed condition named in advance.
- [ ] 11.6 Per-device deliberate-blink matrix, on the 11.0a/b cued protocol: duration bands, long-closure count per cued closure, catch-rate and latency bands per setup, mechanism explicitly open. Check: report tool tests failing first on adversarial synthetics; every table row prints delivered rate, processing rate, and sampled fps; the DSLR column reads ABSENT until that rig returns; tablets and Android read volunteer-supplied or ABSENT.
- [ ] 11.7 Run validation round II, the frozen 27 August plan, on the pinned build (11.8a) after 10.11, 10.13a, 11.6a-c, 12.0a-b, 14.0a-e and 14.0b — the calibration fixes' first test on people they were not built from. Check: committed docs/validation-round2.txt grading the three pre-committed structural predictions; round I's frozen table re-derives unchanged; a dated section says how guided sessions are read.
- [ ] 11.8 Owner n-of-1 longitudinal protocol: same setup, same protocol, same build (11.8a), ~3 weeks of sessions, test-retest bound as prediction one. Within-subject claims only, the expectancy and time-of-day confounds named in paragraph one of the plan. Check: pre-registered plan first; intake tool passes adversarial synthetics before any real file; analysis row split from the run row.
- [ ] 11.9 The assessment pilot run, the written and unrun plan (docs/assessment-pilot-plan.md), on the pinned build after 14.0a-e and 11.8a, with its own publication restraint kept: structural outcomes, never a recall figure, six people is a smoke test. Check: the plan's voiding conditions scored; KSS before and after captured once each on the `ended` transition; the KSS-versus-feature relationship reported as a bound with its n, never as a claim, until the n at which a claim would be attempted is stated with its detectable effect.

## Phase 12. New signals, then the one labelled read

Every per-second field that wants a validation path must exist and freeze before 12.17's owner-signed freeze; then UTA-RLDD's labels are read once, batched, and that is their last read. GATE, from amendment 17: no signal row here starts before 10.11, 10.12a-c, 10.13a-b, 10.14, 13.2 and 13.8b are ticked, because every signal built before them inherits a tick-rate column and an owner-referenced shut line. The instrument rows are exempt and are the gate's other half: 12.0a-b, 12.1, 12.2, 12.3, 12.15, 12.16 and 12.17's tool. Rows that can move the Eyeblink8 count say [corpus rule] and land after 10.8a with a committed prediction.

- [ ] 12.0a One personal ruler, the rule [C2; corpus rule]: the guided blink line and a guided SHUT line both placed as closed + k × (open − closed), with k pre-registered from the 10.7a noise floor; the shut line adopted for PERCLOS and long closures when a guided line exists, lifting their refusal as it does for blinks. Check: the owner's recorded droop session through both placements, published; the floor-shift prediction tested from existing `shutBaselineMm`/`apertureMm` columns of the dry-run exports; the P80 dry-run phone trace giving nonzero PERCLOS under its own floor; Eyeblink8 predicted unchanged (no stored line on a clip) and scored.
- [ ] 12.0b One personal ruler, the record: `shutBaselineMm`'s source recorded; the two duration quantities named in every column header and MODEL_CARD; the shut-line adoption written as the ADR the ladder's A8 asks for. Check: the column headers and the card carry the two names; the ADR exists and 10.13a's source column takes the new value. **BLOCKED: on row 12.0a, which is a corpus rule and needs the owner's recorded droop session. This Check ends "10.13a's source column takes the new value", and the new value is the guided SHUT line that 12.0a introduces, so the clause cannot be met until that row lands. The documentation half — the two duration quantities named in the column headers and the card, and the shut-line adoption written as an ADR — is startable now; the row as a whole is not. Verified 7 September 2026, after this amendment first named it as the one fully startable row left, which was wrong.**
- [~] 12.1 RETIRED 7 September 2026, on the owner's ruling. The row asked for a shared Theil-Sen slope with refusal floors and tie handling "so no consumer hand-rolls its own", and its Check ended "a test asserts neither consumer defines its own slope". Amendment 17 demoted both consumers three hundred lines away: the slow-drift gaze index (was 12.11) and the fixation turnover trend (was 12.13). What was left is a module built for nobody, and a module built for nobody is speculative infrastructure — the opposite of the rule this ladder runs on, that instrument rows land before their claim rows because the claim is what says which instrument is needed. Amendment 18 marked it BLOCKED, which was the right reading of a row that could not proceed and the wrong verdict on a row that should not: a blocker implies the work is waiting, and this work is not waiting for anything, it has no reason to exist yet. The marker is removed with the retirement, because a retired row carrying a blocker would be two claims about one line. NOTHING IS LOST BY THIS. The argument the row was built on stands: a slope hand-rolled twice will be hand-rolled differently twice, and that is worth one module. If a later row needs a slope, this returns as a NEW row pointed at that consumer, with the consumer named in its Check — which is the order every other row here is held to, and the order this one was written out of.
- [ ] 12.2 Enable outputFaceBlendshapes, with the trace/CSV contract and a measured latency delta. Check: CI pins the option flag and the [0,1] contract; the measured inference delta and a kill criterion (ships disabled by default if the 30 fps budget breaks on the slowest tier) recorded in docs/latency.txt; an e2e runs the vendored model on a stepped clip and asserts the category is present.
- [ ] 12.3 Blendshape closure witness: the pre-registered interrogation of the 49-miss ceiling with the vendored blendshape head, a landmark-fed network over 146 mesh points and NOT an appearance head, so the question is whether mesh points the aperture ignores carry closure the eyelid rim does not; the only vendored route past that ceiling, run on the regression harness BEFORE the sibling corpora are opened. Check: numeric predictions that state the landmark-fed prior, AND a neutrality check (the same 67 misses reproduce digit-for-digit with the head enabled) committed before the corpus run; per-miss verdict CSV; either outcome is a result, and any fusion signal is its own [corpus rule] row that lands before 11.3.
- [x] 12.15 Per-second `sampledFps` and `inferenceMs` export columns, DONE 7 September 2026: two facts about HOW a row was measured were session-level or nowhere. `sampled_fps` — distinct camera frames read per second, the evidence rate the 25 fps refusal and the 60 fps warning both judge — existed as ONE number in a comment line above the header, so a recording that ran at 30 for a minute and 12 for the next reported a single figure and a reader could not tell which rows were which. `inferenceMs` was on the PAGE and not in the file: the timing readout has printed the model's mean cost for increments, and the exported record never carried it, so a row reporting a low `fps` said the machine was slow without saying whether the model or the rest of the loop was the reason. The row now takes the SAME mean the readout prints, from the same samples, so the page and the record cannot disagree. Both are appended at the end, keeping every older header an exact prefix, which is the discipline `baselineOverResting` and `pupilDiameterMm` established. Check met: `test/tools/exportColumnPin.test.ts` holds SPEC.md's `FeatureRecord` block to `CSV_COLUMNS` by set equality, which the block had fallen behind twice before (increment 6.4's review, and `baselineOverResting` landing on 23 August and reaching SPEC on 6 September); the Python loader gains a `PRE_MEASUREMENT_COLUMNS` generation so older files load with both as NaN rather than zero, and a reordered header is refused by name. A LATENT DEFECT was found and fixed on the way: `PRE_LINE_COLUMNS` was `COLUMNS[:-4]`, an absolute slice, in a file whose own comment two lines below insists a generation be sliced from the one after it "so that appending a column never silently re-cuts an older header". This row's two columns would have re-cut it four short of the wrong list and refused every file of that vintage whole. Restoring the absolute slice reddens five loader tests; a property test now asserts every accepted generation is a prefix of the current one, which is the rule those slices depend on and nothing checked.
- [ ] 12.17 Column-freeze manifest for the v2 read, self-retiring on the drozyGuard pattern: the manifest tool and its tests land now; the owner-signed freeze is dated after 12.16's and 14.9a's columns exist and before 12.18a's plan is committed. Check: manifest-vs-header test watched failing first; the corpus runner refuses on mismatch; owner-signed dated in/out per field.
- [ ] 12.4 Yawn detection: mouth aspect ratio with a duration gate, jawOpen recorded as a cross-check. Check: a 6 s yawn counts once and a 2 s speech-like oscillation of BOTH channels counts zero, on hand-built two-channel traces; the slowest-tier model-budget re-check is its own gate before merge. **BLOCKED: on row 12.2, and on a device this is not. `jawOpen` is a blendshape and `src/io/landmarker.ts` does not ask for blendshapes, so the cross-check channel does not exist until 12.2 turns them on — a test reads that option rather than trusting this sentence. The Check also makes "the slowest-tier model-budget re-check" its own gate before merge, which needs the slow device. Verified 7 September 2026.**
- [ ] 12.5 Head-nod events and per-second pitch median/dispersion, read before the validity gate and baseline-relative, after 10.17. Check: scripted pitch traces through the pose math drive the pure reducer; a wiring test proves aperture is null past the pose gate while the nod still fires.
- [x] 12.6 Closure-duration taxonomy, DONE 7 September 2026: a closure that outstays a blink has been one thing since Phase 4, `longClosureCount`, which charges the same fifteen points and writes the same column whether the eyes were shut for six hundred milliseconds or six minutes. `src/core/closureTaxonomy.ts` gives the durations three bands: prolonged, microsleep-RANGE, sustained. Check met on every clause: 0.8 s reads prolonged, 3 s microsleep-range, 20 s sustained, 0.4 s reads null because blink.ts has already counted it, and the 2.0 s edge is pinned to the LOWER side with probes one frame period either way (1960, 2000, 2040 as literals, never derived from the constant they hold). The 15 s edge is pinned the same way although the Check did not ask, because an edge tested from one side is an edge nobody knows the position of. Both edges are stated as CHOICES and not dressed as derivations: placing them by measurement would need closures whose causes are known, which is the ground truth whose absence is why the vocabulary is capped at all. The blink edge is aliased from `LONG_CLOSURE_THRESHOLD_MS` rather than copied, so the one-closure-one-time-category partition cannot drift, and a mutation flipping that comparison reddens. THE NAMING IS THE OTHER HALF. Amendment 16 capped the vocabulary and nothing held the cap; `tools/claimGuard.mjs` now does, as a fourth retired claim over every tracked file. It bans the CLAIM and not the word, in both directions and in the plural, so the honest sentence about the shape the literature watches for survives while the pairing that says the thing was found does not. That exposed a hole in the guard itself: its exemption list was one list for all claims, so letting the ladder quote what it refuses would also have let the ladder say the page sends nothing anywhere. Exemptions now travel with the claim, and a test pins that ROADMAP.md is exempt from this one and from no other. 20 tests watched failing first on the taxonomy and 6 on the guard; six mutations take `tools/mutationCheck.mjs` from 31 to 37, all caught. Nothing is wired to the page or the export: 12.6's Check is the taxonomy and its edges, and 12.6b, 12.6c and 12.10 are the rows that ask for consumers. **Started while the Phase 12 gate was shut (amendment 20): this row is not on the header's exempt list and 10.11, 10.12a-c, 10.13b, 13.2 and 13.8b were open. Nothing published depends on it, because it was shipped with no consumer, but both band edges sit over durations produced against the owner-referenced shut line that 12.0a will re-rule and the timing 13.8b will change. Re-look when the gate lifts: 13.8b, 12.0a.**
- [ ] 12.6b Closure completeness beside duration [C11]: `closureFraction = amplitude / frozen baseline` and a complete/incomplete label at the arm line; the 1.5 mm absolute grey line retired. Check: the arm line and the label agree on every fixture blink; the committed miss table's min_ratio distribution reproduces from the new column; the column appended trailing. **BLOCKED: on a corpus run this container cannot do. The Check's second clause wants the committed miss table's `min_ratio` distribution reproduced from the new column, and `min_ratio` came from per-frame traces that `docs/evidence/2026-09-02-awake-autopsy/README.md` records as deliberately not committed, on Eyeblink8 clips this repository does not hold. The first and third clauses, the arm line agreeing with the label on every fixture blink and the column appended trailing, are startable now; the row as a whole is not. Verified 7 September 2026.**
- [ ] 12.6c Droop-then-blink and re-crossing blinks timed honestly [C5; corpus rule]: the closed phase timed from the arm-line crossing or emitted as a "refused: over-length" event class; a slow reopener reported to the reopen that clears the re-arm line with a `fragmented` flag. Check: pre-registered against Eyeblink8's 9 long-closure misses and the dry-run traces; the audit's probe traces become pinned unit tests; scored through 10.8a's regression discipline.
- [ ] 12.7 Lid openness ratio instrument over the frozen shut baseline, with the born-wrong-ruler refusal; export and panel only. Check: unit tests including the refusal; a claimGuard family for "lid openness" claims until 12.18 reads.
- [ ] 12.8a Peak closing velocity from a quadratic through the three steepest samples, where three exist [C6; corpus rule; after 13.8b]: the single finite difference reads 81-88% of the true peak for an 80 ms close at 25-30 fps; the harness's own blinks close in about 50 ms, which puts one or two samples on the descent at 30 fps, so the quadratic applies only where at least three velocity samples fall on the descent and the finite difference is emitted with its bound otherwise; the sampling interval attached to each row. Check: bias pre-registered per (descent duration × rate) from the committed raised-cosine sweep beside blink-sample-rate.txt, both branches pinned at 25/30/60 fps; one bias number generated from the sweep feeds 10.10c's sentence.
- [ ] 12.8 Blink reopening velocity and reopening amplitude-velocity ratio (after 10.3's ruling and 12.8a). Check: an asymmetric synthetic recovers both peak velocities within 5%; a regression test proves the reopening window clips at the next blink's onset; within-device-only caveat until 10.9's gap is explained.
- [x] 12.9 Inter-blink-interval irregularity (coefficient of variation), DONE 7 September 2026: blink RATE cannot tell a metronomic blinker from one who goes quiet and then flurries — both report fifteen a minute — and this project had no meter for the difference. `src/core/blinkRhythm.ts` is the coefficient of variation of the onset-to-onset gaps, taken from `statistics.ts` rather than hand-rolled so the one place this repository defines a coefficient of variation stays the one place. A RATIO and not a spread in milliseconds, which is the whole design: 200 ms of spread means something different to somebody blinking every second and somebody blinking every ten, so a raw spread would be measuring blink rate a second time under another name. Check met on all three clauses: a known 0.25 recovered from alternating 3000/5000 ms gaps; null below fifteen blinks, and null is a REFUSAL rather than the zero a metronome scores; unchanged when every timestamp is multiplied by 7.5. Onsets that do not move forward throw by name, because a backwards gap would shrink the mean and inflate the ratio, which is a wrong number that looks entirely ordinary. The floor of fifteen is stated as a CHOICE and not dressed as a derivation: fourteen gaps is where one unusual gap stops being able to double the answer, and at an ordinary blink rate it is about a minute, the same order as the PERCLOS window. 10 tests watched failing first; three mutations — the floor dropped, a spread substituted for the ratio, and the forward-time refusal disabled — each redden their own tests, and the floor is now in `tools/mutationCheck.mjs` from both sides, taking it from 29 mutations to 31. One correction inside the row: the first boundary tests derived their probes from the constant, so moving it moved them too and the floor passed at any position; they are written as the literals 14 and 15 now, which is what roadmap 10.1c learned. Nothing is wired to the page or the export: 12.9's Check is the measurement and its properties, and building a consumer ahead of a row that asks for one would be guessing at what it wants. **Started while the Phase 12 gate was shut (amendment 20): this row is not on the header's exempt list and 10.11, 10.12a-c, 10.13b, 13.2 and 13.8b were open. Nothing published depends on it, because it was shipped with no consumer, but the fifteen-blink floor was chosen against the timing 13.8b will change. Re-look when the gate lifts: 13.8b, 12.0a.**
- [ ] 12.10 Closure-fraction curve: an honest multi-line family (30/40/50/60% of baseline); the existing PERCLOS becomes one point on it; score.ts untouched. Check: monotone non-increasing on every synthetic and fixture; every column labelled instrument-referenced, with the header saying why none is literature P80.
- [ ] 12.10a A blink-excluded PERCLOS beside the current one [C7]: a second accumulator excluding closures shorter than `MAX_BLINK_DURATION_MS`, both columns exported; the score keeps the current one until 12.18 reads. Check: unit tests on hand-built series; the DROZY refit from retained CSVs (rho side by side); the UTA-RLDD comparison is a named 12.18 rider, never a separate read; the literature's definition named in the header. **BLOCKED: on the owner's machine. The Check's second clause wants the DROZY refit from the retained CSVs with rho side by side, and those CSVs are not in this repository — row 10.10c2b already records that the measured corpus directory exists only on the owner's machine. The unit tests on hand-built series and the literature's definition in the header are startable now; the row as a whole is not. Verified 7 September 2026.**
- [ ] 12.12 Look-away events and off-screen gaze share, after 14.9a and 14.9: unknown frames are neither on nor off, blink frames are excluded, and the off-screen threshold derives from 14.9's error file rather than the eyeballed 0.18. Check: known spans recover exact counts; the 499 vs 500 ms boundary pinned; unknown frames can neither count as off-screen nor bridge an event.
- [x] 12.14 Time-on-task drift of blink parameters, DONE 7 September 2026: does somebody's blinking change over the length of a sitting, and the obvious place to look is the wrong place, which is the whole row. The per-second buffer keeps `FEATURE_RECORD_CAP` rows, about an hour, and drops the oldest to stay bounded, so in any longer sitting it answers "an hour ago" when asked when the session began: a drift read from it would be a real number describing a session that did not happen, and the longer somebody sat the less of their sitting it would cover. `src/core/blinkDrift.ts` reads the BLINK LOG instead, which is bounded at thirty times the per-second cap, and takes the session start as an ARGUMENT rather than inferring it from whatever is still in a buffer, because inferring it is the mistake. Check met: a three-hour synthetic whose blink duration climbs steadily reports the change measured from the true start, and the same events read from where the buffer would have wrapped understate it by more than half — the assertion is written against the true reading rather than against a number typed into the test. Two design choices, both stated as choices: the halves split by TIME and not by blink count, because half the blinks is not half the sitting and a count split would draw the boundary wherever blinking happened to cluster; and the change is a FRACTION of where it started, for the reason 12.9 chose a ratio, since 100 ms means something different to a 200 ms blink and a 600 ms one. A blink whose parameter was never measured is skipped rather than counted as zero, a blink stamped before the session start throws by name, and the floor of fifteen a half is the rhythm floor for the rhythm floor's reason. 13 tests watched failing first; four mutations take `tools/mutationCheck.mjs` from 37 to 41, all caught, including one that turns the fraction back into an absolute difference and one that moves the midpoint off the middle. The label travels with the number and refuses the interpretation: this is a demonstration, validated against no outcome, and 12.18 is the row that would earn a stronger word. Nothing is wired to the page or the export. **Started while the Phase 12 gate was shut (amendment 20): this row is not on the header's exempt list and 10.11, 10.12a-c, 10.13b, 13.2 and 13.8b were open. Nothing published depends on it, because it was shipped with no consumer, but the fifteen-blink floor and the halving midpoint were chosen against the timing 13.8b will change. Re-look when the gate lifts: 13.8b, 12.0a.**
- [x] 12.16a Scene and face luminance, the pure half, DONE 7 September 2026: split from 12.16 on taking it up, along the seam row 9.3a cut for the pupil and for the reason LEARNING recorded there — when an increment straddles the core/io fence, cut it at the fence, so the testable side is exhaustively tested and the side needing a camera shrinks to the thinnest adapter. `src/core/sceneLuminance.ts` takes a `LuminanceField` (the grid row 9.3a already produces from RGBA) and returns its mean, or null. Check clauses met on this half: a dark field reads low, a bright one high, and an unevenly lit one averages — with the spotlight case asserted as the PAIR it exists to justify, a lamp on the face leaving the scene dark, which is why two regions are worth exporting rather than one; malformed fields refuse, including the one a wrong crop actually produces, where the dimensions and the sample count disagree. **BLANK-NOT-ZERO IS PINNED AT ITS SOURCE, not at the serializer:** a fully black frame reads 0 because a lens cap is a real measurement, and a failed field reads null, and a test holds the two apart — an export that gave them the same value could not tell a dark room from a dropped frame. The label is a constant with its own tests: it says "the camera's rendering of light, not lux", because a webcam's automatic exposure and white balance act before this project sees a byte, so a bright room and a compensated dim one can arrive looking alike. It carries NO verdict, and a test enumerates the adjectives it may not contain, because the parked half is parked on purpose: no threshold until something is measured against an outcome, which is 13.6b's job. 14 tests watched failing first.
- [x] 12.16b Scene and face luminance, the wired half, DONE 7 September 2026: the row's design decision is that ONE downscaled raster serves both numbers. Landmarks arrive normalised, so a face covers the same FRACTION of any raster whatever the camera's resolution; `faceBox` maps that onto the thumbnail and needs no frame size at all. So the browser downscales the whole frame once into 64 by 36, the scene mean is taken over all of it and the face mean over the box the face lands in. The alternative was two full-resolution reads: about eight megabytes a second through `getImageData` on a 1080p camera, into a loop the September audit already flagged for drawing more than it needs. This is about nine kilobytes, at the same once-per-row budget the pupil read is held to — and it buys a property worth more than the bytes, because both numbers come from the SAME frame at the SAME exposure through the SAME scaling, so the difference between them is a fact about the light rather than about how they were read. Check met: `sceneLum` and `faceLum` are appended TRAILING, so every older header stays an exact prefix and the Python loader's new `PRE_LUMINANCE_COLUMNS` generation is sliced from `COLUMNS` while every older one is sliced from the generation after it, never by an absolute offset — the discipline row 12.15 found a latent defect against; the columns' presence and their null-is-blank serialization are pinned against the record's own keys, and the cross-language contract test holds both languages to the same list; SPEC.md carries the label 12.16a fixed. A new `fractionOrNull` validator refuses a luminance outside [0,1], tighter than `nonNegativeOrNull` on purpose, because 1.5 would pass that one and a reader would have no way to know the column had stopped meaning its header. `faceLum` is null with no trusted face while `sceneLum` is still read, because how lit a room is does not depend on whether the model found somebody in it, and a dark scene with no face is exactly the row a later reader would want. Its own tiny offscreen canvas rather than the pupil's, which is sized to the camera and would be resized back and forth twice a second.
- [ ] 12.18a The UTA-RLDD v2 plan (docs/uta-rldd-plan-2.md), committed before re-download: the droop, taxonomy, completeness, reopening, interval, closure-curve, blink-excluded PERCLOS, yawn and nod families as named predicted riders; the look-away and gaze riders only if 14.9 has produced a committed number; the shape-feature ablation (v1 features minus shape) and the within-subject null with the paired share of subjects as pre-registered sections; a v1-parity gate CONDITIONAL on 10.14b's container coverage check passing. Check: the plan names the pinned build and every rider's prediction.
- [ ] 12.18b The one-shot UTA-RLDD v2 re-measure, owner, on the pinned build with 10.14b's manifest. Check: the coverage check passes per clip or the clip is excluded by name; the exports carry `app_commit`.
- [ ] 12.18c The pre-registered LOSO v2 analysis: per-feature and combined LOSO with 1000-shuffle controls and the within-subject null; intervals on every number; predictions kept when wrong. Check: parity within the pre-stated tolerance where the coverage check passed; the result file is the last read of these labels.

## Phase 13. Capture chain and platform truth (the maximum this hardware can deliver, measured)

The app has never asked its cameras for more than the default, never recorded which delegate actually ran, never measured its own first load, and, the audit found, measures processing ticks rather than delivered photographs. Rows 13.2, 13.4 and 13.8 are the front of the era.

- [ ] 13.2 Frame-rate negotiation, FIRST [C1]: read capabilities, then ask the live track for 60 fps as a MEASUREMENT, with `facingMode: {ideal: "user"}` and the dim-room session 10.12a needs on the same day. Check: the builder test asserts negotiated resolution is unchanged by the frameRate step, any change surfaced rather than silent; a dated docs section records declared max, negotiated rate and any resolution change per owner device; the adoption condition written before any device is read; the day decides whether live accuracy is capped at 30 fps on every owned camera.
- [ ] 13.4 Frames-missed-while-busy counter from presentedFrames metadata. Check: synthetic sequences (1,2,4,5 yields missed 1; a monotonicity violation refuses, never goes negative); two export rows; one deliberately-loaded session in a dated note. Lands before 13.8 so its before/after is trustworthy. **BLOCKED: on a live camera under load. The Check ends "one deliberately-loaded session in a dated note", and this container has no camera and no way to load one. The synthetic sequences and the two export rows are startable now; the dated note is the owner's. Verified 7 September 2026.**
- [ ] 13.8a Inference once per photograph, the arithmetic first [A2]: pre-registered on the existing dry-run exports that A/V scales inversely with processing/delivery ratio across the 30 / 55 / 126.7 fps machines; no code. Check: the prediction and its outcome in a dated docs section before 13.8b opens.
- [ ] 13.8b Inference once per photograph, the driver, a CORRECTNESS row [A2]: `processFrame` driven from `requestVideoFrameCallback` (one call per presented frame, rVFC timestamp), rAF left to repaint, the rAF fallback kept and the driver recorded in the export header; peak closing velocity and amplitude-over-velocity stop scaling with display refresh over camera delivery. Check: a driver-level test detects exactly once per presented frame; the stepped Eyeblink8 rerun reproduces 10.8a's anchor digit-for-digit (the stepped path does not read the driver).
- [ ] 13.8c Inference once per photograph, the consequences [A2, E10; F-044, F-082, F-109]: one A/B live session with a velocity PREDICTION, not an equivalence expectation (owner); the LID_SLUGGISH ramp and MANUAL item 26 re-derived; docs/latency.txt re-run and the "42 µs core chain" sentence withdrawn (the per-tick cost is quadratic in tick rate, about 5 ms at 120 Hz with a still gaze); the per-tick residue: readouts rewritten only on change, running min/max in `detectFixations`, the pupil read drawing only the source rect. Check: the A/B prediction scored; the latency benchmark carries the work; the fixation scan is linear by test.
- [ ] 13.0 Engine agreement on a fixed clip, owner [C10, D17, B6's measurement half]: one commit, one machine, one prepared clip, WebKit and Chromium paired, on the m5max template with three outcome classes committed first; `user_agent` and the webgl2 probe written in clip mode so an export can name its engine; the stepped frame-count assertion made the WebKit project's mandatory local test; the results block says "WebKit binary" and "no engine other than WebKit has measured this corpus" until this row lands. Check: identical counts confirm; blinks moving with coverage identical is the cross-engine bound the project does not have; coverage differing at all stops the line; a precondition of 13.1's phone rows.
- [ ] 13.1 Session survival kit, phones and laptops [E8]: a scoping ADR first (citing the five phone sessions already in the record and the engine question 13.0 answers), then a wake lock re-requested on visibilitychange (the 260 s light protocol and 14.10, 14.11 need it on laptops as much as on phones), orientation re-reads with a flip count in the export, and the Mirror default off on an environment camera. Check: constraint-builder test, wake-lock wrapper against a fake navigator, orientation event test, MANUAL.md items.
- [ ] 13.3 Capture-rate claim: repeat the cued protocol (11.0a/b) wherever a setup negotiates ≥55 fps, at 30 and 60. Check: the plan pre-commits BOTH branches — the improved-catch prediction against blink-sample-rate.txt's sweep, and the empty-set branch ("no owner camera negotiated above 30: platform limit, prediction unscored").
- [ ] 13.5 Delegate truth and inference percentiles in the export [D8]: the request, the webgl2 probe and p50/p95 inference, unconditional and in clip mode too, because the executed delegate is unobservable in the vendored API and MediaPipe falls back to CPU silently; camera rows conditional, machine rows unconditional; one CPU retry when the GPU load rejects. Check: null-in/unknown-out tests; a resultGuard pin holds the observability sentence in MODEL_CARD; eyeblink8-result.txt's run header says "unknown, probe added after this run" where it is unrecoverable.
- [ ] 13.6a Capability ladder NOW, from what is already measured [E6]: delivered rate and iris-pixel verdicts rendered per setup on the existing sessionVerdict surfaces, light reading "unknown" until 12.16. Check: a unit test per verdict boundary with every null path yielding "unknown"; Playwright asserts the rendered ladder matches core verdicts; each sentence cites its source doc.
- [ ] 13.6b The light term joins the ladder, after 12.16. Check: the threshold derived from committed data or the cell stays "unknown".
- [ ] 13.7 Resolution-versus-rate probe and an honest mode menu, after 13.2 and 13.6a. Check: pure summarizer tests; fake-camera Playwright for rendering and export of the chosen mode; a dated note with real per-device probe tables.
- [ ] 13.10 Cold-load truth [E9]: measure time-to-first-measurement, then bound it; the 15.8 MB first load (3.8 MB model, 12 MB runtime) prefetched at page idle, a load timeout landing in `modelFailed`, the Cache API after first load. Check: docs/cold-load.txt with per-file wire sizes under stated profiles; a load-timeout state machine test (timeout plus no model is a visible stuck state, never healthy idle); the improvement row may terminate in "not fixable on this host".
- [x] 13.11 Synthetic pupil resolution floor, DONE 7 September 2026, and it REFUTES the explanation the project had been carrying. The prediction was committed and pushed in its own commit before the generator existed (`docs/pupil-resolution-floor.txt`): the floor would land below 43 px, expected 12 to 30 px at a one pixel blur. DIRECTION CORRECT and not marginally — the floor is 4 px at no blur, 5 px at one pixel, 8 px at 1.5, and 18 px at a heavily softened four pixels, so a 43 px iris clears it at every blur swept. MAGNITUDE WRONG: 5 px at one pixel of blur is well under the predicted band, because the reasoning over-estimated how much contrast the estimator needs; at a 5 px iris the pupil radius is 1 px against a 1 px blur and it still clears the 0.12 gate. The band is left standing in the document rather than corrected. **What it means is the row's real output:** iris pixel count ALONE does not explain the 0.4 percent yield of the 9.4 light-response run, so `docs/pupil-light-result.txt`'s line localising the limit to the small webcam iris is incomplete, and the remaining candidates are reflections, exposure, lashes, the lid, motion and sensor noise — none of which a synthetic disc has. 13.12 stays dormant: its arming condition has a number now, and the number says the condition was never really about pixels. Check met on every clause: the committed table at `docs/evidence/2026-09-07-pupil-floor/resolution-sweep.csv` is regenerated and compared digit for digit on the verdict-fixture pattern; the floor is monotone in iris width at every blur above the meaningful minimum; the anchor was committed before the sweep; the floor is labelled model-derived until real optics clear it. **Two findings recorded rather than trimmed.** The sweep is NOT monotone below 4 px — 2 resolves, 3 does not, 4 does — because at a 3 px iris the pupil is 1.2 px across and the answer turns on where the raster's centre lands; the minimum is declared, the monotonicity test runs above it, and a separate test pins the 2/3/4 flip so the minimum cannot later be raised to hide a real island. And the FIRST run of this sweep started at 8 px and reported a floor of 8 px at most blurs, which was the edge of the grid rather than the floor; it was extended downward before anything was written down. A sweep whose smallest entry is its answer has measured its own grid.
- [ ] 13.12 Pupillometry re-run on real optics — DORMANT, owner-gated, zero rows now: armed only when optics that clear 13.11's floor become accessible, through the unchanged pre-registered gate in docs/pupil-light-plan.md.
- [ ] 13.13 The detector recovers from a lost face instead of riding it to the end of a clip. On 8 September 2026 one run of six lost the glasses clip's face 39 seconds in and never reacquired it: faceDetected false to the end, inferenceMs at ~1 ms, recall 14.0% for a day (docs/eyeblink8-result.txt, the transient section). Check: a fake-landmarker harness that returns no-face from a chosen frame onward shows the wiring re-attempting acquisition rather than coasting, and a stepped run whose face is lost for more than a few seconds says so on the page and in the export rather than finishing silently. **BLOCKED: the real defect occurred once in six runs and cannot be reproduced on demand, so a fix cannot be shown to fix the real event; starts when a reproduction exists or the owner accepts the harness as its stand-in.**

## Phase 14. The demo front (legible output, interactive protocols, honest theater)

The show-and-tell goal, plus the cognitive-state work honestly reframed as task effects. The session-end state and the phone-first surface land first, because a demo that loses the session on Stop or traps a phone user is not a demo; the timeline strip lands next because the podium view (14.2), the report card (14.4) and the tour (14.5) draw on it.

- [x] 14.0a Session end is a state, DONE 6 September 2026 [A4, A17, A22]: `ended` reached from Stop, clip finish and the crash path; exports, report and KSS-after depend on it (KSS-after asked once, on the transition, with `kss_after_at_seconds` exported); calibrations, markers and light depend on `running && camera`; interruptions gated on running with a null stamp exported as `unknown`. Check: unit tests per transition and one Playwright sequence each for stop-then-export (export enabled, KSS-after asked once) and clip-then-report. Required before any pilot session is asked of a stranger.
- [x] 14.0d The camera that cannot be turned off, and the one that stopped, DONE 6 September 2026 [A5, A18, A26]: a superseded camera start stops its stream (request, check the token, then attach) and the picker and clip input are disabled while a start is in flight; delivery rates and the evidence rate captured at the `ended` transition rather than at the first consumer; a camera that stops delivering reads "no frames in the last 5 s", not "this browser does not report it", track `ended`/`mute` routed to failed or ended, and record writing waits for a delivered frame. Check: a Playwright picker double-change leaves one live track; the report's evidence rate is the same number at every consumer; a frozen fake camera ends the session by name.
- [ ] 14.0e The crash branch and the reset [A21, A17's residue]: a stepped clip that throws mid-run exports as a file session with its clip name (`# source: file`), the decision extracted into core; `resetSession` clears calibration state and inference samples; every stimulus start refused or exported. Check: an e2e injecting a throw during a stepped clip asserts the provenance rows; a unit test that a reset leaves no calibration in flight.
- [x] 14.0b Phone-first demo surface, DONE 6 September 2026 [A6, E1, E4, E5, B19]: the Source box first under 1000 px so Start camera is in the idle viewport; the light overlay guarded on `requestFullscreen` with a touch exit and touch wording; idle strings that say "not measuring", never "measuring..." or "Blinks: 0"; the short caveat returns beside the score; a model-loading sentence on the camera path and the baseline countdown beside the score; the refusal sentence rendered whenever `calibrationRefused`; the stored-data box titled for what it lists. Check: a 375-wide Playwright project asserts Start camera is within the idle viewport; lightResponse.spec.ts covers the no-fullscreen path via addInitScript; an e2e that `#box-alertness` contains "not a safety or medical device"; uiGuard extended to button and label strings and the idle-string table.
- [x] 14.0f1 Keyboard and the dialog [E2], DONE 7 September 2026: Escape closes every screen the page raises over itself, and the sleepiness question is a native `<dialog>` opened with `showModal()`, focused on Skip. `src/core/overlayEscape.ts` is the register of all five screens and whether Escape may close each; `src/main.ts` reads both its keydown handler and its refusal from that one list, and `tools/uiGuard.mjs` holds the register to the page in both directions, so a screen added without an entry goes red. THREE things this row found that were not in its own text. One, the page was not keyboard-dead: row 14.0b had given the light stimulus an Escape listener of its own, written beside it, and nothing carried it to the other three overlays — the escape hatch existed and nothing made the next overlay inherit it, which is this project's recurring shape rather than a new one. Two, a `<dialog>` CANNOT be made to refuse Escape through its `cancel` event: `preventDefault()` holds the first press and, with no user activation in between, Chromium fires cancel again and closes anyway. Watched happen on the second press. The refusal is now a capture-phase `preventDefault()` on the keydown, so no close request is ever made, and the reason is written where the rule is. Three, `hidden = true` on an OPEN modal gives `display:none` while the element still matches `:modal` — the question vanishes and the page behind it stays inert, a page that looks fine and accepts nothing. All five reset paths spelled it exactly that way while it was a div, so `hiddenAssignments` in uiGuard now refuses the old spelling. Initial focus is Skip rather than the first rating for a data reason: a modal focuses its first focusable element, which is `1 Extremely alert`, so a reflexive Enter would write a sleepiness label nobody meant into an exported file; Skip records a declining, which is true, and stays last in the tab order. Check: 16 unit tests over the register, watched failing first and each verified against a bend of the module; 4 Playwright tests, each verified against a bend of the wiring; two uiGuard checks, each watched to redden against a bend of `main.ts`. The overlays' own words now promise the key they honour.
- [x] 14.0f2 The page links to what it cites [E7], DONE 7 September 2026: the nav bar gains a repository link, the conditions sentences' cited documents render as anchors pinned to the commit in the page's own `<meta name="build-commit">` tag, and the head gains a description and an icon. `src/core/docCitations.ts` segments a sentence into text and citations and reassembles to exactly the string it was given, so linking a sentence cannot become a way to edit one; `src/core/pageIdentity.ts` owns the repository URL, the description and the brand mark's path, and the nav mark now reads that path from it so the header and the tab icon are one drawing rather than two that agree today. **The favicon decides the held choice by deciding as little as possible:** it is the eye the nav bar already draws, on the page's own ink, so nothing new was chosen about how this project looks; the one difference is a 2.4 stroke where the header uses 1.8, because at 16 px the thinner line disappears, and that is written into the file. Reversible in one commit. **Three things worth recording.** One, the citation reader was given an explicit test for a path that ENDS a sentence, because the extension dot and the full stop are the same character and this repository has shipped that defect twice; the bend that removes the anchored extension reddens exactly that test. Two, `linkHrefs` read only quoted URLs, so it went silent the moment the repository URL moved into a shared constant — the direction this repository keeps moving things — and a guard that falls quiet when the code improves is a guard that will be quiet on the day something is missing; it reads both forms now. Three, the first version of the e2e href assertion had an ungrouped alternation, so `|` split the whole pattern and the test would have passed on any string ending in the path; found by re-reading it rather than by a failure, and the grouping is now commented as load-bearing. The dangling-citation check is PREVENTIVE and says so: all 26 paths cited in `src/` exist today, so it caught nothing on the day it was written. Check: 13 unit tests on the segmenting and the URL, each verified against one of four bends; 12 disk-reading checks over the icon, the head and the nav; 2 Playwright tests, both verified against a bend of the wiring.
- [x] 14.0g The demo page carries no explanatory notices, DONE 8 September 2026: the two "Conditions:" caveats and the export-contents paragraph came off the page at the owner's direction, because they interfered with the demo's use. The numbers stay where they are measured — docs/sampling-bounds.txt, docs/blink-sample-rate.txt and the committed tables — and `exportContents.ts` left the repository with its disclosure, with README's privacy section regenerated to the stored-keys enumeration alone. This supersedes 14.0f2's on-page citation anchors for the removed sentences; the reasoning stays in docs/ and this ledger. (PR #468)
- [ ] 14.0c The page's own documents tell the truth [D15]: docs/UI.md brought to the shipped design; MANUAL items added with dated "superseded by" notes; the PR template requires a reason for "n/a". Check: uiGuard sees the sections UI.md claims; MANUAL items 56, 57, 64 and 66 marked owner-manual and performed once, dated.
- [ ] 14.9a The gaze profile refuses and carries its conditions, BEFORE 14.9 and before any gaze signal [A23, C9]: per-axis RMS residual and R² with a pre-stated refusal bound (RMS > 0.15 or R² < 0.8); median pose, iris width px, viewport, DPR, screen size and camera stored with the profile and refused or flagged on a different window or camera; the calibrated point nulled with "head moved since calibration" beyond ~5° or ~10% iris width; the residual in the profile, the button label and the export header. Check: the noise-only-axis case refuses; `gaze_calibrated` and the threshold exported; a pinned profile with a stale window refuses on load.
- [ ] 14.9b Blinks out of the gaze chain, one definition of on-screen [A25, C9]: the offset nulled below the blink line, I-DT bridging a blink-length null gap, blink frames excluded from the heatmap, the dwell and the on-screen column; with a profile, on-screen means the calibrated point within the margin, worded "window" not "screen"; one ruler for live and replay I-DT. Check: the fixture recovers one fewer fixation per blink than today; the export, heatmap and quadrant on-screen definitions agree on the fixture.
- [ ] 14.9 Gaze validation pass and mid-session drift probe: five fresh points, screen-fraction error, accuracy and precision reported separately per target, the pose delta logged beside every error sample so drift and movement separate, stored and exported. Instrument only, no claim. Check: pure scorer on synthetic profiles of known error; SPEC and Python loader contract for the new column; the first committed gaze number replaces "not measured" in MODEL_CARD.
- [ ] 14.1 Session event timeline strip: blinks, closures, alerts, and score over the session, with every null second a visible gap. Check: property tests from synthetic records and the committed fixture (no zero-height points, gaps never bridged); e2e via synthetic injected events, never a face video.
- [ ] 14.2 Podium big-numbers view for a projector, drawing on 14.1's strip. Check: a no-face fake camera renders the refusal sentences verbatim, never "0"; the big view and the panel share the identical formatted string for a null; the demo notice stays legible at distance.
- [ ] 14.3 Explain-this-number provenance popovers: every rendered metric names its status in the record's real taxonomy (the score is cohort-level AUC 0.70 and unvalidated per-person; PERCLOS is an instrument-adjusted convention that includes blink time; velocity is device-conditioned until 13.8b). Check: a CI guard iterates the provenance table — every metric has an entry, and every evidence-bearing status names an existing docs/ result file.
- [ ] 14.4 Printable report card, with 14.1's strip. Check: parity test against the text report (withheld lines carry reasons, unknown never renders empty); unreachable while the camera runs; the demo notice prints.
- [ ] 14.5 Guided two-minute tour, on the 11.0a/b cued protocol, with 14.1's strip as its recap. The 30 fps misses are the script, not the bug: the tour shows a per-blink caught/not-caught tally and narrates the sampling story rather than coaching only slow blinks. Check: an empty event stream ends every gated step not-caught and the recap reads 0/5, never a faked success.
- [ ] 14.6 Record-yourself clip walkthrough, feasibility probe first. Check: the probe row pre-commits its result (recorded Blob stepped through the existing pipeline, honest refusal for variable-rate or duration-less recordings by name, which 10.14 now provides); later rows gated on the probe.
- [ ] 14.10 Follow-the-dot smooth-pursuit test: schedule, overlay, bounded offset log, analysis, and a pre-registered discrimination run, on RAW offsets or with the One Euro lag (100-300 ms by step size) pre-registered per amplitude, because the lag is the size of the effect. The output says "tracking agreement", not pursuit purity. Check: synthetics recover gain 0.8 and lag 100 ms within tolerance THROUGH the smoothing filter, center-stare near zero, anti-phase negative — a mutation per rule; the discrimination result kept if it fails.
- [ ] 14.11 Task-block schedule machinery and read-vs-rest blink suppression — the era's honest "cognitive state" claim, a demonstrated task effect on the owner's own sessions. Check: the no-difference synthetic must FAIL the analysis; per-block rates from blink-log events, never the rolling window; a pre-committed droop refusal bound so suppression-versus-masking is visible.
- [ ] 14.12 Reading-rhythm detector from the horizontal gaze sawtooth, demo-only until 14.11's run carries its committed prediction. Check: sawtooth accepted; staring, random walk, and the PURSUIT SINUSOID rejected, with the asymmetry-rule mutation reddening; promotion capped at "checked on the maintainer's sessions".
- [ ] 14.13 Scripted look-away validation for 12.12, per device, on the 11.0a/b cue machinery. Check: cue schedule and metadata pure and tested; the ignore-the-cues adversarial synthetic must FAIL the criterion; the derived per-event table (cue, caught, latency, device class, delivered, processing and sampled rates) generated by the report tool is committed, and raw session CSVs stay in $DATASETS, since they identify a machine.
- [ ] 14.14 In-browser psychomotor vigilance task (PVT): reaction times and lapses beside the eye features, within the moderated-volunteer discipline. Check: pure scorer tests failing first (lapse threshold, false starts, inter-stimulus bounds); e2e off a shortened injected schedule; columns null outside an active PVT window; the eye-features-versus-lapses claim is its own later pre-registered row.

## Phase 15. Full re-validation, in layers (every published number re-derived on the pinned build, on public data and on people, across devices, with an interval)

The owner's sixth brief: the validation must be complete and multi-layered. This phase does not add signals; it re-derives every number the page and the card publish, on the pinned build (11.8a), through six named layers, and replaces the hand-written claims with a generated report. Nothing here starts before every Phase 10 row except the conditional 10.7c is ticked, because re-validating a display-rate velocity or an owner-referenced shut line would validate the artefact; each layer names its own prerequisites. Volunteers stay pseudonymous and numbers-only under the participant discipline docs/validation-plan.md set; raw session files never enter the repository, only the derived tables the report tools produce; no frame ever does.

- [ ] 15.1 The validation matrix and its generator: a dated docs/validation-matrix.md naming the six layers (L1 pinned fixtures and an injected face in CI; L2 public corpora, stepped; L3 repeatability across runs, engines, devices and calibrations; L4 the cued live protocol on volunteers; L5 the rounds on the pinned build; L6 intervals and pre-registration on every read) and the device matrix (macOS laptop, iPhone, Android phone volunteer-supplied or ABSENT, external webcam volunteer-supplied or ABSENT, tablet volunteer-supplied or ABSENT, Windows laptop or ABSENT), each cell's status generated from committed evidence files and this ledger's ticks. Check: a tool renders the matrix and a test refuses a cell that reads "done" without a committed file behind it; the matrix is the only place a coverage claim may be made.
- [ ] 15.2 L1, a face in CI [D7], after 13.8b and 14.0a: a DEV-only landmark-injection hook fed the consented fixture recording (the owner's own landmark numbers, already committed) or a scripted blink, so an e2e asserts "Blinks: 2", the 25 fps refusal and a PERCLOS value through the real wiring; `blinkFeed` and `shutLineFeed` extracted into src/core under the coverage floor and the ratchet; the branch floor made a ratchet (current − 0.5, raised on green runs). Check: CI goes red with blink counting, aperture, PERCLOS or the fps refusal broken in the wiring, which today it does not.
- [ ] 15.3 L2, the public corpora on the final pinned build, after 11.3, 11.4 and 12.18c: Eyeblink8 re-run on that build (it is the regression harness; re-reads are allowed), TalkingFace and Silesian5 read once on it, UTA-RLDD v2 read once on it, DROZY retired (10.3); each with the container coverage check, the digit-reproducible command, the engine and Playwright version, and an interval. Check: the three once-only reads and the Eyeblink8 re-run name the same `app_commit`; a table in the matrix lists recall, precision and their intervals per corpus side by side.
- [ ] 15.4a L3, repeatability of the stepped path: the same clip stepped twice on one machine (byte-identical exports); the same clip on WebKit and Chromium (13.0); the same clip on at least three owned devices (device independence of the stepped path, a property the record does not yet hold). Check: every comparison's bound committed before the second run; a mismatch is a finding with its own row, never a tolerance widened after the fact.
- [ ] 15.4b L3, repeatability of the person-facing instrument: the same person's guided calibration run twice on one setup and once on a second (test-retest of the personal ruler: line mm and separation ratio within a pre-stated bound, from 11.6a-c's exported procedure numbers); the same session's verdict re-derived by the Python mirror byte for byte (10.15). Check: the bounds committed first; the numbers land in the matrix as their own cells.
- [ ] 15.5a L4, the cued live plan: the pre-registered plan for the cued protocol (11.0a/b) on volunteers, with each volunteer on at least two device classes, n chosen so the per-cell Wilson interval on cued catch rate is at most ±10 points (about 60 cued blinks per cell), prediction bands per (delivered rate, processing rate) cell with device class as the label amendment 16 makes it, the dim-room condition, and the 20 s cued closure for PERCLOS. Check: the plan names the pinned build, the n arithmetic and the voiding conditions before any session.
- [ ] 15.5b L4, the report tool: catch rate, latency, long-closure count per cued closure, duration per cell, PERCLOS under the 20 s closure, all with intervals and every row printing delivered rate, processing rate and sampled fps; adversarial synthetics before any real file. Check: the ignore-the-cues stream scores 0; a mixed-build folder is refused by name.
- [ ] 15.5c L4, the sessions, owner-moderated: at 30 fps and, where 13.2 negotiates it, 60 fps; external webcam, Android and tablet volunteer-supplied or ABSENT. Check: every session file carries the pinned `app_commit` or is excluded by name; raw files stay in $DATASETS.
- [ ] 15.5d L4, the report: the derived tables committed, the matrix cells filled, the predictions scored. Check: a cell with fewer cued blinks than the plan's n reads "under-powered", never a number without its interval.
- [ ] 15.6 L5, the rounds on the pinned build, after 11.7, 11.8 and 11.9: validation round II, the owner's n-of-1 as the within-subject layer, and the assessment pilot with its publication restraint. Check: each round's frozen plan names the build; the intake refuses a mixed folder; the results carry intervals and their voiding conditions scored; the KSS-versus-feature relationship is a bound, not a claim, until 11.9's stated n is reached.
- [ ] 15.7 L6, the generated validation report: docs/validation-2026.md written by a tool from the committed evidence — every headline number with its interval, its layer, its device coverage, its corpus or cohort size and its date; MODEL_CARD's "who it has been tested on" and README's results block regenerated from it; resultGuard pins every sentence to the file it came from. Check: a hand-edited number in either document reddens the build; the report regenerates byte-identically from the evidence directory.
- [ ] 15.8 The claim gate: after 15.7, no accuracy, precision, drowsiness or gaze sentence on the page, in README or in MODEL_CARD that is not in the generated report, and no sentence without its interval and device scope. Check: claimGuard's regex family extended with the retired forms of every superseded claim; the provenance popovers (14.3) read their status from the same report.

## Deliberately not taken up in this era

- Researcher's Night corpus scoring: deferred, the stepping-time sink; TalkingFace plus Silesian5 already deliver the generalization statement. A future-era candidate once those two are read.
- ZJU: no row until 11.1 verifies its download contains video rather than annotations alone.
- A separate immediate UTA-RLDD gaze read: merged into 12.18, not killed — the labels' third read is batched and final, never the cheapest and first.
- A second read of the sibling corpora (the former row 11.5): 10.8's delta is scored on Eyeblink8 by its own committed prediction, and the siblings are read once, on the final pinned build.
- Saccade and pursuit velocity, and microsaccades: 30 fps physics forbids them (a saccade spans one to two frames, so any velocity would be fabricated between samples). Turnover, drift, and tracking-agreement are the honest residues.
- Any post-hoc winner from the closure-fraction curve feeding score.ts: a forbidden path; any line-separation claim goes through 12.18's report-all or not at all.
- WebGPU delegate work and WASM optimization of the core chain: the delegate does not exist in the vendored API. The "42 µs core chain" sentence is withdrawn by amendment 17 (the per-tick cost is quadratic in tick rate, about 5 ms at 120 Hz with a still gaze); the honest fix is 13.8b, not an optimizer, and 13.8c carries the residue. 13.5's request-and-probe recording is the honest maximum on the delegate.
- New hardware purchases: out by the owner's own constraint. 13.12 stays dormant until optics the project already owns become accessible.
- A WebCodecs decode path for corpus runs (audit F-083): the "hours per dataset" premise is contradicted by the runner's own ~58 frames per second, and the seek path is the reference the ratchet pins.
- Restructuring the documentation layer beyond single-source numbers (F-070): the measurements hold (62% of recent file touches are prose) and the rule adopted is the narrow one, a number that appears in more than one document is generated or appears once with links; archiving docs/audit was declined on 15 August and nothing has changed.
- I-DT thresholds in degrees before 14.9 gives the geometry to state a degree (F-035); the replay's second ruler is a display choice.
- Per-eye iris offset bias (F-088): reproduces at about 0.07 and no consumer uses a single eye; reopened the day a single-eye fallback, asymmetry or vergence feature is proposed.
- A second full review before the ladder is half done: the August review found drift in prose, this one found it in wiring and at the border between the two languages; a third pass before Phase 10 is ticked would find the same classes wearing new numbers.
- Rewriting main.ts: still correct, linted and honest about its size; 14.0a-e and 15.2 pull the two decisions that produced defects (the session boundary and the gate wiring) into src/core, and nothing else moves.

Ideas, not commitments:

- Microsaccade detection, needs higher frame rates than any owned camera delivers, a genuine research challenge — still parked.
- Ambient light COMPENSATION (the measurement half is 12.16; correcting a measurement by it waits until a measured threshold and a validation path exist).
- Researcher's Night as a third sibling corpus, once TalkingFace and Silesian5 are read.
- A second learned-model attempt against the heuristic score, only if 12.18's new features move the LOSO needle (9.1's two-bar rule stands).
- Slow-drift gaze index (was 12.11) and fixation turnover trend (was 12.13): demoted by amendment 17, because both would move with blink rate on an offset that 14.9 has not measured (F-041, A25); they return, if at all, behind a committed 14.9 number.
- Thermal-drift proxies (was 13.9): demoted on the owner's priority, since no browser thermal API exists and the proxy would be a device-scoped note; 12.15's columns keep the door open.
- Local session history and trends (was 14.7) and the installable offline demo (was 14.8): demoted on the owner's priority until 14.0a's session state and 11.8a's pinned build exist, because history without a common ruler and an offline cache of an unpinned instrument both demo a stale claim.
- Early abort of a stepped run once its inexact-landing fraction is certain to refuse, so a ten-minute variable-rate clip is refused in seconds rather than minutes: an optimisation of 10.14's rule, not a change to it.
