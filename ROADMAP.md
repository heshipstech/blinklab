# ROADMAP.md

The full increment ladder. One row is one pull request with green continuous integration; the working branch is reused between rows and reset to main after each merge. If a row looks like it needs more than two hours, split it here into lettered sub-rows before starting.

## Amendments to the master plan

Accepted on 2026-07-28, before any code:

1. Increment 1.1's automated check was replaced by manual steps in `test/MANUAL.md`. Playwright (the browser testing tool) is introduced at 5.5 as planned, and a headless browser cannot click a real camera permission prompt anyway.
2. Increment 5.4 was split into 5.4a and 5.4b. The gaze target is reliable quadrant classification with the head reasonably still, not a precise gaze point. The head pose gate from 3.8 marks gaze invalid when the head moves too much.
3. Video upload mode moved from 8.1 to 7.0, so dataset videos run through the same TypeScript pipeline as the live camera. Python only loads the resulting CSV files and does statistics. Increment 7.3 is a go or no-go gate on dataset licensing.
4. Accepted 2026-07-31: increment 3.7 delivers a verification of tilt invariance instead of a tilt correction. The aperture is measured as distances between landmarks, and rotations cannot change distances, so there is nothing to correct. The synthetic tests at 0, 15 and 30 degrees prove it, and a counterfactual test shows the cos(roll) shrinkage a naive vertical measurement would have suffered.
5. Accepted 2026-08-06: the long closure detector (6.2) and its alert (6.3) moved off the blink threshold onto a dedicated shut line at 40 percent of the personal baseline. The owner's face proved the need: naturally low eyelids reading at the bottom of the screen sat below the blink line for five seconds while fully awake, and the literature's 20 percent P80 line proved unreachable because the instrument reads fully shut eyes as about a third of baseline, not zero. The 40 percent line is the measured midpoint between the shut floor and the reading droop. The band between the blink line and the shut line is a partial droop, deliberately neither event. PERCLOS shares the unreachable-line problem and has its own follow-up issue.
6. Accepted 2026-08-06: PERCLOS (6.1) adopts the same measured shut line as the long closure detector, aliased from one constant. Its original 20 percent P80 line was proven unreachable on this instrument (0.0 percent through a witnessed 12.9 second closure), so PERCLOS is now an instrument-adjusted convention, documented as such rather than presented as literal literature P80. The deliberate cost: the disagreement between PERCLOS and the long closure detector was the tripwire that exposed amendment 5's bug, and aligning them removes that tripwire; the regression tests and manual items that replaced it are named in the fix.

7. Accepted 2026-08-08: increment 7.3's gate returned NO. No public dataset clears all four of the project's requirements at once (face video, a drowsiness label, per-clip subject identity, and a licence a solo unaffiliated founder may rely on for a public MIT repository). The openly licensed drowsiness data is physiological, still-image or synthetic; the video corpora with real sleepiness labels are behind signed agreements, non-commercial clauses, or no licence at all. Full evidence in DATASETS.md. Rows 7.4 to 7.7 are therefore held for replanning, and split into two tracks. Track A needs no permission from anyone: validate blink DETECTION against the GPL3 blink-annotated benchmark set, which has ground-truth blink intervals and no gate. Track B, a drowsiness classifier on UTA-RLDD, is defensible but not clean, because that dataset carries no licence at all; it is a judgement call for the maintainer and is recorded as a recommendation rather than a decision.

8. Accepted 2026-08-10: rows 7.5 and 7.6 are HELD, not done, and not achievable as written. They assume a dataset large enough to train a classifier and hold out subjects. The only dataset this project has permission for and can measure is DROZY, which yields 20 sessions from 13 subjects once the 25 fps floor removes 16, and those 16 are systematically the sleepier ones: they average KSS 6.38 against 4.60, and every rating of 9 is among them. No feature survived correction against the sleepiness ratings. Training a classifier on that and reporting a leave one subject out score would produce a number that is mostly the noise of whichever subject was held out, which is the exact failure this project spent two days climbing out of. What would unblock them: more recordings carrying real sleepiness labels, at 25 frames per second or better, from more people. Row 7.7 is delivered inside the DROZY analysis, which shuffles the ratings 1000 times with a fixed seed. Rows 7.8 and 7.9 remain achievable and are unaffected.

9. Accepted 2026-08-09: DROZY is unblocked too. Professor Jacques Verly, senior author of the DROZY database, granted written permission on 8 August 2026 for the use this project described: computing per-second numeric features locally and publishing those numbers and the resulting evaluation metrics. His reply asked about a connection with MIT, having read "MIT licensed" as the institution rather than the software licence; the owner corrected that in writing on 9 August, which matters because term 4's non-commercial clause bit this project precisely BECAUSE the MIT licence grants downstream commercial use. The condition is citation of the database and the WACV 2016 paper wherever results appear. DROZY carries real Karolinska Sleepiness Scale ratings, which is what rows 7.5 to 7.7 need and what no other permitted dataset has, so those rows are unblocked. Full record in DATASETS.md. The safeguards are unchanged: numbers only, never a frame.

10. Accepted 2026-08-08: Track B is unblocked. Professor Vassilis Athitsos, senior author of UTA-RLDD, granted written permission on 8 August 2026 for exactly the use this project proposed: computing per-second numeric features locally and publishing those numbers and the resulting evaluation metrics. The dataset's absence of a licence, which was amendment 7's reason for holding rows 7.4 to 7.7, is therefore resolved for this use. Track A continues first because it is further along and needs nobody's permission. The safeguards recorded in DATASETS.md are not relaxed by the permission: numbers only, never a frame, pseudonymous subject identifiers, source video deleted once features are computed, and the CVPR Workshops 2019 paper cited. The reason they stand is that participants recorded themselves and their consent terms are published nowhere, so no author can grant rights over a face beyond what its owner allowed.

11. Accepted 2026-08-16: rows 7.10 to 7.12 are added, a six-person validation round on live webcams. The reason they are not on the original ladder is that the ladder assumed validation meant a corpus, and a corpus is exactly what the licensing gate at 7.3 could not deliver. Meanwhile every live-camera claim this project makes rests on ONE face through ONE camera, the owner's, because Eyeblink8 and DROZY are both recorded clips at fixed rates rather than webcams in real rooms with real lighting. That is the largest untested claim in the repository and no held row unblocks it: rows 7.5 and 7.6 want labelled sleepiness data, and this round wants six people and a scripted session. It also blocks 7.9 honestly, since "limitations stated plainly" cannot be written while the biggest limitation is unmeasured. The round is not a classifier, publishes no recall figure, and is pre-registered in `docs/validation-plan.md` before any file exists, for the same reason the DROZY plan was.

12. Accepted 2026-08-18: issue #178 is reopened off the declined list and reconciled without changing any behaviour, and the reason it may be reopened is one that did not exist on 15 August. It was declined because settling it needed a corpus run or an argument; the blink-trace harness built on 17 August for the sampling-rate question runs one closure past the real detector with every variable held still, so both sides of the contradiction can be reproduced in the same framework in milliseconds. The finding is that `MAX_BLINK_DURATION_MS` does two non-overlapping jobs and which one it is doing depends on `baseline_over_resting`. The constant is NOT moved: that is step 4 of the issue's own suggested order and it is the owner's decision. Written up in `docs/max-blink-duration.txt`. The same increment adds the gradual-descent case closed issue #126 asked for and issue #115 filed, asserted as what today's code does.

13. Accepted 2026-09-04: the Phase 9 line "pupil diameter estimation and a light response experiment using screen brightness" is taken up as rows 9.2 to 9.4. The iris ruler the aperture already uses (11.7 mm across the iris) doubles as a size reference for the pupil, so a pupil diameter in millimetres is reachable without any new calibration; the honest question the plan flagged is whether a webcam can segment the pupil from the iris at all, which is why 9.2 (the pure estimator) refuses rather than guesses when the contrast, the shape, or the plausibility is not there, and 9.4 (the light-response experiment) is pre-registered before it runs, the same discipline as DROZY and UTA-RLDD. Rows 9.2 and 9.3 build the capability and are ordinary test-driven increments (no claim, no pre-registration); 9.4 makes a claim and gets its plan first.

14. Accepted 2026-09-04: the Phase 9 line "a small learned model to replace one heuristic, with the heuristic kept as the baseline it must beat" is taken up as row 9.1, against the demo alertness score (`src/core/score.ts`). It can be taken up now because the UTA-RLDD run (7.5/7.6) produced both a labelled dataset the score can be tested against and a validated learned model to be the contender, and the README now states in the open that the demo score "has never been shown to correspond to anyone's actual sleepiness" — a claim row 9.1 tests rather than leaves standing. Pre-registered in `docs/alertness-score-plan.md` before the comparison code exists, predict-then-verify. The work is analysis-only; any change to `src/core/score.ts` is a later increment gated on the plan's Bar 2.

15. Accepted 2026-09-04: the light-response experiment (row 9.4) gets an in-app stimulus screen, so the maintainer records by pressing start and watching the screen rather than toggling monitor brightness by hand on a stopwatch. The pre-registered plan (`docs/pupil-light-plan.md`) already permits "a minimal in-app stimulus screen" as the mechanism, so this adds no claim and needs no new pre-registration; it is an instrument, like 9.2/9.3. Being an instrument that straddles the core/io fence, it is split the same way 9.3 was: 9.4a is the pure schedule (which phase the screen shows at each moment, and the export rows that log it), 9.4b is the fullscreen screen and the wiring. Row 9.4 stays the experiment itself — the run on the maintainer's camera and the analysis against the committed prediction — and once 9.4b lands it awaits only that camera.

16. Accepted 2026-09-05: the owner decided the browser prototype CONTINUES as the project's working instrument, and Phases 10 through 14 below are the new era's ladder. The goals: raise the accuracy of what is already measured to the most this class of hardware can deliver, add new measurements toward drowsiness and attention, and grow the demo surface — the prototype is the thing that can be shown, and the record it builds is part of the point. The ladder was assembled by proposing candidates from seven lenses (existing-signal accuracy, new drowsiness measurements, attention, capture and devices, the demo surface, performance, and the evidence base itself) and subjecting every candidate to two independent adversarial reviews — one against webcam physics and what the code actually contains, one against this repository's evidence standards — with two refutations killing a candidate and surviving revisions folded in. The scrutiny corrected the goals, and the corrections are accepted as part of this amendment:
    - Blink recall on Eyeblink8 is near its measured ceiling: ~49 of the 67 misses are flat on both landmark signals (docs/iris-occlusion.txt), so the reachable headroom there is about five blinks, not the fifteen points to the literature's 98% end. Eyeblink8 is henceforth a REGRESSION harness only; every future accuracy claim graduates on the never-scored GPL3 sibling corpora (Phase 11) or a pre-registered live protocol.
    - "Microsleep" is EEG-defined and cannot be validated with any data this project may use, now or in this era. The vocabulary is capped at "microsleep-RANGE closure" (12.6); the phrase "microsleep detection" is refused in UI and docs alike.
    - "Cognitive state" has no webcam-honest meter: the one channel the literature ties to load, the pupil, is measured dead at this optics (docs/pupil-light-result.txt). The era's honest versions are demonstrated task effects (read-vs-rest blink suppression, 14.11), an in-browser vigilance task (14.14), and permanently-labelled demo-only trends. Gaze events never wear the word "attention" without the unvalidated framing the demo notice set the pattern for.
    - Capability is a property of a SETUP, not a device class: the dry run measured a phone front camera out-resolving the DSLR rig at default framing (44.6 px vs 36.9 px iris). The ladder the era builds (13.6) is measured per setup — delivered rate, iris pixels, light — and device class is only the label the export already records.
    - The UTA-RLDD labels have been read twice and are read exactly ONCE more: one batched, pre-registered v2 read (12.18) carries every new signal family together. Per-feature dips into the same 148 videos are refused; that drip is how evidentiary freshness dies.
    - WebGPU is not in the vendored API (the delegate is CPU or GPU-as-WebGL2, nothing else), and optimizing the 42 µs core chain against a ~30 ms inference budget is performance theater. The real levers are asking cameras for 60 fps for the first time (13.2) and not running inference twice on the same photograph (13.8).
      Era rules, holding for every row below: instrument rows land before their claim rows; every claim row commits its prediction to docs/ before any data is read; a row bigger than one pull request splits into lettered sub-rows when taken up, as 9.3 and 9.4 did.

17. Accepted 2026-09-06: the owner asked for the era's roadmap to be re-worked from the September audit (`docs/audit/2026-09-06-audit-report.md`; 154 confirmed findings, 1 critical, 16 high, each tested by two independent skeptics) against six briefs: (1) improve what exists; (2) finish what is partial; (3) add, expand or correct the measurements, metrics and eye signals; (4) fix every confirmed defect, mismatch and inconsistency; (5) take up everything else within the audit's scope that pushes the software toward its maximum; (6) re-do the tests and measurements on public data and volunteers across devices so that validation is complete and multi-layered. Phases 10 to 14 below are re-ordered and extended, Phase 15 is new, and every confirmed finding maps to a row here (its ladder item named in brackets) or to a dated decline in `REMEDIATION-2026-09.md`, whose stages A to E remain the fix ORDER while this table holds the STATUS. This amendment is itself ladder item D12 [D12; F-041, F-072, G-Stepped-8]. The draft was read by two adversarial reviewers, one for traceability against every finding and one for feasibility against the code and webcam physics, and their 45 corrections are folded in. Where the briefs land: (1) 10.11, 10.12, 10.13, 10.8, 11.6a-c, 12.0, 12.8a, 13.8, 13.10, 14.0, 14.0b, 14.9a-b; (2) 10.5, 10.6, 10.8, 10.9, 11.7, 11.8, 11.9 (the written, unrun assessment pilot), 12.2, 12.3, 13.12, 14.9; (3) 10.16, 10.17, every Phase 12 row, 14.9, 14.10 to 14.14, behind a gate that needs the owner's cameras for one dim-room session (10.12a) and one A/B live session (13.8c); (4) Phase 10 entire, 14.0, 14.0b, 14.9a; (5) 10.1c to 10.1g, 10.2b, 11.8a, 13.0, 13.5, 14.0c; (6) 11.3, 11.4, 11.6, 11.7, 13.3 and Phase 15. The audit's corrections are accepted as part of this amendment:
    - The camera path measures processing ticks, not delivered photographs. Peak closing velocity and the amplitude-over-velocity ratio in every live export scale with display refresh over camera delivery (up to 4× on a 120 Hz display), the 25 fps refusal reads the tick rate and passes 15-24 fps cameras behind a 60 Hz display, the blink rate divides by elapsed rather than observed time, and the ruler's sample floors count ticks. Row 13.8 is therefore a CORRECTNESS row, and the "42 µs core chain" sentence of amendment 16 is withdrawn: the per-tick cost is quadratic in tick rate, about 5 ms at 120 Hz with a still gaze. New rows 10.12a-c fix the gate, the rate and the floors on the evidence rate; 13.8a-c drive `processFrame` from `requestVideoFrameCallback` with a pre-registered velocity prediction rather than an equivalence expectation. Both precede every Phase 12 signal row.
    - The frame stepper fabricated frames on variable-rate clips: a 200-frame 20 fps file measured as 399 frames at 40 fps and passed the refusal. Row 10.14 (landed 6 September 2026, `docs/stepper-honesty.txt`) makes an inexact landing a counted refusal and writes the stepping witness into every stepped export; 10.14b gives UTA-RLDD and DROZY the container cross-check Eyeblink8 already has; 10.14c lowers the calibration probe below half a period for rates above 60, where a probe landing on every other frame would calibrate at twice the period and every target would land exactly on a real frame, invisible to the inexact-landing rule. They precede 10.8a, every Phase 11 read and 12.18, whose v1-parity gate becomes conditional on the container coverage check so a corrected stepper cannot be read as a regression.
    - The regression run at HEAD (10.8a) runs BEFORE any change on the corpus-rule list and scores the stepper and guided-line predictions written against the detector as it stands; this supersedes the ladder's "D10 depends on A2", because the stepped path does not read the frame driver, so the anchor 10.8a moves is the anchor 13.8b's parity check reads. Rows that can move the Eyeblink8 count say so and carry a committed prediction: 10.8, 10.11, 10.12c, 10.13b, 12.0a, 12.6c, 12.8a and 12.3's fusion decision, if any.
    - The instrument has one personal ruler and does not use it. The guided line sits 30-50% above the passive line, its closed median is discarded while PERCLOS and long closures keep the owner's 0.4, the long-closure reducer has no hysteresis (one 6 s closure fired 3 and 2 events on two iPhones), and no export says which line produced a session's numbers. New rows 10.11 (hysteresis on the arming side, so durations do not move), 10.13a (the line travels with the number), 11.6a-c (guided calibration as a procedure, with a settle window, a verification blink, a soundness ceiling and audible cues) and 12.0a-b (one personal ruler for blinks AND closures) land before round II (11.7), which cannot otherwise read a guided session. Row 10.9's device gap is first tested as arithmetic on the existing columns under the two line placements.
    - Rows 13.2 and 13.4 move to the front of the era: the app has never asked a camera for a frame rate, and whether live accuracy is capped at 30 fps on every owned camera is decided in half a day. The cued blink-and-closure protocol (11.0a/b) replaces the two-marks protocol as the ground truth for 10.9, 11.6, 13.3, 14.5, 14.13 and Phase 15, because the round showed the marks contaminated (3 of 6 volunteers pressed Mark more than twice).
    - Gaze instrument before gaze signal: 14.9a-b (a refusing solver that carries pose, viewport and camera context; blinks excluded from the whole gaze chain; one definition of on-screen) and 14.9 move ahead of every gaze signal; 12.11 and 12.13 move to ideas because both would move with blink rate on an offset 14.9 has not measured (F-041, A25), and 12.12 waits for 14.9's error file; 12.18's look-away and gaze riders are gated on a committed 14.9 number. MODEL_CARD's "reliable near the centre, degrades at the corners" becomes "not measured" until 14.9 produces a file.
    - The blendshape witness (12.2, 12.3) runs on the regression harness before the sibling corpora are read once with whatever detector it leaves. The vendored blendshape head is a landmark-fed network over 146 mesh points, not a pixel head: the witness tests whether mesh points the aperture ignores carry closure the eyelid rim does not, and it is the only vendored route past the 49-miss ceiling; the pre-registration states that prior.
    - UTA-RLDD's labels stay read exactly ONCE more (amendment 16): the shape-feature ablation and the within-subject null are pre-registered sections of 12.18, and 12.10a's blink-excluded PERCLOS is scored on DROZY and as a 12.18 rider, never as a separate read. The sibling corpora are read once, on the final pinned build, after every corpus-rule row is ticked; the former row 11.5 is deleted because 10.8's delta is scored on Eyeblink8 by its own committed prediction.
    - Row 10.3 is DECIDED: the three DROZY shape correlations are retired rather than re-measured; drozyGuard gains its rule and a per-feature source map; the sample is re-measured only if 12.18's v2 read moves a shape feature.
    - The headline number becomes re-derivable (10.1d) and the ratchet watches the whole chain (10.1g): the anchor run's blink logs and derived tables are committed under docs/evidence/, the evaluator gains tests and a non-zero exit on a partial corpus, and the ratchet watches the scorer, the stepper, the runner, the engine and the lockfile. Row 10.1c revives the mutation runner in CI with a literal boundary probe per refusal constant, six of which survived mutation with the whole suite green. Row 10.10c puts an interval on every published number; none carried one.
    - New row 13.0 measures engine agreement on one fixed clip before 13.1, because every published detection number is WebKit-only and Phase 13's phone rows would otherwise confound engine with device; "browser binary" in the results block becomes "WebKit binary".
    - New rows 14.0a-e give the session an `ended` state; Stop currently disables export and loses the session, a superseded camera start leaves a live track the page cannot stop, and the light overlay traps a phone user with no touch exit (14.0b). Both precede any pilot session. Row 11.8 gains row zero 11.8a: a pinned build at a stable path named in every frozen plan, with an intake that refuses a differing `app_commit`.
    - Demoted to ideas, on the owner's priority (the six briefs name accuracy, completeness and validation first) and not on the audit's F-104, which both skeptics killed: 13.9, 14.7, 14.8. 13.11 is KEPT, because 13.12's arming condition needs its floor. Row 14.10 runs on raw offsets or with the smoothing filter's lag pre-registered. Row 13.6 splits into 13.6a (rate and iris-pixel verdicts, now) and 13.6b (light, after 12.16). Tablets and the Android phone appear as volunteer-supplied or ABSENT until measured. Row 12.6 gains 12.6b (closure completeness) and 12.6c (droop-then-blink timing); 12.8 is preceded by 12.8a (a quadratic peak velocity where the descent holds three samples, after 13.8); 12.10 gains 12.10a; 12.5 is preceded by 10.17 (the pose matrix is read column-major, not row-major), a Phase 10 correctness fix rather than a gated signal row.
    - Documentation is a load-bearing cost: 62% of recent file touches are prose and every false sentence the audit found lives in the hand-synchronised layer. Rows 10.0a-b are the truth pass, closed with positive pins; thereafter any number in more than one document is generated or appears once with links, and the per-row ritual is the PR body plus the one document the row's Check names. Line 3 of this file now says what is practised (one PR per row, the branch reused).
    - Phase 15 is the validation the owner asked for, in six layers: pinned fixtures and an injected face in CI; the public corpora once each on the final pinned build; repeatability across runs, engines, devices and calibrations; the cued live protocol on volunteers across device classes at 30 and 60 fps with n set by the interval it must reach; the rounds on the pinned build; and a generated validation report from which the card and the README are regenerated, behind a claim gate. Each layer names its prerequisites; none starts before every Phase 10 row except the conditional 10.7c is ticked, because re-validating an artefact validates the artefact.
      Era rules, unchanged and now enforced by order: instrument rows land before their claim rows; every claim row commits its prediction to docs/ before any data is read; a row bigger than one pull request splits into lettered sub-rows; no Phase 12 signal row starts before 10.11, 10.12a-c, 10.13a-b, 10.14, 13.2 and 13.8b are ticked; every number published from here carries an interval and its device scope; volunteers stay pseudonymous and numbers-only, raw session files never enter the repository, and no frame ever does.

## Phase 0. Foundations (no eye code at all)

- [x] 0.1 The four working documents, plus LICENSE, .gitignore, README skeleton. Done when the repo has docs and nothing else.
- [x] 0.2 Vite plus TypeScript project that prints "blinklab" to the page. Done when `npm run dev` shows a page locally. Check: `npm run build` succeeds.
- [x] 0.3 ESLint, Prettier, strict tsconfig, and the rule that `core` cannot import `io` or `ui`. Check: lint and typecheck scripts pass.
- [x] 0.4 Vitest with one real test of a trivial pure function in `core`. Check: `npm test` shows 1 passing test.
- [x] 0.5 GitHub Actions CI (continuous integration) running install, lint, typecheck, test, build on every pull request. Check: green check on a pull request.
- [x] 0.6 Branch protection on `main`, pull request template, two issue templates. Done when direct pushes to `main` are blocked.
- [x] 0.7 GitHub Pages deployment workflow. Done when a public URL shows the page.
- [x] 0.8 ADR-0001 recording the stack decision and its alternatives.

## Phase 1. Pixels

- [x] 1.1 Request webcam permission, show the live video element. Done when your face appears. Check: manual steps in `test/MANUAL.md` (amended, see above).
- [x] 1.2 Permission denied and no camera states with readable messages. Check: unit test on the state machine.
- [x] 1.3 Measured frames per second, displayed. Check: unit test of the fps calculator with fake timestamps.
- [x] 1.4 Draw the video into a canvas instead of showing the video element. Check: visual check documented in `test/MANUAL.md`.
- [x] 1.5 Camera device picker when more than one camera exists. Check: unit test on the device list mapper.
- [x] 1.6 Mirror toggle and resolution readout. Check: unit test on the transform matrix.

## Phase 2. Landmarks

- [x] 2.1 Load FaceLandmarker, log face detected true or false. Check: unit test on the face present predicate using a fixture.
- [x] 2.2 Draw all 478 landmarks as dots on the canvas. Check: fixture based test of the point projector.
- [x] 2.3 Move landmark index groups into `core/constants.ts`, draw only the eye region. Check: index sets do not overlap and are in range.
- [x] 2.4 Draw the iris landmarks in a second colour. Check: iris indices form a closed ring.
- [x] 2.5 Guard against a model returning 468 landmarks instead of 478. Check: unit test with a 468 point fixture.
- [x] 2.6 Display per frame inference time in milliseconds. Check: test on the timing helper.
- [x] 2.7 Record 300 frames of landmarks to a JSON fixture file for tests. Done when `test/fixtures/session-01.json` exists and is used by a test.

## Phase 3. First real measurement

- [x] 3.1 Eye aspect ratio (EAR) per eye, displayed. Check: unit test with hand built synthetic eye shapes.
- [x] 3.2 Rolling sparkline of EAR over the last 10 seconds. Check: unit test of the ring buffer.
- [x] 3.3 Synthetic face generator in `test/fixtures/syntheticFace.ts`, landmark sets at known distances and angles. Check: whole new test file.
- [x] 3.4 Iris width normalisation, pixels to millimetres via the fixed 11.7 mm human iris diameter. Check: mm output stable while px output varies across synthetic distances.
- [x] 3.5 Lean in and lean out validation, coefficient of variation for px and mm. Check: CV(mm) < CV(px) on synthetic data.
- [x] 3.6 Head pose estimate: pitch, yaw, roll. Check: three separate synthetic tests, one per axis.
- [x] 3.7 Tilt invariance of aperture, verified instead of corrected (amended, see above). Check: synthetic test at 0, 15, 30 degrees roll, plus the cos(roll) counterfactual.
- [x] 3.8 Reject frames outside a head pose range, mark them invalid instead of guessing. Check: unit test of the validity gate.

## Phase 4. Blinks

- [x] 4.1 Fixed threshold blink detector, blink counter. Check: unit test on a synthetic aperture time series.
- [x] 4.2 Personal baseline learned over 30 seconds, visible countdown. Check: baseline rises but never falls.
- [x] 4.3 Blink duration in milliseconds. Check: synthetic series with known durations.
- [x] 4.4 Blink rate per minute, rolling window. Check: ring buffer and window edge tests.
- [x] 4.5 Blink closing velocity and the amplitude over velocity ratio. Check: unit tests on velocity extraction.
- [x] 4.6 Frame rate honesty gate, null instead of a number when fps is too low. Check: null, not zero, below threshold.
- [x] 4.7 Squint versus blink separation. Check: synthetic squint plateau counts no blinks.
- [x] 4.8 Blink event log with timestamps in a side panel. Check: test on the event reducer.

## Phase 5. Gaze and attention

- [x] 5.1 Iris centre offset relative to the eye corners, per eye. Check: synthetic tests at known offsets.
- [x] 5.2 Screen quadrant classification, four regions. Check: test on labelled fixture frames.
- [x] 5.3 On screen versus off screen classification. Check: threshold test.
- [x] 5.4a Calibration capture screen: nine points shown, samples collected and stored (amended, split from 5.4). Check: test on the sample collector.
- [x] 5.4b Calibration solver producing a stored profile. Done when quadrant classification is reliably correct with the head reasonably still (amended target). Check: solver test with synthetic points.
- [x] 5.5 Introduce Playwright, first end to end test of the calibration flow. Check: headless test passes in CI.
- [x] 5.6 Gaze point smoothing filter, raw and smoothed traces both drawn. Check: filter preserves step response within tolerance.
- [x] 5.7 Fixation and saccade separation using a dispersion threshold algorithm (I-DT). Check: synthetic scanpath of known structure.
- [x] 5.8 Fixation duration statistics panel. Check: aggregation tests.
- [x] 5.9 Gaze heatmap over a static image. Check: test on the accumulation grid.
- [x] 5.10 Scanpath replay with a time slider. Check: test on the replay index lookup.

## Phase 6. Rolling state and the demo score

- [x] 6.1 PERCLOS (percentage of eye closure) over a rolling 60 second window. Check: synthetic series with known closure fraction.
- [x] 6.2 Long closure detector, eyes shut beyond a threshold triggers an event. Check: boundary case included.
- [x] 6.3 Alert with debounce so it cannot fire repeatedly. Check: repeated triggers within the debounce window.
- [x] 6.4 Feature vector assembled into one typed FeatureRecord per second. Check: schema test.
- [x] 6.5 Explainable 0 to 100 demo score using a documented weighted heuristic. Check: contribution numbers sum to the score.
- [x] 6.6 Contribution panel showing the top three drivers of the score. Check: snapshot test.
- [x] 6.7 Session recorder, export CSV (comma separated values) to a local download. Check: serialiser tests including comma and header edge cases.
- [x] 6.8 KSS (Karolinska Sleepiness Scale, 1 to 9) self report before and after a session, written into the CSV. Check: metadata writer test.
- [x] 6.9 Prominent, permanent "demo, not a safety or medical device" notice in UI and README. Check: Playwright assertion.

## Phase 7. Honest evaluation (the Python track)

- [x] 7.0 Video file upload mode so recorded clips run through the same TypeScript pipeline (amended, moved from 8.1). Check: end to end test with a sample clip.
- [x] 7.1 `/analysis` folder, pinned Python environment, ruff, pytest, second CI job. Check: Python CI job green with one real test.
- [x] 7.2 CSV loader plus a first plot of one recorded session. Check: loader tests on malformed CSV.
- [x] 7.3 DATASETS.md, choose one openly licensed public dataset, record licence and access terms. Go or no-go gate: if no suitable openly licensed dataset exists, replan 7.4 to 7.7 before starting them (amended). Gate returned NO, see amendment 7.
- [x] 7.4 Batch runner computing features over dataset videos into feature CSVs, via the 7.0 upload pipeline (amended). Check: two frame sample test. Delivered as a stepped measurement mode, which fixes #145: the clip waits for the instrument on every frame, so the output is a property of the file rather than of the machine.
- [x] 7.5 Baseline classifier with a clearly stated train and test split, DONE 3 September 2026. Check: reproducibility with a fixed seed — met, `analysis/blinklab/rldd.py` is a deterministic numpy multinomial logistic regression (zero-init, fixed step and count, standardised and median-imputed inside each fold) that reproduces bit for bit, with the label-shuffle control seeded from one recorded value. Pre-registered in `docs/uta-rldd-plan.md`, run on the full dataset (54 usable subjects, 148 videos) by `analysis/tools/analyse_rldd.py`, result in `docs/uta-rldd-result.txt`. The fold-1 pilot was a null at DROZY's scale; the full sample DETECTS drowsiness (three-class balanced accuracy 0.498, binary alert-vs-drowsy 0.732, both past the shuffle control at p 0.001), which is the surprising outcome the plan named and it is recorded as such with its caveats.
- [x] 7.6 Leave one subject out evaluation, per subject scores reported, DONE 3 September 2026. Check: no subject in both splits — met and verified empirically on the 148-video matrix (54 folds, every video held out exactly once, none in both splits). Same pre-registered plan, `docs/uta-rldd-plan.md`: per-subject balanced accuracies reported for all 54 subjects, pooled balanced accuracy against the 1/3 floor, and the 1000x label-shuffle control whose null sits at chance (0.310) — the proof identity leakage was blocked. Both tasks clear the pre-registered two-bar decision rule. Recorded in `docs/uta-rldd-result.txt`.
- [x] 7.7 Negative control: shuffle labels, confirm collapse to chance. Delivered inside the DROZY analysis: the ratings are shuffled 1000 times with a fixed seed and the strongest chance correlation is printed beside the observed one. Check: test asserting the collapse.
- [x] 7.8 Latency measurement, DONE 21 August 2026: time from eye closure to alert is deterministic, 500 ms plus at most one frame period (520.0 ms at 25 Hz, 533.3 at 30, 516.7 at 60), proven sensitive by mutation; the core chain costs about 42 microseconds per frame on one measured machine, asserted under a 2 ms ceiling so CI noise cannot redden it while per-frame growth still would. The debounce regime is stated beside it: within 5 s of a firing a new closure is counted, not told. Record: docs/latency.txt. Check: timing test with tolerance, test/core/alertLatency.test.ts.
- [x] 7.9 Results section in README generated from analysis output, DONE 21 August 2026: a "Results at a glance" block between markers, built by tools/resultsBlock.mjs from docs/eyeblink8-result.txt, docs/drozy-result.txt and docs/validation-round.txt, with the limitations stated plainly in the block and the DROZY citation required by its permission carried in it, refused if missing. Regenerate with npm run results:write. Check: a test regenerates the block and fails when the committed README drifts from it, and no TODO remains in the block. AMENDED 4 September 2026: the sleepiness bullet now carries the UTA-RLDD detection alongside the DROZY null, generated from docs/uta-rldd-result.txt via parseUtaResult, with both required citations carried and a verdict guard that reddens the build if the result ever stops reading "detecting drowsiness" — the mirror of the DROZY null guard.
- [x] 7.10 The six-person validation round, pre-registered and read (amendment 11). `docs/validation-plan.md` written before any session file exists, and `analysis/blinklab/validation.py` pairing a folder of exports with a named refusal for every way it can be handed something untrustworthy, including the camera blink log that `blink_log.py` is correct to refuse. No check is computed here, deliberately: a reader and its checks arriving together is how a refusal softens into a default. Check: a mutation per refusal turns its own test red, and a session whose blink log does not exist is KEPT, because that is a result and not an accident.
- [x] 7.11 The checks and the published table: blinks between the two marks with their boundary counts, the long closure, the baseline's readiness and drift, face-detected share, iris width per device with its measurement frame, and declared against processing frame rate. `validation_checks.py` judges, `tools/validation_report.py` prints, and the report answers the plan's three failure criteria out loud rather than leaving a reader to count rows. Check: a fixture per verdict, a mutation per rule, and a refused participant gets a named row plus a non-zero exit rather than a silence. NOT YET RUN ON A REAL FILE, which is 7.12.
- [x] 7.12 The dry run, DONE 16 and 17 August. FIVE sessions on three devices, at the owner's suggestion of three rather than two: iPhone 14 Pro Max twice, MacBook Air twice, and the Sony A7 IV through a Cam Link 4K. The second MacBook run is the test that separates the processing rate from the device, repeating the first at the same rate with a baseline that lands correctly. All eight files read with no refusals and the pairing coped with device names in the filenames. The drift threshold is SET at 15 percent from the measured 0.0, 0.0, 0.0 and 5.0, recorded in `docs/validation-plan.md` before any volunteer file existed. Full write-up in `docs/validation-dry-run.txt`. It found two things no fixture could: a baseline 41 percent longer than its own session's median aperture passing BOTH halves of the pre-registered baseline check, which produced the plan's second correction and a fifth check; and the instrument losing deliberate blinks at the rate a four core machine produces, 3 of 10 on the phone and 1 of 10 on the laptop, each miss a hole a whole number of blinks wide in a metronome cadence, with the 25 fps gate never firing. It also refuted one of its own first conclusions, that blink duration moves with the processing rate, which the fifth session ordered the wrong way. Check: the threshold was recorded before any volunteer file was opened, and the session that disagreed with the protocol was re-run rather than deleted.

## Phase 8. Making it public and durable

- [ ] 8.1 (moved to 7.0, see amendments)
- [x] 8.2 ARCHITECTURE.md with a module diagram and the data flow. Done when a newcomer understands it in 5 minutes.
- [x] 8.3 CHANGELOG.md written 15 August covering all seven existing releases plus an Unreleased section. Tags exist to v0.7.0. The v0.8.0 tag is deliberately not cut: 8.8 is declined rather than done, and a version number is not worth a ceremony.
- [x] 8.4 MODEL_CARD.md: what it measures, what it does not, where it fails, who it fails for.
- [x] 8.5 Dependabot config, alerts on and reporting 0, SECURITY.md with a real threat model. Verified against the API 15 August.
- [x] 8.6 Coverage floor on src/core as its own CI step: statements 98, branches 95, functions 100, lines 98, against a measured 98.61 / 95.52 / 100 / 98.57. Far above the 70 this row asked for. Proven able to fail.
- [x] 8.7 Bundle size ceiling of 240 kB checked after every build, against 217.6 kB actual. Written against the one commit that bundles the 3.7 MB model rather than against creep. Inference time stays a locally recorded number, 6 ms, since CI machines have no camera.
- [~] 8.8 DECLINED 15 August. The floor is already met: keyboard operable, focus visible, all text clears WCAG contrast. What remained was polish. **AMENDED 22 August 2026, additively rather than reopened: one clause of that decline was false when it was written.** Five colour pairs failed AA on the committed stylesheet, and the worst of them was `.rate-warning` at 2.80:1 — the processing-rate limitation shipped the very next day, which made "limitations belong in the open" untrue on the page while this row sat closed. The palette is corrected and `tools/contrastGuard.mjs` now computes every pair, on both grounds, and checks that the page uses the legible tokens rather than merely declaring them. The decline itself STANDS: what remained after this really is polish. What did not stand was checking a claim like that by eye.

## Phase 9. Pupillometry, the light response, and the learned-model question

Taken up from the ideas list (now at the end of this file) and tracked as rows, because each had the data and the baseline it needed (amendments 13-15):

- [x] 9.2 Pupil diameter, the pure estimator, DONE (amendment 14). A `core/pupil.ts` that reads a luminance field of the eye region plus the iris centre and radius and returns a pupil diameter in millimetres through the same 11.7 mm iris ruler the aperture uses, or `null` when the contrast, the shape consistency, or the physiological plausibility is not there. No `io`, no camera, no wiring — pure and synthetic-tested. Check: synthetic dark-disc fields at known radii recover the diameter; low contrast, an occluded/partial pupil, an implausible size, and a malformed field each return null. **The tick means the code exists and passes its synthetic Check; it does NOT mean pupillometry works — the real-world 9.4 run resolved the pupil in under 1% of webcam seconds, so the estimator is effectively non-functional on this hardware (docs/pupil-light-result.txt).** (Box left unticked by oversight when the estimator shipped ahead of 9.3a; reconciled 5 September 2026.)
- [x] 9.3a Pupil diameter, the pure pixels-to-field half, DONE 4 September 2026. `core/pupil.ts` gains `luminanceField(rgba, frameWidth, frameHeight, box)`: crop a rectangle of an RGBA frame to a grayscale (Rec. 601) luminance field in [0,1], the pure half of reading the eye off the canvas, refusing a box off the frame or a pixel array that does not match the frame. Check: RGBA converts to luminance, crops correctly, feeds the estimator end to end from raw pixels to a recovered diameter, and refuses the malformed cases. (Split from 9.3 so the camera-free, testable half lands before the io wiring.)
- [x] 9.3b Pupil diameter wired, DONE 4 September 2026. The io layer (`readVideoPixels`) reads the iris-region pixels from an OFFSCREEN, full-resolution, unmirrored copy of the clean video (not the visible 640px canvas, which is downscaled and may carry overlays), a pure `irisSampleRegion` boxes the iris and translates its centre, and `luminanceField` + `pupilDiameterMm` turn that into a `pupilDiameterMm` column in the per-second export (blank, not zero, on refusal) and an "Eyes" readout. Check: the column's presence and its null-is-blank serialization are unit-tested (`csvHeader`/`serializeRecords` against the record's own keys), `irisSampleRegion` is pinned end to end from raw pixels to a recovered diameter, and the coverage floor holds; the pupil number's real-eye behaviour is 9.4's job (a headless clip has no resolvable pupil, so an e2e could only re-assert the header the unit tests already pin). No measurement the detector or score reads changes.
- [x] 9.4a Light-response stimulus, the pure schedule, DONE 4 September 2026 (amendment 15). `src/core/lightSchedule.ts`: the stimulus fixed as timing, not pixels — a 20 s settle the analysis discards, then six cycles of 20 s dark then 20 s bright (260 s total). `lightPhaseAt(elapsedMs)` returns the phase the screen should show at any moment from the stimulus start; `lightScheduleTransitions()` lists every boundary; `lightStimulusMetadataRows(startMs)` (in `sessionMetadata.ts`) writes the schedule and the stimulus's moment-zero into the export in the `timestampMs` clock, so the analysis recovers a row's phase as `lightPhaseAt(row.timestampMs - startMs)`, or writes nothing when there was no stimulus. Nothing paints yet. Check: every phase boundary, the transition list, the agreement between the two, and the exact metadata rows are unit-tested; the loader already reads arbitrary `# key: value` metadata, so no analysis change. (Split from 9.4 at the core/io fence, like 9.3a before 9.3b.)
- [x] 9.4b Light-response stimulus, wired, DONE 4 September 2026 (amendment 15). A "Light response" button (enabled once a live camera session is recording) raises a fullscreen overlay that paints the 9.4a schedule — near-black through settle and dark, near-white through bright — driven by `lightPhaseAt` off `performance.now()`, the clock the per-second records share. The camera keeps recording underneath; at export `lightStimulusMetadataRows` writes the schedule and the stimulus start, so the analysis recovers each row's phase by subtraction. A pure `lightPhaseBackground` makes the colour-per-phase a tested decision. Esc (or leaving fullscreen) ends it; the start stays logged. Check: three unit tests pin the colours and one Playwright test drives the button, asserts the overlay paints the settle phase, and reads the schedule plus start back out of the exported file (the 260s run is not waited out — the phase logic is unit-pinned). The overlay carries no inline `display`, so `hidden` alone controls it.
- [x] 9.4 The light-response experiment, PRE-REGISTERED before it runs — first run INCONCLUSIVE, 4 September 2026. Plan committed in `docs/pupil-light-plan.md` before the instrument (9.3b) was even wired: a screen-brightness stimulus (six 20 s dark/bright cycles), the prediction that the pupil constricts to the bright screen committed in writing first, then measured on the maintainer's camera. The maintainer ran it behind the 9.4b stimulus screen; `analysis/blinklab/light_response.py` + `analysis/tools/pupil_light.py` recover each second's phase from the logged stimulus start, apply the usable-data gate first, and only then compare. Result in `docs/pupil-light-result.txt`: the estimator resolved the pupil in 1 of 239 in-window seconds (0.4%, 0 dark / 1 bright), so the gate did not clear and the verdict is **inconclusive** — the plan's committed most-likely-non-detection branch, recorded as plainly as a detection. Face and pose tracking were flawless, localising the limit to the small (43 px) webcam iris interior; eyeglass reflections are a likely aggravator. The plan invites a closer/no-glasses/darker re-run, appended to the result page; the analysis method is fixed and untouched. Check: the plan was committed before any measurement, the committed tool is tested on synthetic detected/null/inconclusive cases and pins the schedule to the browser's, and the result page carries the prediction beside the outcome.
- [x] 9.1 Does a learned weighting beat the demo alertness heuristic? DONE 4 September 2026. Pre-registered in `docs/alertness-score-plan.md`; an analysis-only head-to-head on the UTA-RLDD usable set (threshold-free alert-vs-drowsy AUC, `analysis/blinklab/alertness.py` + `tools/compare_alertness.py`, two shuffle controls, a two-bar rule). Result in `docs/alertness-score-result.txt`: on 95 alert/drowsy videos from 52 subjects the demo heuristic separates drowsiness at AUC 0.70 (p 0.001, Bar 1 CLEARED — the score the README calls unchecked is above chance across strangers), while the learned model's higher 0.78 does not beat it beyond the shuffle (+0.083, p 0.099, Bar 2 NOT cleared). Verdict: KEEP the heuristic; `src/core/score.ts` is unchanged. The committed prediction was half wrong (Bar 1 right, Bar 2 predicted to hold and did not), recorded as such. Adopting a learned score would have been a separate increment gated on Bar 2, which was not met.

## Phase 10. Pay the ledger, then make the live instrument honest

Nothing here needs a volunteer. It closes published results' own loose ends, installs the guards, and fixes the seams the September audit found under the live camera path and the personal ruler, before any new signal is built on them. Ladder items in `REMEDIATION-2026-09.md` are named in brackets; the ladder holds the reasoning and the fix order, this table holds the status. Rows that can move the Eyeblink8 count say so; they carry a committed prediction and land after 10.8a's regression run, never before it.

- [x] 10.1a Detector-change measurement ratchet, DONE 5 September 2026: docs/eyeblink8-result.txt carries a "Built from commit" anchor (the 2 September awake run), and tools/detectorRatchet.mjs reddens CI when any of 16 named detector sources gains a commit past the anchor without a dated DETECTOR CHANGED caveat naming it — and reddens from the other side when a caveat still reads "not yet re-measured" on a fresh anchor. Check: 12 tests, watched failing first — the verdict truth table both ways, a disk-existence pin so a rename cannot silently drop a file from the watch, and the live repo held coherent. The ratchet found a real staleness in its first hour: the guided blink-line commits (1a6c88f and kin) postdate the anchor, and the honest caveat now stands in the result file until 10.8a's regression run re-measures.
- [x] 10.1b MODEL_CARD's measurement-uncertainty section wired into resultGuard, DONE 5 September 2026: the card states the two measured conditions — precision is preparation-conditioned (the 65 invented are a property of the committed remux; re-encoded derivatives measured 12.4 points lower, one clip's false alarms collapsing 19 to 3 — the planning note's "65 vs 13" conflated this with the re-arm gate's 13 removed and is corrected here from the committed record), and blink duration is device-conditioned (iPhones ~96 ms, Macs 149-166 ms, mechanism open until 10.9). resultGuard's uncertaintySection() holds the section to the result file: the quoted invented count must equal the committed one, and the condition figures are pinned. Check: 5 tests, watched failing first, including the drift case (a re-measure that moves the count reddens the card until it moves too).
- [x] 10.0a1 Truth pass, the page and the card [B1, B3, B6's dating half, B7, B13], DONE 6 September 2026: MODEL_CARD carries the UTA-RLDD alertness result (AUC 0.70, p 0.001, cohort-level, per person unvalidated) and its tested-on table names 54 subjects for the LOSO read and 52 for the alertness read, and tablets as untested; the notice and its six echoes say the model tries to send and this page intercepts it, quoted verbatim into README and MODEL_CARD; "reliable near the centre" becomes "not measured" until 14.9; the cross-engine table is dated and says WebKit (its re-measurement is 13.0); SECURITY.md names a channel that exists. Check: 13 tests, watched failing first — the alertness headline and both subject counts parsed out of their own records and held to the card, the notice quoted verbatim in both documents, the retired gaze claim held out of both, the table's measurement date and commit, and the reporting channel. (10.0a was split into three sub-rows on 6 September 2026 under this file's two-hour rule; no item moved, and 10.0a2 and 10.0a3 carry the rest.)
- [x] 10.0a2 What is kept and what is exported, said in the open [B2, B18], DONE 6 September 2026: README's Privacy section is generated from STORED_ITEMS, names what a CSV contains, and offers a reduced user agent unless the full string is needed; the third-party notice carries the shipped MediaPipe version and the OFL text. Check: every storedData key found in README's Privacy section, from a generator whose committed output the test rebuilds; `lockfileVision().version` quoted in the notice; the OFL preamble and every font family `src/styles.css` imports. Check met by 24 tests, watched failing first, plus one Playwright sequence proving the disclosure and the browser-string checkbox stand in the Session box rather than existing only as tested strings.
- [x] 10.0a3 The generators stop freezing prose [B5], DONE 6 September 2026: the miss mechanism reads as a measured ceiling everywhere the generators print it, every generated sentence a parse or an `assertQuote`, and README's duplicated miss paragraph is dropped. Check: cannotSee.ts regenerated with the ceiling sentence held to docs/iris-occlusion.txt; a test greps the generators for unpinned sentence literals. Check met by 6 tests, watched failing first, and two mutations: a claim stripped of its pins and the limitations bullet returned to a typed literal each reddened their test.
- [ ] 10.0b Truth pass, the trackers and the guards that read them [B14, B15, B17, D13, D14]: resultGuard parses only the FIRST stamp line, gains a "read in full on <date>" stamp and three literal pins (file count, phases complete versus this file, the gate list versus ci.yml); the test count is counted from the runner; CHANGELOG joins the guarded documents and its Unreleased figures point at the generated block; ADR-0005 records the priority pivot and corrects PROJECT.md's four contradicting sentences, and CONTRIBUTING gains the rule that intent or measurement-definition changes get a one-page ADR; STATE.md gains a generated STATUS block, REMEDIATION.md and NEEDS-REVIEW.md are closed at their first line with a pointer to REMEDIATION-2026-09.md, and docs/log.md is retired in writing. Check: a hand-edited count in any of the three documents reddens the build; the STATUS block regenerates byte-identically; ADR-0005 exists and PROJECT.md agrees with amendment 16.
- [x] 10.1c Refusal constants pinned at their boundaries and the mutation runner revived [D2, D16], DONE 6 September 2026: one literal probe per refusal constant in its owning test (24.9/25/25.1; 59.9/60/64.9/65; 14 999/15 000 ms; 99/100 samples; last+2000/2001; 29/30 guided samples), tools/mutationCheck.mjs repaired (RISE entries dropped, these constants added, each entry runs only the test it expects to redden) and run in CI; claimGuard matches a regex family and PROJECT.md's one-word-away restatement is reworded while README's "of ours" scope is stated; exportGuard matches both quote styles and `session-*.json`; a guardsArmed test asserts a sibling test per tools/*Guard|Block|Ratchet.mjs; `npm ci --ignore-scripts` in both workflows. Check: the runner is green on main and red for `MIN_BLINK_FPS = 24` and `= 26`; six of the audit's surviving mutations redden. All 27 entries are caught in about two minutes. Narrowing each entry to its owning test file turned 10 of them from caught into SURVIVED first — nine pose-gate mutations and the blink refractory, whose owning tests also derived their probes — and those two files carry literals now.
- [ ] 10.1d The headline becomes re-derivable [D1, D3, A12]: the anchor run's eight blink logs, the 408-row annotation table and the 65-row false-alarm table committed under docs/evidence/ (numbers only, the class DATASETS.md already rules on); `analysis/tests/test_evaluate_eyeblink8.py` reproduces "341 of 408", 65, 83.6/84.0/83.8 and every per-clip row; the evaluator exits non-zero on a partial corpus, prints its skips into the report body, refuses a clip whose coverage gap or missing `frames_measured` header it cannot explain, and says "not computable" for a glasses split it cannot form; a dated paragraph names which candidate produces the published "+1" on two clips or that it is unresolved. Check: a pytest binds the committed miss CSV to the published miss column (67 rows, 47/67 = 70.1%); a watched-mode log, a >1% coverage gap and a missing header each turn a test red.
- [x] 10.1e Pages waits for CI [D5], DONE 6 September 2026: deploy on `workflow_run` of CI, guarded on success, with `head_sha` passed to checkout and `GITHUB_SHA` set for the provenance stamp. Check: 8 tests, watched failing first, in a new `tools/deployGuard.mjs` that reads both workflow files — the trigger set with no `push` in it, the CI workflow's own name held to the string the deploy watches for, the success condition, the checked-out ref and the stamped `GITHUB_SHA`, and that `vite.config.ts` still reads that variable. The reader refuses a file whose `on:` block it cannot parse rather than reporting no triggers. The live half of the check, that a red push does not publish and that the deployed commit line matches the green run, is observable only after this merges.
- [ ] 10.1f The metadata contract across the language border [D6, B16]: SPEC.md gains the session-metadata table (key, when written, format, which reader consumes it); `test_metadata_contract.py` in the shape of test_csv_contract.py lists every key the five TS writers emit and every literal the Python readers read; keys split into REQUIRED and CONDITIONAL with written presence rules, loader.py refusing a missing REQUIRED key; `records_dropped`, `app_commit` and `protocol` carried on `Session` and every cohort tool printing the distinct commit set; the verdict fixtures generated from the real row builders with 24.96 and 59.96. Check: renaming a key reddens both suites; the SPEC key list equals `declared_keys()`.
- [ ] 10.1g The ratchet watches the whole chain [D4]: DETECTOR_SOURCES gains blink_match.py, blink_log.py, eyeblink8.py, evaluate_eyeblink8.py, measure_corpus.mjs, videoStepper.ts, stepCalibration.ts and package-lock.json, and the sha check reads the caveat block rather than the whole file; `DEFAULT_TOLERANCE_FRAMES` pinned with a 4-matches / 5-does-not pair; modelProvenance pins `@playwright/test` from the lockfile and the engine identifier from measure_corpus.mjs as MODEL_CARD lines, with browser and Playwright versions in the reproduction block; Playwright gets its own Dependabot entry. Check: the ratchet names 10.14's merged commit or the build is red; the tolerance pin reddens on 3 or 6.
- [x] 10.2 Vendored-model provenance joins the ratchet, DONE 5 September 2026: MODEL_CARD gains a "Model provenance" section pinning the model file's sha256, the runtime's lockfile version and integrity, and the landmarker options (blendshapes stated `false` explicitly, the pin 12.2 flips); tools/modelProvenance.mjs recomputes every line from the committed artifacts, and the model file plus src/io/landmarker.ts join the detector ratchet's watch, so a swap without a re-measure or caveat is a red build twice over. The WASM bundle is pinned through the lockfile integrity rather than hashed from disk — public/mediapipe-wasm is generated at build time from the locked package, so the lockfile IS its committed provenance. Check: 9 tests, watched failing first — the card parsed and recomputed (hash, version, integrity prefix, option-for-option against the real source), absent-blendshapes read as disabled never unknown, and the ratchet list containing both new paths.
- [ ] 10.2b A Content-Security-Policy closes the telemetry residual at the network layer [D9]: `connect-src 'self'` (plus `blob:` only if the model loader needs it) as a meta policy, the wrapper kept as the second layer, SECURITY.md and ADR-0004 corrected. Check: the full e2e suite including the WebKit stepped clip passes under the policy; the 70-second telemetry e2e still proves the wire is live before asserting silence.
- [ ] 10.3 DROZY shape rows, DECIDED by amendment 17 [B4]: the three shape correlations are retired rather than re-measured, because the ~3-hour re-measure would re-run a null on a sample missing its sleepiest sessions, on a rate the stepper had not verified, to defend verdicts that pass by chance at p 0.5 (10.10c appends that chance rate). drozyGuard gains a per-feature source map so the caveat must name every feature whose sources moved since bd2a98d, and README's DROZY paragraph says which rows are stale (all seven). Check: guard map test failing first on an unmapped feature; the ruling dated in docs/drozy-result.txt and DATASETS.md; the sample is re-measured only if 12.18's v2 read moves a shape feature.
- [x] 10.4 Long-session export honesty, DONE 5 September 2026: the drop site counts every per-second row the 3600-row buffer discards, and a nonzero count writes `feature_records_dropped` plus a plain-sentence note into the export (featureRecordOverrunRows in sessionMetadata.ts, on the blink log's WARNING precedent); the participant report's truncation line carries the same count. Absent when nothing dropped, never a zero row. Check: under-cap emits nothing at all; an over-cap count emits both rows with the exact count; the note cites the cap from the one shared constant.
- [ ] 10.5 Iris-signal positive control: the caveat docs/iris-occlusion.txt names in its own last paragraph, closed with a per-blink table over the caught blinks. Check: dated prediction appended before the tool reads real spans; tool tests watched failing first on synthetic tables. The cheapest claim row on the board. (Prediction, decision rule and the committed tool — analysis/blinklab/iris_control.py, analysis/tools/iris_positive_control.py, 17 tests — shipped 5 September 2026; the tick waits on the run, which needs the caught-blink span extraction from the machine that holds the measured folder.)
- [ ] 10.6 False-alarm autopsy: the 65 false alarms get the mechanism treatment the 67 misses got, preparation-sensitivity as a droppable sub-question. Check: current tables regenerated and committed first; numeric predictions committed before any real table is read; verdict CSV under docs/evidence/.
- [x] 10.7a Aperture-mm noise floor, DONE 5 September 2026: the prediction committed first (four numeric bounds, its own commit), then src/core/apertureNoise.ts measured the fixture through the real pipeline — median frame-to-frame wobble 0.020/0.019 mm per eye (an order of magnitude under the 0.4 mm shallow-blink band, so temporal landmark noise is not what hides shallow blinks), p95 0.286/0.242 mm (the fatter tail any per-frame threshold must respect), cross-eye disagreement 0.200 mm median and 0.860 mm p95 (ten times the temporal floor). All four predictions held, two exactly at their boundaries. Result in docs/aperture-noise-floor.txt, recomputed by test on every run so it cannot drift. Check: pure stats pinned on hand-built series (a delta never bridges an excluded blink, the mask uses each series' own median), and the committed numbers held to the fixture recomputation.
- [ ] 10.7b Cross-iris disagreement refusal, threshold DERIVED from 10.7a's measured cross-eye distribution within the pose gate, not chosen. Check: the constant held at or above the p95 parsed from docs/aperture-noise-floor.txt (the 10.10b pattern), so a threshold below it reddens the build unless the doc carries the stated reason.
- [ ] 10.7c Ruler scheme identity-invariance check, only if a pooled-ruler change is ever proposed: prediction with a committed adopt-or-revert rule, gated on 10.7a's floor proving the current scheme noisier. Check: a ratchet entry — a commit touching rulerFit.ts, baseline.ts or calibrationWindow.ts requires a dated docs line naming the floor comparison. Conditional; it never ticks on its own and does not gate Phase 15.
- [ ] 10.7d Iris ruler physical check, owner [the scale-bar half of B12]: a printed scale bar at three distances on the owner's setup. Check: prediction committed first (ruler within ±5% of the bar at all three distances); absolute-mm thresholds kept out of the measurement path; the result written beside the ±4% population spread 10.10c already states.
- [ ] 10.13a The line travels with the number [A8, A15's columns; G-Guided b-11]: `blinkLineMm`, `blinkLineSource` (passive | guided | fixed | none), `shutLineMm`, `shutLineSource` appended to CSV_COLUMNS and FeatureRecord; guided medians, n_open, n_closed and the separation ratio as metadata rows; one `blinksWithheld` boolean per frame shared by readout, record, report and log button; refusal withholds durations, amplitude and velocity as docs/calibration-refusal.txt promises; a stored line refused when `frameSource` is file and flagged with its stamped conditions when the live camera's iris px or frame size differ from the stamp beyond a stated margin; both calibration buttons gated on the camera; plot.py reads the exported line. Check: e2e that a seeded line reaches the export; a wiring test that a refused session's records carry null in all three fields; a dated section in docs/validation-plan-round2.md on reading guided sessions; prediction committed that Eyeblink8 is byte-identical apart from the new rows (a corpus clip starts with no stored line). Lands BEFORE 10.8a and before any further session file is collected.
- [ ] 10.14b The corpus exclusion cross-checked against the container, owner [A11; G-Stepped-9]: analysis/tools/prepare_rldd.py writes one ffprobe line per clip (`r_frame_rate`, `avg_frame_rate`, `nb_read_packets`) into a manifest; rldd.py gains the coverage check Eyeblink8 has, refusing above a stated tolerance; docs/uta-rldd-plan.md restated as "the rate this instrument measured"; the 30 exclusion reason lines pasted from a re-run of analyse_rldd.py over the retained CSVs and reconciled with the count; the same manifest attached to the 20 retained DROZY sessions and the corroboration sentence (DROZY's README states its rate) written into drozy-result.txt. Check: a synthetic manifest with one wrong frame count reddens the check; no re-measurement.
- [ ] 10.14c The stepper's probe below half a period at high rates: `CALIBRATION_STEP_S` is 10 ms, one whole period at 100 fps, so a probe landing on every other frame calibrates at twice the period, every target then lands exactly on a real frame, and 10.14's inexact-landing rule cannot see it. The probe step derives from the first two landings (a quarter of the smallest gap seen, floored at 2 ms) and the stale "4 ms" comment goes. Check: a 120 fps fixture with an explicit frame-time list measures every frame with zero inexact landings and reports 120, red on the current stepper; the 60 fps e2e fixture unchanged; pre-registered as 11.4's voiding condition.
- [ ] 10.8a Regression run at HEAD, owner, BEFORE any change on the corpus-rule list [D10]: the replay tool committed first (per miss: crossing-to-reopen span, distance to the previous event, the re-arm flag from a full-clip replay), then Eyeblink8 re-measured, the anchor moved, the guided-line prediction (docs/blink-line-adoption.txt), the stepper prediction (docs/stepper-honesty.txt) and 10.13a's byte-identity prediction scored in dated sections, engine and Playwright version recorded beside "Built from commit". Check: the DETECTOR CHANGED caveat clears; every prediction's outcome section exists whichever way it lands; 10.14's `inexact_landings` row reads 0 on every clip or the design is falsified as written. The stepped path does not read the frame driver, so this anchor is the one 13.8b's parity check reads (supersedes the ladder's "D10 depends on A2").
- [ ] 10.8 The ~5 armed_then_dropped misses [corpus rule]: each reproduced as a failing trace-harness test, then the re-arm mechanism fixed. Check: failing test per slice committed before any fix; Eyeblink8 regression prediction committed (5 recovered, no false-alarm identity churn, other 62 misses unchanged) with an adopt-or-revert rule. The accuracy CLAIM itself waits for Phase 11's out-of-sample gate. Follows 10.8a, so the diagnosis rests on the replay tool rather than an uncommitted table.
- [ ] 10.9 Blink-duration device anomaly, re-pointed [C2's arithmetic; the A2 candidate]: first tested as arithmetic on the existing `shutBaselineMm`/`apertureMm` columns of the dry-run exports under the two line placements (the guided line sits 30-50% above the passive one); then the re-read candidate, stated as the one pair that supports it (one Mac at about four stamps per delivered frame read 166 ms against one at about one stamp reading 149 ms, while the two iPhones read 96 and 97 ms at one and two stamps), whose observable, stamps per delivered frame, the dry-run exports do not carry, so the read is a fresh paired session on one Mac behind a 60 Hz and a 120 Hz display; and only then live, through the 11.0a/b cued protocol on phones and Macs, with the same clip stepped on each device as the device-independence control (15.4a). Check: each candidate's prediction committed before its read; disagreement beyond one frame period falsifies the camera-side hypothesis and names the next instrument row.
- [x] 10.10a PERCLOS sampling bound, DONE 5 September 2026: prediction committed first (its own commit), then a seeded deterministic sweep (src/core/samplingBounds.ts, hand-rolled LCG, seed 20260905, 2000 draws per cell) measured p95 absolute PERCLOS error at 15/25/30/60 fps and P 0.05/0.15/0.30. The answer is a rounding error with a visible mechanism: every cell's p95 is exactly two samples over the window's count (0.0022 at 15 fps down to 0.0006 at 60), boundary quantization, independent of P. All four predictions held, two by an order of magnitude. Blink RATE's sampling story stays with its own committed catch tables (docs/blink-sample-rate.txt) — shallow quick blinks are LOST there, so rate is a floor, not a count. Result in docs/sampling-bounds.txt, recomputed by test on every run. Check: sampler and generator pinned exactly on hand-built cases; the committed 12-cell table held to recomputation; monotone-in-fps property tested.
- [x] 10.10b The on-page conditions lines, DONE 5 September 2026: two static sentences built in core (samplingBounds.ts) and placed beside the numbers they scope — the Blinks box says the count is a floor, not a count, citing docs/blink-sample-rate.txt; the Eyes box says PERCLOS's sampling term is at most ±0.002 even at 15 fps and names the shut line and the landmark floor as the real conditions, citing both documents. Check: the sentence contents unit-pinned (the ±0.002 constant held to the committed table's own maximum, parsed from the doc), and one Playwright test proves both actually render — a created node nobody appended is the 9.4b lesson. Duration's line stays held behind 10.9.
- [ ] 10.10c Intervals and stated bounds on every published number [B8, B9, B10, B11, B12]: Wilson intervals and subject-level bootstrap intervals printed by the three headline tools beside their points (recall 341/408 is 79.7-86.9; three-class balanced accuracy 0.498 sits in about 0.42-0.57) and carried by resultsBlock and resultGuard; the UTA-RLDD per-subject table and standardised coefficients the plan promised; Bar 2's p 0.099 re-read as "no evidence either way, detectable edge about 0.14 at this n" with a paired bootstrap beside it; DROZY's within-subject bar annotated with its chance rate (p 0.5) and validation criterion 1 with its n (0 of 3 sound sessions, 95% upper bound about 56%); all six known-and-unstated bounds written into the conditions sentences: PERCLOS includes blink time; peak velocity is biased by the single finite difference at 25-30 fps, the bound generated from the committed sweep rather than quoted; the iris constant's citation and its ±4% population spread; aperture not pose-invariant inside the gate (pitch 20° reads about −6%, yaw 25° up to +12%); the sampling bound excludes ordinary blinks; the fixture's 1e-4 storage precision beside the noise floor. Check: every interval and bound recomputed by test from the committed tables; no headline sentence without one after this row.
- [ ] 10.11 Long-closure hysteresis on the ARMING side and the bounded gap [A7, A19; corpus rule]: a long closure still ends at the shut line, so every duration is unchanged; a new closure cannot arm until the aperture has cleared the line by a noise-floor-derived fraction (blink.ts's own re-arm pattern); closure state survives a bounded run of untrusted frames; a closure beginning right after a gap does not arm unless the eye was seen open. Cadence-independent, because duplicate ticks carry the same aperture, so it may precede 13.8. Check: prediction committed first on synthetic hover traces parameterised from the dry run's per-second band (2.79-3.01 mm against a 3.04 mm line) at 30 and 120 Hz: the iPhone shapes' 3 and 2 events collapse to 1, desktop unchanged, durations byte-identical; a 6 s closure with ±0.3 mm noise counts once; test/core/longClosure.test.ts:291-307 re-pinned to the physical intent; hysteresis-not-filtering stated as the rule; Eyeblink8 through 10.8a's regression discipline. After 10.8a.
- [ ] 10.12a The blink gate reads the evidence rate [A16]: `blinkMeasurable` from min(sampledFps, fps) with 60/65-style hysteresis where delivery is reported, at the three gate sites; the report can no longer assert withholding while printing the numbers. Check: one deliberate dim-room session run BEFORE the code changes so the pre-registered rule (docs/blink-sample-rate.txt) has its observation (owner, on 13.2's day); a verdict/report fixture at sampled 20 / processing 60 whose measured rows and verdict agree; the stepped corpus unchanged by construction (no delivery on a clip).
- [ ] 10.12b Blink rate over observed time, and counting-suspended as a fact [A13, A14]: the rate divides by frames that were fed an aperture, windowed like blink times, null below the observation minimum, the observed fraction exported beside it; the rate reads null while closed longer than the maximum blink or while the re-arm gate is down, "blink counting suspended for N s" on the page and a per-record flag; vertical iris offset exported so downgaze droop is separable. Check: a face-loss gap no longer reads as calm on a synthetic; resting at 4.2 mm on a 4.0 line with five deep blinks reads suspended, not 1; the DROZY and RLDD rate columns re-derived from retained CSVs are named as changed in their caveats.
- [ ] 10.12c Sample floors count face-seconds, not ticks [A20; corpus rule]: baseline and guided floors require a minimum span of trusted-face time (or distinct `deliveredCount` values) per window and per phase, face seconds shown and exported. Check: prediction committed first that the stepped corpus is byte-identical (deliveredCount does not exist on a clip and face-seconds equal elapsed seconds on every corpus clip whose face is never lost; any clip where they differ is named); a 120 Hz display no longer satisfies the floor on 25 photographs. After 10.8a.
- [ ] 10.13b The learning window, decided [A15's decision; corpus rule]: the first 30 s of every session currently count blinks against a 4 mm fixture line from one face; the decision is pre-registered and lands as its own delta, either feeding null while learning or keeping the fixture line and labelling the count provisional on the page and in eyeblink8-result.txt and MODEL_CARD's conditions. Check: the committed delta (which clips, how many blinks, in which direction) scored against the 10.8a anchor; the line source per blink is 10.13a's column either way. After 10.8a.
- [x] 10.14 Stepper honesty on variable-rate clips, code DONE 6 September 2026 [A1]: every calibration gap must be a whole multiple of the smallest within 1.5 ms or the clip is refused by name as a variable frame rate; the step is the mean period those multiples imply; an inexact landing is counted and refuses the run past 2 in 100; `frame_interval_s`, `frames_sought`, `inexact_landings` written to every stepped export; the fixture takes an explicit frame-time list and the four pre-registered cases are pinned (docs/stepper-honesty.txt, committed before the code). Check: the four fixtures were red on the old stepper; six mutations reddened; the corpus half (zero inexact landings on all eight clips, byte-identical or the quantised-branch prediction) is scored by 10.8a.
- [x] 10.15 The refusal survives the export border, DONE 6 September 2026 [A3]: `sampled_fps` handed to the verdict as the same one-decimal value the export writes, so the page and the Python mirror agree in [24.95, 25.0) and [59.95, 60.0) and pilot.py stops calling a rounding artefact an instrument defect. Check: verdict fixtures at 24.96 and 59.96 on both sides; the self-refuting "25.0 ... below the 25" sentence cannot render. (Landed: the page rounds `sampledFps` through `asExported(…, 1)` before the verdict, an `edge` fixture at 25.0 reproduces byte for byte on both sides, the literal boundary probes 24.9/25/59.9/60 are pinned, and the wiring is source-pinned.)
- [x] 10.16 Four small arithmetic fixes, one pull request [A27], DONE 6 September 2026: the two delivery rates measured over one span and worded "read all N" when sampled ≥ delivered; PERCLOS's minimum observation as a valid-sample count, exported; the permutation p-value as (k+1)/(N+1) in light_response.py; the frame trace's duplicate `frames_measured` row dropped and one `_read_metadata` shared by loader, validation and blink_log. Check: a literal test per fix; `sampled <= delivered` as a property over five delivery rates against six processing rates, plus the sped-up-camera case staged as a state, 99 and 100 PERCLOS samples across a long enough span, a p that must be 1/1001 rather than 0, one `frames_measured` line where the caller owns the key, and the blink log refusing a repeated key.
- [ ] 10.17 Head-pose matrix layout verified, owner (one frame) [A24]: one DEV frame logs the 16 numbers with the head turned (translation at 12-14 means column-major); `headPose.ts` reads `data[col*4+row]`; a real-recording fixture with a matrix pinned; MANUAL item 19 re-run for signs; the translation column kept for 14.9a's distance gate. Check: the fixture test fails on the row-major read. A correctness fix; it does not wait for the Phase 12 gate.

## Phase 11. Out of sample and out of house (the generalization era)

Eyeblink8 has been read to per-miss identity across five detector eras; it is exhausted as evidence. Every accuracy claim from here graduates on corpora and people no version of the detector ever saw, on a pinned build, through a cued protocol whose ground truth costs no privacy. The sibling corpora are read ONCE, on the final pinned build that 12.18 and Phase 15 also use, after every corpus-rule row is ticked and after the blendshape witness (12.2, 12.3) has been scored on the regression harness, so a fused detector does not graduate on corpora already spent.

- [ ] 11.0a Cued blink-and-closure schedule, pure [C4]: a timed cue list on the lightSchedule pattern (blink now; close for 3 s; close for 20 s; look away; rest) and a scorer that pairs cues with detector events into caught / missed / latency, refusing an uncued or over-long session. Check: hand-built event streams recover exact tallies; an ignore-the-cues adversarial stream scores 0 caught, never a faked success; the two-marks protocol is named as retired in participant-instructions.md.
- [ ] 11.0b Cued protocol wired: overlay, tones, export rows, per-event table in the report; rows 10.9, 11.6, 13.3, 14.5, 14.13 and 15.5 re-pointed to it. Check: Playwright drives the overlay off a shortened schedule and reads the cue rows back from the export; the report's per-event table prints delivered rate, processing rate and sampled fps beside every tally.
- [ ] 11.6a Guided calibration as a procedure, the detector half [A9, A10, C3]: a settle window per phase derived from constants.ts's own rationale, so the open eye is not sampled while the person reads; the calibration's own 3 s hold fed as null to all four reducers with a marker written; a verification phase ("blink three times, normally") that stores the line only if the reducer caught at least two; a resolve-time soundness ceiling pre-registered against the open distribution's lower tail (a line at 0.85 of open is refused). Check: `calibrationSessionStep` unit-pinned on the settle and verification states; the ceiling's factor committed before any guided data is read; the verification outcome logged per calibrated person as the procedure's first measurement.
- [ ] 11.6b Guided calibration as a procedure, the interface half [B20, E3]: audio tones and vibration at phase boundaries; `role=dialog` with an assertive live region; an explicit Cancel that says nothing was stored, Escape included; refusal text conditional on what the run saw (pose rejections versus late closure versus true non-separation); n_open, n_closed and the separation ratio shown, worded "thin, consider running it again" near the floors. Check: a Playwright run of the overlay off a shortened schedule reaches every state; MANUAL item 66 rewritten to the shipped behaviour and performable with closed eyes.
- [ ] 11.6c Guided calibration as a procedure, the record: camera label, resolution and timestamp stored beside the three numbers (parse kept strict) and the outcome exported through 10.13a's rows, so round II and Phase 15 can read which procedure produced a line. Check: the stored-shape parser refuses a stale format; the export carries the procedure's numbers on a seeded line.
- [ ] 11.8a A pinned build, row zero of 11.8, 11.9 and every Phase 15 row [D11]: the commit of any run that becomes evidence is tagged and released with `dist` attached; a README "reproduce the exact page" section (`git checkout <app_commit> && npm ci && npm run build && npm run preview`); the frozen plan names the build and the intake refuses a differing `app_commit`; validation_report.py, pilot.py, analyse_drozy.py and analyse_rldd.py print the distinct commit set and refuse or state a mix. Check: the intake tool reddens on a synthetic mixed-build folder; a test asserts that every plan naming a build names a tag that exists in git, and "frozen" appears in no plan without one.
- [ ] 11.1 Row-zero corpus gate: verify the TalkingFace and Silesian5 GPL3 downloads actually contain video (ZJU joins only if its videos verifiably ship — its official page is dead, see DATASETS.md). Check: a dated docs note per corpus recording what the download contains, with the ffprobe manifest 10.14b introduced. After 12.3 has been scored and its fusion decision, if any, has landed.
- [ ] 11.2 TalkingFace prepare tool and annotation loader, remux-not-re-encode, reusing the Eyeblink8 .tag loader where the format matches. Check: prepare test verifies decoded frame counts on both sides and refuses re-encoded input; loader tests watched failing first on malformed tag files.
- [ ] 11.3 TalkingFace pre-registered frozen-instrument run, ONCE, on the final pinned build, after every corpus-rule row (10.8, 10.11, 10.12c, 10.13b, 12.0a, 12.6c, 12.8a) is ticked. Check: prediction band committed before any measured folder exists, derived from Eyeblink8's per-clip spread (recall 66.2-93.1) plus the preparation-sensitivity finding, voiding conditions named in advance; the result carries the digit-reproducible command, the container coverage check, the `app_commit` and a Wilson interval.
- [ ] 11.4 Silesian5 prepare and pre-registered run, same discipline and same build as 11.3; its higher native frame rate additionally probes the 0.4 mm shallow-blink band on real footage. Check: the pre-registered voiding condition is `frames_measured` against the ffprobe count, refused above the tolerance (10.14c's case, where a doubled period lands every target on a real frame); the high-speed condition named in advance.
- [ ] 11.6 Per-device deliberate-blink matrix, on the 11.0a/b cued protocol: duration bands, long-closure count per cued closure, catch-rate and latency bands per setup, mechanism explicitly open. Check: report tool tests failing first on adversarial synthetics; every table row prints delivered rate, processing rate, and sampled fps; the DSLR column reads ABSENT until that rig returns; tablets and Android read volunteer-supplied or ABSENT.
- [ ] 11.7 Run validation round II, the frozen 27 August plan, on the pinned build (11.8a) after 10.11, 10.13a, 11.6a-c, 12.0a-b, 14.0a-e and 14.0b — the calibration fixes' first test on people they were not built from. Check: committed docs/validation-round2.txt grading the three pre-committed structural predictions; round I's frozen table re-derives unchanged; a dated section says how guided sessions are read.
- [ ] 11.8 Owner n-of-1 longitudinal protocol: same setup, same protocol, same build (11.8a), ~3 weeks of sessions, test-retest bound as prediction one. Within-subject claims only, the expectancy and time-of-day confounds named in paragraph one of the plan. Check: pre-registered plan first; intake tool passes adversarial synthetics before any real file; analysis row split from the run row.
- [ ] 11.9 The assessment pilot run, the written and unrun plan (docs/assessment-pilot-plan.md), on the pinned build after 14.0a-e and 11.8a, with its own publication restraint kept: structural outcomes, never a recall figure, six people is a smoke test. Check: the plan's voiding conditions scored; KSS before and after captured once each on the `ended` transition; the KSS-versus-feature relationship reported as a bound with its n, never as a claim, until the n at which a claim would be attempted is stated with its detectable effect.

## Phase 12. New signals, then the one labelled read

Every per-second field that wants a validation path must exist and freeze before 12.17's owner-signed freeze; then UTA-RLDD's labels are read once, batched, and that is their last read. GATE, from amendment 17: no signal row here starts before 10.11, 10.12a-c, 10.13a-b, 10.14, 13.2 and 13.8b are ticked, because every signal built before them inherits a tick-rate column and an owner-referenced shut line. The instrument rows are exempt and are the gate's other half: 12.0a-b, 12.1, 12.2, 12.3, 12.15, 12.16 and 12.17's tool. Rows that can move the Eyeblink8 count say [corpus rule] and land after 10.8a with a committed prediction.

- [ ] 12.0a One personal ruler, the rule [C2; corpus rule]: the guided blink line and a guided SHUT line both placed as closed + k × (open − closed), with k pre-registered from the 10.7a noise floor; the shut line adopted for PERCLOS and long closures when a guided line exists, lifting their refusal as it does for blinks. Check: the owner's recorded droop session through both placements, published; the floor-shift prediction tested from existing `shutBaselineMm`/`apertureMm` columns of the dry-run exports; the P80 dry-run phone trace giving nonzero PERCLOS under its own floor; Eyeblink8 predicted unchanged (no stored line on a clip) and scored.
- [ ] 12.0b One personal ruler, the record: `shutBaselineMm`'s source recorded; the two duration quantities named in every column header and MODEL_CARD; the shut-line adoption written as the ADR the ladder's A8 asks for. Check: the column headers and the card carry the two names; the ADR exists and 10.13a's source column takes the new value.
- [ ] 12.1 Shared trend module: hand-rolled Theil-Sen slope with refusal floors and tie handling, so no consumer hand-rolls its own. Check: known-slope recovery, time-scaling and offset property tests, below-floor null; a test asserts neither consumer defines its own slope.
- [ ] 12.2 Enable outputFaceBlendshapes, with the trace/CSV contract and a measured latency delta. Check: CI pins the option flag and the [0,1] contract; the measured inference delta and a kill criterion (ships disabled by default if the 30 fps budget breaks on the slowest tier) recorded in docs/latency.txt; an e2e runs the vendored model on a stepped clip and asserts the category is present.
- [ ] 12.3 Blendshape closure witness: the pre-registered interrogation of the 49-miss ceiling with the vendored blendshape head, a landmark-fed network over 146 mesh points and NOT an appearance head, so the question is whether mesh points the aperture ignores carry closure the eyelid rim does not; the only vendored route past that ceiling, run on the regression harness BEFORE the sibling corpora are opened. Check: numeric predictions that state the landmark-fed prior, AND a neutrality check (the same 67 misses reproduce digit-for-digit with the head enabled) committed before the corpus run; per-miss verdict CSV; either outcome is a result, and any fusion signal is its own [corpus rule] row that lands before 11.3.
- [ ] 12.15 Per-second sampled_fps and inference_ms export columns, append-only, NOW (SPEC drift is already two fields behind). Check: TS test pins columns against SPEC.md; the Python loader accepts older files with NaN and refuses reordered columns.
- [ ] 12.17 Column-freeze manifest for the v2 read, self-retiring on the drozyGuard pattern: the manifest tool and its tests land now; the owner-signed freeze is dated after 12.16's and 14.9a's columns exist and before 12.18a's plan is committed. Check: manifest-vs-header test watched failing first; the corpus runner refuses on mismatch; owner-signed dated in/out per field.
- [ ] 12.4 Yawn detection: mouth aspect ratio with a duration gate, jawOpen recorded as a cross-check. Check: a 6 s yawn counts once and a 2 s speech-like oscillation of BOTH channels counts zero, on hand-built two-channel traces; the slowest-tier model-budget re-check is its own gate before merge.
- [ ] 12.5 Head-nod events and per-second pitch median/dispersion, read before the validity gate and baseline-relative, after 10.17. Check: scripted pitch traces through the pose math drive the pure reducer; a wiring test proves aperture is null past the pose gate while the nod still fires.
- [ ] 12.6 Closure-duration taxonomy: prolonged, microsleep-RANGE, sustained. The copy never says "microsleep detected". Check: 0.8/3/20 s classify correctly, 0.4 s classifies as nothing, the 2.0 s boundary pinned to a side within one frame period.
- [ ] 12.6b Closure completeness beside duration [C11]: `closureFraction = amplitude / frozen baseline` and a complete/incomplete label at the arm line; the 1.5 mm absolute grey line retired. Check: the arm line and the label agree on every fixture blink; the committed miss table's min_ratio distribution reproduces from the new column; the column appended trailing.
- [ ] 12.6c Droop-then-blink and re-crossing blinks timed honestly [C5; corpus rule]: the closed phase timed from the arm-line crossing or emitted as a "refused: over-length" event class; a slow reopener reported to the reopen that clears the re-arm line with a `fragmented` flag. Check: pre-registered against Eyeblink8's 9 long-closure misses and the dry-run traces; the audit's probe traces become pinned unit tests; scored through 10.8a's regression discipline.
- [ ] 12.7 Lid openness ratio instrument over the frozen shut baseline, with the born-wrong-ruler refusal; export and panel only. Check: unit tests including the refusal; a claimGuard family for "lid openness" claims until 12.18 reads.
- [ ] 12.8a Peak closing velocity from a quadratic through the three steepest samples, where three exist [C6; corpus rule; after 13.8b]: the single finite difference reads 81-88% of the true peak for an 80 ms close at 25-30 fps; the harness's own blinks close in about 50 ms, which puts one or two samples on the descent at 30 fps, so the quadratic applies only where at least three velocity samples fall on the descent and the finite difference is emitted with its bound otherwise; the sampling interval attached to each row. Check: bias pre-registered per (descent duration × rate) from the committed raised-cosine sweep beside blink-sample-rate.txt, both branches pinned at 25/30/60 fps; one bias number generated from the sweep feeds 10.10c's sentence.
- [ ] 12.8 Blink reopening velocity and reopening amplitude-velocity ratio (after 10.3's ruling and 12.8a). Check: an asymmetric synthetic recovers both peak velocities within 5%; a regression test proves the reopening window clips at the next blink's onset; within-device-only caveat until 10.9's gap is explained.
- [ ] 12.9 Inter-blink-interval irregularity (coefficient of variation). Check: known-CV recovery; null below 15 blinks; invariance to uniform time scaling.
- [ ] 12.10 Closure-fraction curve: an honest multi-line family (30/40/50/60% of baseline); the existing PERCLOS becomes one point on it; score.ts untouched. Check: monotone non-increasing on every synthetic and fixture; every column labelled instrument-referenced, with the header saying why none is literature P80.
- [ ] 12.10a A blink-excluded PERCLOS beside the current one [C7]: a second accumulator excluding closures shorter than `MAX_BLINK_DURATION_MS`, both columns exported; the score keeps the current one until 12.18 reads. Check: unit tests on hand-built series; the DROZY refit from retained CSVs (rho side by side); the UTA-RLDD comparison is a named 12.18 rider, never a separate read; the literature's definition named in the header.
- [ ] 12.12 Look-away events and off-screen gaze share, after 14.9a and 14.9: unknown frames are neither on nor off, blink frames are excluded, and the off-screen threshold derives from 14.9's error file rather than the eyeballed 0.18. Check: known spans recover exact counts; the 499 vs 500 ms boundary pinned; unknown frames can neither count as off-screen nor bridge an event.
- [ ] 12.14 Time-on-task drift of blink parameters, computed from the blink log rather than the wrapping per-second buffer; demo-only label. Check: a synthetic multi-hour sequence still measures against true session start after buffer wrap.
- [ ] 12.16 Scene and face luminance as exported numbers — the parked ambient-light idea, measurement half only, no verdict adjective until a measured threshold exists. Check: dark/spotlight/malformed synthetic fields; blank-not-zero pins; labelled as the camera's rendering of light, not lux.
- [ ] 12.18a The UTA-RLDD v2 plan (docs/uta-rldd-plan-2.md), committed before re-download: the droop, taxonomy, completeness, reopening, interval, closure-curve, blink-excluded PERCLOS, yawn and nod families as named predicted riders; the look-away and gaze riders only if 14.9 has produced a committed number; the shape-feature ablation (v1 features minus shape) and the within-subject null with the paired share of subjects as pre-registered sections; a v1-parity gate CONDITIONAL on 10.14b's container coverage check passing. Check: the plan names the pinned build and every rider's prediction.
- [ ] 12.18b The one-shot UTA-RLDD v2 re-measure, owner, on the pinned build with 10.14b's manifest. Check: the coverage check passes per clip or the clip is excluded by name; the exports carry `app_commit`.
- [ ] 12.18c The pre-registered LOSO v2 analysis: per-feature and combined LOSO with 1000-shuffle controls and the within-subject null; intervals on every number; predictions kept when wrong. Check: parity within the pre-stated tolerance where the coverage check passed; the result file is the last read of these labels.

## Phase 13. Capture chain and platform truth (the maximum this hardware can deliver, measured)

The app has never asked its cameras for more than the default, never recorded which delegate actually ran, never measured its own first load, and, the audit found, measures processing ticks rather than delivered photographs. Rows 13.2, 13.4 and 13.8 are the front of the era.

- [ ] 13.2 Frame-rate negotiation, FIRST [C1]: read capabilities, then ask the live track for 60 fps as a MEASUREMENT, with `facingMode: {ideal: "user"}` and the dim-room session 10.12a needs on the same day. Check: the builder test asserts negotiated resolution is unchanged by the frameRate step, any change surfaced rather than silent; a dated docs section records declared max, negotiated rate and any resolution change per owner device; the adoption condition written before any device is read; the day decides whether live accuracy is capped at 30 fps on every owned camera.
- [ ] 13.4 Frames-missed-while-busy counter from presentedFrames metadata. Check: synthetic sequences (1,2,4,5 yields missed 1; a monotonicity violation refuses, never goes negative); two export rows; one deliberately-loaded session in a dated note. Lands before 13.8 so its before/after is trustworthy.
- [ ] 13.8a Inference once per photograph, the arithmetic first [A2]: pre-registered on the existing dry-run exports that A/V scales inversely with processing/delivery ratio across the 30 / 55 / 126.7 fps machines; no code. Check: the prediction and its outcome in a dated docs section before 13.8b opens.
- [ ] 13.8b Inference once per photograph, the driver, a CORRECTNESS row [A2]: `processFrame` driven from `requestVideoFrameCallback` (one call per presented frame, rVFC timestamp), rAF left to repaint, the rAF fallback kept and the driver recorded in the export header; peak closing velocity and amplitude-over-velocity stop scaling with display refresh over camera delivery. Check: a driver-level test detects exactly once per presented frame; the stepped Eyeblink8 rerun reproduces 10.8a's anchor digit-for-digit (the stepped path does not read the driver).
- [ ] 13.8c Inference once per photograph, the consequences [A2, E10; F-044, F-082, F-109]: one A/B live session with a velocity PREDICTION, not an equivalence expectation (owner); the LID_SLUGGISH ramp and MANUAL item 26 re-derived; docs/latency.txt re-run and the "42 µs core chain" sentence withdrawn (the per-tick cost is quadratic in tick rate, about 5 ms at 120 Hz with a still gaze); the per-tick residue: readouts rewritten only on change, running min/max in `detectFixations`, the pupil read drawing only the source rect. Check: the A/B prediction scored; the latency benchmark carries the work; the fixation scan is linear by test.
- [ ] 13.0 Engine agreement on a fixed clip, owner [C10, D17, B6's measurement half]: one commit, one machine, one prepared clip, WebKit and Chromium paired, on the m5max template with three outcome classes committed first; `user_agent` and the webgl2 probe written in clip mode so an export can name its engine; the stepped frame-count assertion made the WebKit project's mandatory local test; the results block says "WebKit binary" and "no engine other than WebKit has measured this corpus" until this row lands. Check: identical counts confirm; blinks moving with coverage identical is the cross-engine bound the project does not have; coverage differing at all stops the line; a precondition of 13.1's phone rows.
- [ ] 13.1 Session survival kit, phones and laptops [E8]: a scoping ADR first (citing the five phone sessions already in the record and the engine question 13.0 answers), then a wake lock re-requested on visibilitychange (the 260 s light protocol and 14.10, 14.11 need it on laptops as much as on phones), orientation re-reads with a flip count in the export, and the Mirror default off on an environment camera. Check: constraint-builder test, wake-lock wrapper against a fake navigator, orientation event test, MANUAL.md items.
- [ ] 13.3 Capture-rate claim: repeat the cued protocol (11.0a/b) wherever a setup negotiates ≥55 fps, at 30 and 60. Check: the plan pre-commits BOTH branches — the improved-catch prediction against blink-sample-rate.txt's sweep, and the empty-set branch ("no owner camera negotiated above 30: platform limit, prediction unscored").
- [ ] 13.5 Delegate truth and inference percentiles in the export [D8]: the request, the webgl2 probe and p50/p95 inference, unconditional and in clip mode too, because the executed delegate is unobservable in the vendored API and MediaPipe falls back to CPU silently; camera rows conditional, machine rows unconditional; one CPU retry when the GPU load rejects. Check: null-in/unknown-out tests; a resultGuard pin holds the observability sentence in MODEL_CARD; eyeblink8-result.txt's run header says "unknown, probe added after this run" where it is unrecoverable.
- [ ] 13.6a Capability ladder NOW, from what is already measured [E6]: delivered rate and iris-pixel verdicts rendered per setup on the existing sessionVerdict surfaces, light reading "unknown" until 12.16. Check: a unit test per verdict boundary with every null path yielding "unknown"; Playwright asserts the rendered ladder matches core verdicts; each sentence cites its source doc.
- [ ] 13.6b The light term joins the ladder, after 12.16. Check: the threshold derived from committed data or the cell stays "unknown".
- [ ] 13.7 Resolution-versus-rate probe and an honest mode menu, after 13.2 and 13.6a. Check: pure summarizer tests; fake-camera Playwright for rendering and export of the chosen mode; a dated note with real per-device probe tables.
- [ ] 13.10 Cold-load truth [E9]: measure time-to-first-measurement, then bound it; the 15.8 MB first load (3.8 MB model, 12 MB runtime) prefetched at page idle, a load timeout landing in `modelFailed`, the Cache API after first load. Check: docs/cold-load.txt with per-file wire sizes under stated profiles; a load-timeout state machine test (timeout plus no model is a visible stuck state, never healthy idle); the improvement row may terminate in "not fixable on this host".
- [ ] 13.11 Synthetic pupil resolution floor: how many iris pixels does the estimator actually need? Kept, because 13.12's arming condition needs it. Check: the committed table reproduces digit-for-digit from the generator; the floor is monotone in iris width at fixed blur; the 43 px anchor committed as a one-line prediction BEFORE the sweep; the floor labelled model-derived until real optics clear it.
- [ ] 13.12 Pupillometry re-run on real optics — DORMANT, owner-gated, zero rows now: armed only when optics that clear 13.11's floor become accessible, through the unchanged pre-registered gate in docs/pupil-light-plan.md.

## Phase 14. The demo front (legible output, interactive protocols, honest theater)

The show-and-tell goal, plus the cognitive-state work honestly reframed as task effects. The session-end state and the phone-first surface land first, because a demo that loses the session on Stop or traps a phone user is not a demo; the timeline strip lands next because the podium view (14.2), the report card (14.4) and the tour (14.5) draw on it.

- [x] 14.0a Session end is a state, DONE 6 September 2026 [A4, A17, A22]: `ended` reached from Stop, clip finish and the crash path; exports, report and KSS-after depend on it (KSS-after asked once, on the transition, with `kss_after_at_seconds` exported); calibrations, markers and light depend on `running && camera`; interruptions gated on running with a null stamp exported as `unknown`. Check: unit tests per transition and one Playwright sequence each for stop-then-export (export enabled, KSS-after asked once) and clip-then-report. Required before any pilot session is asked of a stranger.
- [x] 14.0d The camera that cannot be turned off, and the one that stopped, DONE 6 September 2026 [A5, A18, A26]: a superseded camera start stops its stream (request, check the token, then attach) and the picker and clip input are disabled while a start is in flight; delivery rates and the evidence rate captured at the `ended` transition rather than at the first consumer; a camera that stops delivering reads "no frames in the last 5 s", not "this browser does not report it", track `ended`/`mute` routed to failed or ended, and record writing waits for a delivered frame. Check: a Playwright picker double-change leaves one live track; the report's evidence rate is the same number at every consumer; a frozen fake camera ends the session by name.
- [ ] 14.0e The crash branch and the reset [A21, A17's residue]: a stepped clip that throws mid-run exports as a file session with its clip name (`# source: file`), the decision extracted into core; `resetSession` clears calibration state and inference samples; every stimulus start refused or exported. Check: an e2e injecting a throw during a stepped clip asserts the provenance rows; a unit test that a reset leaves no calibration in flight.
- [x] 14.0b Phone-first demo surface, DONE 6 September 2026 [A6, E1, E4, E5, B19]: the Source box first under 1000 px so Start camera is in the idle viewport; the light overlay guarded on `requestFullscreen` with a touch exit and touch wording; idle strings that say "not measuring", never "measuring..." or "Blinks: 0"; the short caveat returns beside the score; a model-loading sentence on the camera path and the baseline countdown beside the score; the refusal sentence rendered whenever `calibrationRefused`; the stored-data box titled for what it lists. Check: a 375-wide Playwright project asserts Start camera is within the idle viewport; lightResponse.spec.ts covers the no-fullscreen path via addInitScript; an e2e that `#box-alertness` contains "not a safety or medical device"; uiGuard extended to button and label strings and the idle-string table.
- [ ] 14.0f Keyboard, dialog and links [E2, E7]: Escape closes every overlay and KSS is a native `<dialog>` with a focus trap and initial focus on Skip; a repository link, cited docs rendered as links, a meta description and a favicon (the held choice decided here). Check: Playwright tabs out of the calibration and heatmap overlays; the dialog's cancel is intercepted; uiGuard sees the repository link.
- [ ] 14.0c The page's own documents tell the truth [D15]: docs/UI.md brought to the shipped design; MANUAL items added with dated "superseded by" notes; the PR template requires a reason for "n/a". Check: uiGuard sees the sections UI.md claims; MANUAL items 56, 57, 64 and 66 marked owner-manual and performed once, dated.
- [ ] 14.9a The gaze profile refuses and carries its conditions, BEFORE 14.9 and before any gaze signal [A23, C9]: per-axis RMS residual and R² with a pre-stated refusal bound (RMS > 0.15 or R² < 0.8); median pose, iris width px, viewport, DPR, screen size and camera stored with the profile and refused or flagged on a different window or camera; the calibrated point nulled with "head moved since calibration" beyond ~5° or ~10% iris width; the residual in the profile, the button label and the export header. Check: the noise-only-axis case refuses; `gaze_calibrated` and the threshold exported; a pinned profile with a stale window refuses on load.
- [ ] 14.9b Blinks out of the gaze chain, one definition of on-screen [A25, C9]: the offset nulled below the blink line, I-DT bridging a blink-length null gap, blink frames excluded from the heatmap, the dwell and the on-screen column; with a profile, on-screen means the calibrated point within the margin, worded "window" not "screen"; one ruler for live and replay I-DT. Check: the fixture recovers one fewer fixation per blink than today; the export, heatmap and quadrant on-screen definitions agree on the fixture.
- [ ] 14.9 Gaze validation pass and mid-session drift probe: five fresh points, screen-fraction error, accuracy and precision reported separately per target, the pose delta logged beside every error sample so drift and movement separate, stored and exported. Instrument only, no claim. Check: pure scorer on synthetic profiles of known error; SPEC and Python loader contract for the new column; the first committed gaze number replaces "not measured" in MODEL_CARD.
- [ ] 14.1 Session event timeline strip: blinks, closures, alerts, and score over the session, with every null second a visible gap. Check: property tests from synthetic records and the committed fixture (no zero-height points, gaps never bridged); e2e via synthetic injected events, never a face video.
- [ ] 14.2 Podium big-numbers view for a projector, drawing on 14.1's strip. Check: a no-face fake camera renders the refusal sentences verbatim, never "0"; the big view and the panel share the identical formatted string for a null; the demo notice stays legible at distance.
- [ ] 14.3 Explain-this-number provenance popovers: every rendered metric names its status in the record's real taxonomy (the score is cohort-level AUC 0.70 and unvalidated per-person; PERCLOS is an instrument-adjusted convention that includes blink time; velocity is device-conditioned until 13.8b). Check: a CI guard iterates the provenance table — every metric has an entry, and every evidence-bearing status names an existing docs/ result file.
- [ ] 14.4 Printable report card, with 14.1's strip. Check: parity test against the text report (withheld lines carry reasons, unknown never renders empty); unreachable while the camera runs; the demo notice prints.
- [ ] 14.5 Guided two-minute tour, on the 11.0a/b cued protocol, with 14.1's strip as its recap. The 30 fps misses are the script, not the bug: the tour shows a per-blink caught/not-caught tally and narrates the sampling story rather than coaching only slow blinks. Check: an empty event stream ends every gated step not-caught and the recap reads 0/5, never a faked success.
- [ ] 14.6 Record-yourself clip walkthrough, feasibility probe first. Check: the probe row pre-commits its result (recorded Blob stepped through the existing pipeline, honest refusal for variable-rate or duration-less recordings by name, which 10.14 now provides); later rows gated on the probe.
- [ ] 14.10 Follow-the-dot smooth-pursuit test: schedule, overlay, bounded offset log, analysis, and a pre-registered discrimination run, on RAW offsets or with the One Euro lag (100-300 ms by step size) pre-registered per amplitude, because the lag is the size of the effect. The output says "tracking agreement", not pursuit purity. Check: synthetics recover gain 0.8 and lag 100 ms within tolerance THROUGH the smoothing filter, center-stare near zero, anti-phase negative — a mutation per rule; the discrimination result kept if it fails.
- [ ] 14.11 Task-block schedule machinery and read-vs-rest blink suppression — the era's honest "cognitive state" claim, a demonstrated task effect on the owner's own sessions. Check: the no-difference synthetic must FAIL the analysis; per-block rates from blink-log events, never the rolling window; a pre-committed droop refusal bound so suppression-versus-masking is visible.
- [ ] 14.12 Reading-rhythm detector from the horizontal gaze sawtooth, demo-only until 14.11's run carries its committed prediction. Check: sawtooth accepted; staring, random walk, and the PURSUIT SINUSOID rejected, with the asymmetry-rule mutation reddening; promotion capped at "checked on the maintainer's sessions".
- [ ] 14.13 Scripted look-away validation for 12.12, per device, on the 11.0a/b cue machinery. Check: cue schedule and metadata pure and tested; the ignore-the-cues adversarial synthetic must FAIL the criterion; the derived per-event table (cue, caught, latency, device class, delivered, processing and sampled rates) generated by the report tool is committed, and raw session CSVs stay in $DATASETS, since they identify a machine.
- [ ] 14.14 In-browser psychomotor vigilance task (PVT): reaction times and lapses beside the eye features, within the moderated-volunteer discipline. Check: pure scorer tests failing first (lapse threshold, false starts, inter-stimulus bounds); e2e off a shortened injected schedule; columns null outside an active PVT window; the eye-features-versus-lapses claim is its own later pre-registered row.

## Phase 15. Full re-validation, in layers (every published number re-derived on the pinned build, on public data and on people, across devices, with an interval)

The owner's sixth brief: the validation must be complete and multi-layered. This phase does not add signals; it re-derives every number the page and the card publish, on the pinned build (11.8a), through six named layers, and replaces the hand-written claims with a generated report. Nothing here starts before every Phase 10 row except the conditional 10.7c is ticked, because re-validating a display-rate velocity or an owner-referenced shut line would validate the artefact; each layer names its own prerequisites. Volunteers stay pseudonymous and numbers-only under the participant discipline docs/validation-plan.md set; raw session files never enter the repository, only the derived tables the report tools produce; no frame ever does.

- [ ] 15.1 The validation matrix and its generator: a dated docs/validation-matrix.md naming the six layers (L1 pinned fixtures and an injected face in CI; L2 public corpora, stepped; L3 repeatability across runs, engines, devices and calibrations; L4 the cued live protocol on volunteers; L5 the rounds on the pinned build; L6 intervals and pre-registration on every read) and the device matrix (macOS laptop, iPhone, Android phone volunteer-supplied or ABSENT, external webcam volunteer-supplied or ABSENT, tablet volunteer-supplied or ABSENT, Windows laptop or ABSENT), each cell's status generated from committed evidence files and this ledger's ticks. Check: a tool renders the matrix and a test refuses a cell that reads "done" without a committed file behind it; the matrix is the only place a coverage claim may be made.
- [ ] 15.2 L1, a face in CI [D7], after 13.8b and 14.0a: a DEV-only landmark-injection hook fed the consented fixture recording (the owner's own landmark numbers, already committed) or a scripted blink, so an e2e asserts "Blinks: 2", the 25 fps refusal and a PERCLOS value through the real wiring; `blinkFeed` and `shutLineFeed` extracted into src/core under the coverage floor and the ratchet; the branch floor made a ratchet (current − 0.5, raised on green runs). Check: CI goes red with blink counting, aperture, PERCLOS or the fps refusal broken in the wiring, which today it does not.
- [ ] 15.3 L2, the public corpora on the final pinned build, after 11.3, 11.4 and 12.18c: Eyeblink8 re-run on that build (it is the regression harness; re-reads are allowed), TalkingFace and Silesian5 read once on it, UTA-RLDD v2 read once on it, DROZY retired (10.3); each with the container coverage check, the digit-reproducible command, the engine and Playwright version, and an interval. Check: the three once-only reads and the Eyeblink8 re-run name the same `app_commit`; a table in the matrix lists recall, precision and their intervals per corpus side by side.
- [ ] 15.4a L3, repeatability of the stepped path: the same clip stepped twice on one machine (byte-identical exports); the same clip on WebKit and Chromium (13.0); the same clip on at least three owned devices (device independence of the stepped path, a property the record does not yet hold). Check: every comparison's bound committed before the second run; a mismatch is a finding with its own row, never a tolerance widened after the fact.
- [ ] 15.4b L3, repeatability of the person-facing instrument: the same person's guided calibration run twice on one setup and once on a second (test-retest of the personal ruler: line mm and separation ratio within a pre-stated bound, from 11.6a-c's exported procedure numbers); the same session's verdict re-derived by the Python mirror byte for byte (10.15). Check: the bounds committed first; the numbers land in the matrix as their own cells.
- [ ] 15.5a L4, the cued live plan: the pre-registered plan for the cued protocol (11.0a/b) on volunteers, with each volunteer on at least two device classes, n chosen so the per-cell Wilson interval on cued catch rate is at most ±10 points (about 60 cued blinks per cell), prediction bands per (delivered rate, processing rate) cell with device class as the label amendment 16 makes it, the dim-room condition, and the 20 s cued closure for PERCLOS. Check: the plan names the pinned build, the n arithmetic and the voiding conditions before any session.
- [ ] 15.5b L4, the report tool: catch rate, latency, long-closure count per cued closure, duration per cell, PERCLOS under the 20 s closure, all with intervals and every row printing delivered rate, processing rate and sampled fps; adversarial synthetics before any real file. Check: the ignore-the-cues stream scores 0; a mixed-build folder is refused by name.
- [ ] 15.5c L4, the sessions, owner-moderated: at 30 fps and, where 13.2 negotiates it, 60 fps; external webcam, Android and tablet volunteer-supplied or ABSENT. Check: every session file carries the pinned `app_commit` or is excluded by name; raw files stay in $DATASETS.
- [ ] 15.5d L4, the report: the derived tables committed, the matrix cells filled, the predictions scored. Check: a cell with fewer cued blinks than the plan's n reads "under-powered", never a number without its interval.
- [ ] 15.6 L5, the rounds on the pinned build, after 11.7, 11.8 and 11.9: validation round II, the owner's n-of-1 as the within-subject layer, and the assessment pilot with its publication restraint. Check: each round's frozen plan names the build; the intake refuses a mixed folder; the results carry intervals and their voiding conditions scored; the KSS-versus-feature relationship is a bound, not a claim, until 11.9's stated n is reached.
- [ ] 15.7 L6, the generated validation report: docs/validation-2026.md written by a tool from the committed evidence — every headline number with its interval, its layer, its device coverage, its corpus or cohort size and its date; MODEL_CARD's "who it has been tested on" and README's results block regenerated from it; resultGuard pins every sentence to the file it came from. Check: a hand-edited number in either document reddens the build; the report regenerates byte-identically from the evidence directory.
- [ ] 15.8 The claim gate: after 15.7, no accuracy, precision, drowsiness or gaze sentence on the page, in README or in MODEL_CARD that is not in the generated report, and no sentence without its interval and device scope. Check: claimGuard's regex family extended with the retired forms of every superseded claim; the provenance popovers (14.3) read their status from the same report.

## Deliberately not taken up in this era

- Researcher's Night corpus scoring: deferred, the stepping-time sink; TalkingFace plus Silesian5 already deliver the generalization statement. A future-era candidate once those two are read.
- ZJU: no row until 11.1 verifies its download contains video rather than annotations alone.
- A separate immediate UTA-RLDD gaze read: merged into 12.18, not killed — the labels' third read is batched and final, never the cheapest and first.
- A second read of the sibling corpora (the former row 11.5): 10.8's delta is scored on Eyeblink8 by its own committed prediction, and the siblings are read once, on the final pinned build.
- Saccade and pursuit velocity, and microsaccades: 30 fps physics forbids them (a saccade spans one to two frames, so any velocity would be fabricated between samples). Turnover, drift, and tracking-agreement are the honest residues.
- Any post-hoc winner from the closure-fraction curve feeding score.ts: a forbidden path; any line-separation claim goes through 12.18's report-all or not at all.
- WebGPU delegate work and WASM optimization of the core chain: the delegate does not exist in the vendored API. The "42 µs core chain" sentence is withdrawn by amendment 17 (the per-tick cost is quadratic in tick rate, about 5 ms at 120 Hz with a still gaze); the honest fix is 13.8b, not an optimizer, and 13.8c carries the residue. 13.5's request-and-probe recording is the honest maximum on the delegate.
- New hardware purchases: out by the owner's own constraint. 13.12 stays dormant until optics the project already owns become accessible.
- A WebCodecs decode path for corpus runs (audit F-083): the "hours per dataset" premise is contradicted by the runner's own ~58 frames per second, and the seek path is the reference the ratchet pins.
- Restructuring the documentation layer beyond single-source numbers (F-070): the measurements hold (62% of recent file touches are prose) and the rule adopted is the narrow one, a number that appears in more than one document is generated or appears once with links; archiving docs/audit was declined on 15 August and nothing has changed.
- I-DT thresholds in degrees before 14.9 gives the geometry to state a degree (F-035); the replay's second ruler is a display choice.
- Per-eye iris offset bias (F-088): reproduces at about 0.07 and no consumer uses a single eye; reopened the day a single-eye fallback, asymmetry or vergence feature is proposed.
- A second full review before the ladder is half done: the August review found drift in prose, this one found it in wiring and at the border between the two languages; a third pass before Phase 10 is ticked would find the same classes wearing new numbers.
- Rewriting main.ts: still correct, linted and honest about its size; 14.0a-e and 15.2 pull the two decisions that produced defects (the session boundary and the gate wiring) into src/core, and nothing else moves.

Ideas, not commitments:

- Microsaccade detection, needs higher frame rates than any owned camera delivers, a genuine research challenge — still parked.
- Ambient light COMPENSATION (the measurement half is 12.16; correcting a measurement by it waits until a measured threshold and a validation path exist).
- Researcher's Night as a third sibling corpus, once TalkingFace and Silesian5 are read.
- A second learned-model attempt against the heuristic score, only if 12.18's new features move the LOSO needle (9.1's two-bar rule stands).
- Slow-drift gaze index (was 12.11) and fixation turnover trend (was 12.13): demoted by amendment 17, because both would move with blink rate on an offset that 14.9 has not measured (F-041, A25); they return, if at all, behind a committed 14.9 number.
- Thermal-drift proxies (was 13.9): demoted on the owner's priority, since no browser thermal API exists and the proxy would be a device-scoped note; 12.15's columns keep the door open.
- Local session history and trends (was 14.7) and the installable offline demo (was 14.8): demoted on the owner's priority until 14.0a's session state and 11.8a's pinned build exist, because history without a common ruler and an offline cache of an unpinned instrument both demo a stale claim.
- Early abort of a stepped run once its inexact-landing fraction is certain to refuse, so a ten-minute variable-rate clip is refused in seconds rather than minutes: an optimisation of 10.14's rule, not a change to it.
