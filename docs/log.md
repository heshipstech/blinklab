# Build log

One line per increment: date, id, what changed, what was surprising.

- 2026-07-28, 0.1, wrote the working documents, license and readme skeleton. Surprise: the plan itself had a contradiction (a Playwright check four phases before Playwright exists) and an ordering bug (dataset features needed video upload mode, which was scheduled later), both fixed as amendments before any code.
- 2026-07-28, 0.2, hand written Vite plus TypeScript scaffold, the page prints blinklab. Surprise: the entire production build is 0.83 kilobytes and takes 25 milliseconds.
- 2026-07-28, 0.3, prettier, eslint with the core purity fence, strict typescript. Surprise: had to step TypeScript back from 7 to 6.0.3 because the lint plugin does not support the new compiler generation yet. Tools travel in convoys.
- 2026-07-28, 0.4, vitest plus the first pure core function, distance, with three real tests, proven able to fail by mutating a minus sign. Surprise: git checkout cannot restore a brand new file, git is blind to files it has never been shown.
- 2026-07-28, 0.5, github actions ci running all five gates on every pull request, judged its own pull request. Also the first review ritual: the recall quiz format did not fit, switched to predict then verify with practical examples.
- 2026-07-28, 0.6, branch protection plus pull request and issue templates. Surprise: the gate locked out its own builders, the STATE.md wrap up ritual had to move inside the pull request, which is the gate doing its job.
- 2026-07-28, 0.7, github pages deployment on every merge to main, live at heshipstech.github.io/blinklab. Lesson banked before the bug: the subpath base setting that prevents the classic blank page deploy.
- 2026-07-28, 0.8, adr-0001 records the stack decision, its rejected alternatives and its accepted costs. Phase 0 complete: eight increments from empty folder to tested, gated, publicly deployed page.
- 2026-07-28, 1.1, start camera button, permission request, live video element. Surprise: the agent's embedded test browser blocks cameras entirely, which accidentally gave the deny path a machine check while the happy path stays human only.
- 2026-07-28, 1.2, camera state machine in core with a readable sentence per state, six new unit tests. The camera-blocking test browser walked idle, requesting and denied live in one sitting.
- 2026-07-28, fix #22, camera preview no longer squeezed: ideal 1280x720 negotiation plus element sized from the stream's real dimensions via a tested pure function. Also landed the 1.2 docs a hand typed file list had silently dropped. Checklists claim, git status verifies.
- 2026-07-28, 1.3, frames per second from injected timestamps, nine fake clock tests. Surprise: the embedded test browser reports itself permanently hidden, and hidden tabs get no animation frames at all, so the live number is human verifiable only.
- 2026-07-28, 1.4, the camera picture is now painted onto a canvas by our own frame loop, video element demoted to invisible source. Second review ritual ran in predict then verify form: the owner called the hidden tab fps behaviour almost exactly.
- 2026-07-28, 1.5, camera device picker behind a pure boundary mapper, seven new tests. Learned that device labels are blank until permission is granted, a privacy feature that dictates the picker can only populate after the first start.
- 2026-07-29, 1.6, mirror toggle driven by a six number transform matrix in core, plus the real capture resolution on screen. Phase 1 complete: the camera phase, from permission button to a canvas we own, drawn mirrored because humans expect mirrors.
- 2026-07-30, 2.1, the FaceLandmarker model runs fully from our own origin, model vendored, wasm copied from the locked package, ADR-0002. Proved the privacy stance with an empty network log during a model load. Surprise: eslint tried to judge 7000 lines of Google's generated code by our house rules.
- 2026-07-30, 2.2, all 478 landmarks drawn as a living dot mask, projected through the same mirror matrix as the picture so they cannot drift apart. The most shareable frame of the project so far.
- 2026-07-30, 2.3, the eye indices became named, tested constants and only the eyelid rims keep their dots. Third ritual ran: the owner offered both 160 and 480 for a mirrored projection, and the code settled it, mirror last, display only. Tests hit a round 50.
- 2026-07-30, 2.4, the ten iris landmarks drawn as orange rings with centre dots. Looks cosmetic, is calibration: these points carry the 11.7 millimetre ruler that Phase 3 divides by. Also today: a ghost bug correctly refused, the presence flapping was a hand over a lens.
- 2026-07-30, 2.5, the wrong landmark count guard: a 468 point model would now announce itself in a sentence instead of silently deleting the irises. Untriggerable with the vendored model, which is exactly the point, it defends the future upgrade.
- 2026-07-31, 2.6, inference time on screen beside its 30 ms budget, mean of the last sixty frames. A budget nobody watches is a rumour. Owner's measured number: 6 ms on the development MacBook, five times inside budget, the CPU delegate question from issue #38 is officially academic.
- 2026-07-31, 2.7, five seconds of the owner's real face frozen into a 3.3 MB fixture, 300 frames, 478 landmarks each, recorded by a dev-only button that provably never ships. Phase 2 complete. Surprise: the first recorder draft shipped its button to production merely hidden, and the build grep caught it. Hidden is not absent.
- 2026-07-31, 3.1, the eye aspect ratio, blinklab's first real measurement, tested on synthetic shapes and proven against the fixture, where it found the recorded blinks. Fourth ritual: owner predicted 3 blinks, a crude threshold found 2 full and 1 shallow, and that gap is Phase 4's opening problem.
- 2026-07-31, 3.2, the EAR sparkline: ten seconds, fixed scale, gaps drawn as gaps. Also fixed a latent stale number, the EAR text now surrenders when the face vanishes. The fixture's 300 frames became the chart's first regression test.
- 2026-07-31, 3.3, the synthetic face generator: faces from arithmetic with true anatomical sizes, rotated, placed and pinhole projected, so geometry tests know every answer in advance. Test count crossed 100 on physics homework: iris spans exactly 11.7 over 500 of the frame at half a metre.
- 2026-07-31, 3.4, pixels became millimetres through the 11.7 mm iris. Flagship test: mm exact across synthetic distances while px swings past double. The owner's recorded median aperture: 7.1 mm, dipping to 2.5 mid blink. Craft trap dodged: normalised x and y are different units on a 16:9 frame, convert to pixels before mixing directions.
- 2026-07-31, 3.5, the two variance numbers, live and tested: across synthetic distances px CV exceeds 25 percent, mm CV stays under a tenth of one. The stability line puts the lean in lean out experiment on screen, the ladder's strongest post now performable by anyone. Owner ran it: 39 to 46 versus 13 to 17, three to one.
- 2026-07-31, 3.6, pitch, yaw and roll unpacked from the model's transformation matrix by pure core maths, null at gimbal lock. Fifth ritual: owner correctly predicted aperture survives a 15 degree tilt, distances are rotation proof, which previews 3.7. Owner's calibration: left shoulder tilt reads positive roll, clean 42 degree isolation.
- 2026-07-31, 3.7, amendment 4: the tilt correction became a tilt invariance proof, distances cannot see rotation, verified at 0, 15, 30 to ten decimals. The counterfactual naive vertical measurement lives in the tests, shrinking by exactly cos(roll), the bug we never wrote, kept as a warning.
- 2026-07-31, 3.8, the validity gate: beyond pitch 20 or yaw and roll 25 degrees, eye numbers become "no valid measurement" with a sentence naming the violated axis, and the pose line stays alive to steer back by. Phase 3 complete: the measurement phase, pixels to millimetres to refusal.
- 2026-08-01, 4.1, the first blink detector: below 4 mm is closed, a reopen counts one, invalid frames break the cycle. The fixture priced the threshold, full blinks bottom at 2.2 mm, the shallow one at 5, so 3 to 4.5 all find exactly two. The ritual's ambiguous third blink now has a number on it.
- 2026-08-01, 4.2, the personal baseline: thirty seconds learns what open means for this face, the threshold becomes half of it, and the ratchet lets the bar rise but never fall, so drooping lids cannot recalibrate away the very signal that would expose them. Restarting the camera is the one honest reset.
- 2026-08-01, 4.3, blinks got a clock: the closed phase duration in milliseconds, the first direct fatigue signal, sleepy blinks are longer. The owner's recorded blinks measured 133 and 117 ms, textbook alert values, pinned as tests. Sixth ritual: the owner passed on guessing, so the number arrived as a worked example instead.
- 2026-08-02, 4.4, blinks per minute over a rolling window, with the young window problem handled honestly: the denominator is time actually observed and no rate exists before fifteen seconds. The famous resting 15 to 20 is now a line on the page, awaiting the owner's staring contest.
- 2026-08-02, 4.5, the blink became a shape: amplitude, peak closing velocity in true mm per second, and their ratio, the drowsiness time constant where velocity fades before travel. The phase's thesis increment, and both recorded blinks passed as physiologically plausible.
- 2026-08-02, 4.6, the frame rate honesty gate: below 25 fps a short blink can fall between frames, so blink metrics return null, never zero. Zero is a claim, null is an admission. The fourth member of the refusal family.
- 2026-08-02, 4.7, squint separation by time signature: closures beyond half a second count nothing, the five second plateau counts zero, and 4.3's held blink promise was honestly amended, a two second hold is a long closure now, 6.2's business. Also documented the engine's boot chatter in the manual after the owner spotted Safari styling an INFO as an error.
- 2026-08-02, 4.8, the blink event log: every blink becomes a durable record, timestamp, duration, shape, newest first, capped at fifty. Phase 4 complete: the blink phase, from a counter to an instrument that remembers. Seventh ritual: skipped by choice, the worked answer was 3 per minute. Phase order decision: the owner chose gaze first, the ladder as written.
- 2026-08-02, 5.1, gaze begins: the iris offset within its eye, projected onto the corner axis so roll rotates ruler and signal together, proven identical to eight decimals at 15 degrees. The generator grew an iris shift so offsets have millimetre ground truth.
- 2026-08-03, 5.2, the four quadrants: two sign comparisons and a perspective flip, positive horizontal is image right is the user's screen LEFT, quadrants named in the user's language. Labelled synthetic frames all agree. Uncalibrated and says so on screen, 5.4 owns the cure.
- 2026-08-03, 5.3, on screen versus off: a box threshold priced by the owner's own corner glance observations, either axis escaping means gone. The first attention measurement, and the second gate against leaving the screen, wandering eyes under a still head, distinct from the turned head the pose gate already catches.
- 2026-08-03, 5.4a, ground truth manufactured on demand: nine dots, thirty trusted samples each, nothing counted mid travel or mid blink, stored locally only. The collector refuses poisoned labels the way every gate before it refused doubtful numbers. Eighth ritual: skipped by choice, the worked answer was plus then minus.
- 2026-08-03, 5.4b, the solver: nine medians, one least squares line per axis, four learned numbers. The mirror flip arrives as a negative slope and the camera-above bias vanishes into the intercept, both proven by staging the bias and curing it. Quadrants now answer calibrated, the profile persists locally, and the solver joins the refusal family: frozen iris, single dot, null.
- 2026-08-03, 5.5, the pyramid grows its top: Playwright drives the real built app in a headless browser, fake camera flags stand in for hardware, and the calibration flow's wiring, open on dot 1 of 9, cancel, store nothing, is machine checked on every pull request. A fake camera exists, a face does not: e2e proves wiring, units prove math, the owner proves vision.
- 2026-08-04, 5.6, the jitter-lag tradeoff escaped by adapting (#95): the One Euro filter estimates how fast gaze is moving and sets its own smoothing from that, so a still eye is smoothed hard and a real glance still lands on time; two trace canvases under the sparkline draw raw against smoothed, and a null input resets the filter so gaps stay gaps. Recorded from LEARNING.md and the pull request on 2026-08-15, not on the day: the measured step coverage was 97% against a fixed filter's 53% in the same 150 ms budget, which is the whole argument for adapting.
- 2026-08-04, 5.7, the eye does not glide (#97): I-DT splits gaze into fixations and saccades on two thresholds at once, still enough for long enough, and it is the smoothed signal's first load-bearing consumer, because raw jitter spreads about as far as the dispersion box and would shatter every real fixation. Recorded on 2026-08-15: fixations never bridge a gap, since a fixation stitched across missing frames would be an invented stillness.
- 2026-08-04, 5.8, from events to a distribution (#99): count, mean, median and longest over the rolling window, every one of them composed from machinery that already existed, mean from 3.5 and median from 4.2's percentile. Recorded on 2026-08-15: the still-growing fixation counts at its length so far, because a panel that waited for the end would sit stale through exactly the long stares worth watching.
- 2026-08-04, 5.9, dwell binned (#101): a 16 by 9 grid counts one frame per cell, normalised so the hottest cell is exactly one, drawn as transparency over a test card that is drawn rather than photographed so no image licence rides along. Recorded on 2026-08-15: the grid is deliberately coarse, because amendment 2 set calibration at quadrant reliability and a fine grid would render precision the profile does not have.
- 2026-08-04, 5.10, time on a slider (#103): the scanpath replay closes Phase 5, answering "how much of the path existed by then" with the codebase's first binary search, about fifteen comparisons instead of eighteen thousand, pinned against the obvious slow count at every kind of probe. Recorded on 2026-08-15: heatmap and scanpath are two halves of one truth, 5.9 shows where attention pooled and 5.10 in what order it travelled.
- 2026-08-04, 6.1, the duty cycle of the eyelid (#105): PERCLOS over a rolling 60 seconds, classified at push time so the ratchet can never rewrite history, sample weighted, with untrusted frames joining neither side and a minimum observed span before it will answer at all. Recorded on 2026-08-15: null rather than zero before the span is met, the 4.6 rule again, because zero is a claim and null is an admission.
- 2026-08-04, 6.2, the long closure detector: the event 4.7 promised, fired while the eyes are still closed, sharing the blink maximum as an aliased constant so the two bins cannot drift apart. Surprise: adversarial review before the pull request found a real partition hole, a closure crossing the line between two frames landed in neither bin, fixed with a late fire on the reopen and a cross-module test that feeds both reducers one stream and demands one bin per closure.
- 2026-08-05, 6.3, the alert governor: long closures now raise a visible banner, debounced to one firing per five seconds, swallowed triggers counted as suppressed instead of silently eaten, the window measured from the last firing so fast triggers cannot silence it forever. Surprise: the increment turned out to be three timers deep, the fired flag inside the event, the debounce between events, and the display window, each one line of arithmetic behind a named constant.
- 2026-08-06, fix #112, amendment 5, the shut line: long closures and alerts moved off the blink threshold onto 40 percent of baseline, priced from the owner's measured shut floor and reading droop after the literature's 20 percent line proved unreachable for this instrument. Surprise: the prediction on record said PERCLOS would jump to ten percent during a held closure, it read exactly 0.0 through thirteen witnessed seconds, the instrument reads fully shut eyes as a third of baseline, never near zero.
- 2026-08-06, fix #114, depth arming: a closure only counts as a blink once it reaches ten percent below the threshold, so chatter riding the line can no longer mint phantom blinks, while closing, reopening and every duration stay exactly as 4.3 defined them. Surprise: the first design, a textbook reopen-side hysteresis latch, passed all 292 tests and was then killed three ways by adversarial review with reproduced sequences, because a latch changes when closures end and half the project's numbers are defined by that moment. The gap moved below the line, where no number lives.
- 2026-08-06, fix #113, amendment 6, PERCLOS learns to see: the closed line moves from the literature's unreachable 20 percent to the measured 40 percent shut line, shared with the long closure detector through one aliased constant, and the blindness experiment (0.0 percent through a witnessed 12.9 second closure) becomes the regression test. Surprise: the fix deliberately retires a safety net, the disagreement between the two detectors was what exposed amendment 5's bug, and the honest move was writing down what replaces it rather than pretending the redundancy was free.
- 2026-08-08, 6.4, the FeatureRecord: sixteen fields, one typed row per second, honestly nullable everywhere a gate can refuse, with a runtime schema that refuses NaN, infinities, negatives and missing keys while tolerating future extras. Surprise: SPEC.md had carried a three-field FeatureRecord contract since day one that nobody, including the staff engineer, had read since Phase 0, and the review caught the built type contradicting the founding document. A contract nobody consults is a fossil; SPEC is amended and back on duty.
- 2026-08-08, 6.5, the demo score: one hundred minus four named penalties, caps summing to exactly one hundred so the ladder's contributions-sum-to-score check is integer arithmetic with no clamp, weights documented and defended (PERCLOS forty, closures thirty, blink shape fifteen each) as the baseline Phase 7's learned models must beat. Surprise: the first draft priced its ramps from the literature and so charged an awake person up to fifteen points for blinking normally, because this instrument's own documented normal ranges sit inside where the literature puts drowsiness. Every floor is now priced above what our own manual calls normal, guarded by a regression test built from the owner's resting numbers scoring exactly one hundred.
- 2026-08-08, 6.6, the contribution panel: the score's arithmetic shown rather than hidden, top three drivers by points with ties broken by the scorer's own declaration order so the list cannot flicker, plus two honest sentences for the empty case and the unavailable case. Surprise: the panel deliberately breaks the 6.5 identity on screen, showing three of four contributions, and the right resolution was choosing legibility for the display while keeping the exact sum in the tested core where the export and the analysis will use it.
- 2026-08-08, 6.7, the CSV export: sixteen columns, RFC 4180 quoting, nulls as empty fields, CRLF endings, and a column list tested against the record's own field set so a new field cannot silently miss the file. Surprise: the type system catches a wrong column name but not a MISSING one, so the load bearing check had to be a test comparing the exported column list to the keys of a real record, not a clever type.
- 2026-08-08, 6.8, the KSS self report: the project's first label, nine published anchors held in code as data so the interface cannot soften them, whole steps only, and a refusal recorded as skipped rather than rounded to a middle value. Surprise: the shape mattered as much as the value, a per-session answer written as a per-second column would have let a model treat one report as three thousand independent observations, so it goes above the header as comment lines instead.
- 2026-08-08, 6.9, the permanent notice: the demo disclaimer moved from a parenthetical and a document nobody opens into a tested constant, a banner above everything that cannot be dismissed, and the README, with an end to end assertion that it is visible before any camera permission and survives interaction. Phase 6 complete. Surprise: the interesting part was treating a disclaimer as a shared value with one source of truth, because three copies of a promise are three promises and the one that drifts is the one someone reads.
- 2026-08-08, layout, second attempt: the zone experiment (#134) reverted at the owner's call and replaced with their own design, a full width graph strip at the very top and everything else in one 1200 pixel centred column. Surprise: setting display block inline on the graph canvases to stack them silently defeated the hidden attribute, so three empty strips occupied the top of the page before the camera started, a reminder that an inline style beats the user agent rule that makes hidden work.
- 2026-08-08, layout tweaks: the demo notice moved out of the centred column into a full bleed bar under the graph, centred, unbold, on a neutral grey; the mirror toggle joined the camera resolution on one line; eyelid dots changed from green to white; and the body margin was removed so the strip and the notice truly reach the window edge. Surprise: none technically, but the notice is now visually quieter than 6.9's brief asked for, which is recorded rather than hidden.
- 2026-08-08, fix #122, the stale window: PERCLOS refuses once its newest sample is more than two seconds old, because a rolling ratio over a shrinking denominator drifts, and with the face absent the open samples age out first and the closed share climbs on its own. Surprise: the owner found this by reading an exported CSV, where PERCLOS rose from 20.5 to 22.1 percent across eight rows that all recorded faceDetected as false. One of the new tests deliberately proves the drift rather than preventing it, so the reason for the rule cannot be forgotten.
- 2026-08-08, fix #126, a ceiling for the ratchet: the baseline may rise but never above 1.4 times its window's median, because the p90 it is built from is exactly what a brief excursion moves while the median is not. Surprise: the real lesson was the abandoned first attempt, which tried to fix the inflated durations downstream and was destroyed by review; a measurement corrupted by its own threshold cannot be repaired below that threshold.
- 2026-08-08, 7.1, the second language: an /analysis folder pinned with uv (interpreter and packages from one lockfile), ruff and pytest, and a separate CI job so the two languages fail independently. The one real test asserts the CSV column contract by reading the TypeScript source from the Python side, proven by adding a fake column and watching it fail. Surprise: the machine only had macOS system Python 3.9, already end of life and unsupported by current pandas, so the first act of the evaluation track was refusing to build it on a cracked foundation.
- 2026-08-08, 7.2, the loader and the first picture: load_session refuses seven malformed shapes by name, recovers the sleepiness labels from the comment block pandas would otherwise skip, and keeps empty cells as NaN rather than zero; plot_session draws aperture with both threshold lines, PERCLOS and blink rate on one time axis. Surprise: the first plot of a real session immediately showed amendment 5's frozen shut line as a flat dotted line under a blink line that visibly steps up twice, a design argued in prose for two increments and drawn in one afternoon.
- 2026-08-08, 7.3, the gate said no: eleven agents across two passes assessed roughly forty public datasets, and none clears face video, a drowsiness label, per-clip subject identity and a usable licence at once; DATASETS.md records every rejection with its reason, and rows 7.4 to 7.7 are held for replanning under amendment 7. Surprise: the failure is structural rather than bad luck, because the openly licensed drowsiness data is physiological or still images while every video corpus with a real sleepiness label is gated, unlicensed, or non-commercial, and the anonymisation that would free the video is exactly what destroys the subject identity a leave-one-subject-out split needs.
- 2026-08-08, 7.0, video upload mode: a recorded clip runs through the same pipeline as the live camera, driven by requestVideoFrameCallback and timed by the clip's own media time; the CSV now records source and clip name, and SPEC.md's timestamp rule is amended because it was true only of camera sessions. Surprise: my first version was built on a false premise and adversarial review took it apart, because video.currentTime is interpolated during playback rather than quantised to decoded frames, so the duplicate gate I wrote rejected nothing and my unit test passed only because I fed it hand-quantised values the real source never emits. Review also found the clip playing before the model loaded (three and a half seconds of every recording lost), the sleepiness question still firing on export for a clip, and a stale rolling buffer that the new backwards clock turned from harmless into a clip's first blink shape being measured from the previous camera session.
- 2026-08-08, 7.4, stepping instead of watching: a clip can now be walked one decoded frame at a time, pausing for the pipeline on each, so the result is a property of the file rather than of the machine, and the export records the mode, the frames measured and the resulting rate. Surprise: the pause has to happen inside the frame callback synchronously, because pausing after an await lets several more frames present in the gap and the careful stepping silently degrades back into the sampling it replaced. Also a correction made the same day: the 3230 ms per frame I first quoted was the FIRST inference, the model initialising lazily, read off a rolling mean computed from one or two samples; steady state measured 73 ms per frame, so a watched clip is capped near 13 measured frames per second rather than being 19x slow.
- 2026-08-08, fix #145 again, seeking instead of playing: the stepper no longer plays the clip at all, it seeks to each frame, because pausing inside the frame callback cannot outrun a video advancing in real time. Surprise: the owner's own recording exposed it, a constant 60 fps clip of 12,626 frames measured as 6,655 and reported as "every frame", and the shortfall tracked how busy their machine was; the old test passed only because its clip ran at 10 fps, where nothing could slip through the gap. Also recorded: I blamed their camera and their lighting before measuring the file, and both were wrong.
- 2026-08-08, Safari fix: a stepped clip measured zero frames in Safari while measuring every frame in Chrome, because Safari does not fetch a video's data until something asks and a stepped run never plays; the element now preloads and waits for canplay, and a finished seek is detected from either the frame callback or the seeked event. Surprise: the deeper fault was that "Measured 0 frames" was printed as a result rather than a failure, the same mistake as "measured every frame" in the opposite direction. WebKit has joined the test matrix for clip tests, and reproduced the failure against the pre-fix build before confirming the fix.
- 2026-08-08, the boxed layout: three tiers instead of a flat list, with the alertness score as the only large text, Blinks/Eyes/Gaze side by side beneath it, and Session/Instrument last in smaller grey type; whole boxes hide rather than individual readouts. Surprise: the page had never set a background or text colour, so on a Mac in dark mode it rendered black text on a black background, and six phases of screenshots went past without either of us noticing because it was just legible enough to squint at. Also caught two readouts that appeared before any session started, one of them a frame counter reporting the monitor's refresh rate as the instrument's.
- 2026-08-09, the corpus day, part one, the export that deleted its own evidence (#172, #173): BLINK_LOG_CAP was 50 and fed a fixed length list that dropped the oldest entry whenever a new one arrived, and the same list was both the on screen panel and the exported record, so a display limit silently truncated the measurement; three clips exceeded fifty detections and lost their opening stretches, 63 rows of which 54 were real blinks. Split into BLINK_LOG_DISPLAY_CAP and BLINK_LOG_RECORD_CAP, with a WARNING header when rows are missing. Recorded on 2026-08-15 from the pull requests: recall moved 69.6% to 82.8% on that fix alone, and the published number had been measuring the defect.
- 2026-08-09, the corpus day, part two, refuse to measure a server we did not build (#184): a leftover preview server holds port 4173, strictPort makes the new one exit rather than take it, and curl still answers 200 from the OLD bundle, so a corpus run finishes and hands back a confident wrong number. The runner now compares the built bundle filename against the one the server actually serves and refuses on a mismatch. Recorded on 2026-08-15: this trap had already produced a fake 69.1% from code that was, by then, fixed, and it cost a day.
- 2026-08-09, the corpus day, part three, a clock derived from the clip (#189): the face model was handed a wall clock reading and uses the gap between readings to follow a face, so how busy the machine was leaked into the measurement and the same clip gave two different answers. Timed by the clip's own clock now. Recorded on 2026-08-15: this is what made the number REPEAT, byte for byte across three runs, which no earlier figure on the page had ever done.
- 2026-08-09, the corpus day, part four, an eyelid cannot blink twice that fast (#190): a closure is not counted if it ends within 150 ms of the previous counted blink. It removed 39 false alarms at zero cost to recall. Recorded on 2026-08-15: the constant STAYS at 150 and was not tuned upward, because a constant chosen to improve a score on a benchmark already read is fitting rather than measuring, and 72 false alarms remain on the record instead.
- 2026-08-09, the corpus day, part five, the number published with its own history (#191, #170, #173): 87.7% recall, 83.3% precision, 85.4% F1, printed beside the two retired figures rather than replacing them, because a project that shows only its final answer tells you less than one that shows the road. DROZY's written permission was recorded the same way as UTA-RLDD's. Recorded on 2026-08-15.
- 2026-08-09, the corpus day, part six, four rounds of correcting the page (#180, #181, #182, #183): the evidence behind issues #174 to #179 moved into the repository, three wrong statements and their copies were corrected, four README claims each got a source, a rule or a deletion, and four more statements true the day before were retired. Recorded on 2026-08-15: the pattern that day was that every fix to the measurement made some published sentence false, and the sentences were chased four separate times rather than once at the end.
- 2026-08-09, the interface pass (#167, #171, #185, #186, #187, #188): a permanent banner and two column sleepiness grid, one column rule, a shorter top bar and a footer, then the traces cut short by the same cap as the aspect ratio, and traces bounded by their window rather than by a guessed count. Recorded on 2026-08-15: two of these were the same defect wearing different clothes, a display cap standing in for a measurement bound, which is the export cap of #172 in another file.
- 2026-08-09, refuse an unknown frame rate instead of guessing 60 (#169), and the throughput correction (#168): the page had been assuming 60 frames per second when it could not read the real one, and the recorded throughput was corrected from a debugger measurement of 8.4 frames per second to about 58, measured end to end. Recorded on 2026-08-15: the wrong figure had made a full corpus run look like an overnight job and DROZY like a twenty hour one, and the evaluation plan was nearly cut down around a cost that did not exist.
- 2026-08-09, the DROZY analysis, pre-registered (#194): the analysis code, its pre-registration, MODEL_CARD.md and ARCHITECTURE.md, with the plan committed in its own commit carrying no results, so the result could later be checked against a plan that could not have been written to fit it. Recorded on 2026-08-15: the measured run behind the published correlations dates from this night, and PR #225 changed the blink shape window three days later, which is why README and docs/drozy-result.txt now name the measuring commit.
- 2026-08-10, fix, the export the privacy rule forgot: `.gitignore` refused the per-second session CSV but never the blink log added at 6.7, so a user's exported blink events could have been committed to a public repository. One pattern, plus a test that reads the filenames main.ts downloads and asks `git check-ignore` about each rather than re-implementing git's globs. Surprise: five auditors checked the privacy constraint and all five checked only what leaves over the network; the gap was found by an agent whose sole job was to ask what the audit had not looked at, and it took one command.
- 2026-08-10, fix, the telemetry claim: six places said the page sent nothing anywhere, including the notice printed on the page, and all six were false. The vendored MediaPipe bundle posts usage statistics to Google about sixty seconds after the face model loads, needing no detections; no video, image, landmark or measurement is in it. All six corrected, ADR-0004 written, and the retired phrases are now banned by a test so they cannot come back quietly. Surprise: this was the most checkable claim in the repository, falsifiable by opening the network tab for one minute, and six documents asserted it while no test did. ADR-0002 was not edited, only pointed at its successor.
- 2026-08-11, fix, the stale prose claims (remediation A1): the reproduction command pointed at the retired dataset folder and printed 82.8% against the 87.7% headline, the withdrawn-glasses paragraph printed the superseded run's split while calling it corrected, "planned and not built" survived twenty-eight lines below the paragraphs describing it built, a citation's 45 belonged to a different run, and three documents carried two different test counts, 442 and 461, neither the suite's. All fixed against docs/eyeblink8-result.txt, and tools/resultGuard.mjs now fails the build when a summary document stops agreeing with that file. Surprise: the guard's first failure was its own author, before anything was committed: adding its nine tests made the just-written count of 486 stale at 495, and the local gates went red until the documents were bumped.
- 2026-08-11, fix, EAR measured in pixel space (remediation A2): the displayed eye aspect ratio was computed on normalised coordinates, reading 1.8x the standard definition on a 16:9 camera and falling 27% under roll on a quantity SPEC.md declared invariant; eyeAspectRatio now converts through the shared toPixels in geometry.ts, the tilt test runs at 1280x720 where the defect cannot hide, and the old formula is pinned as a second counterfactual. Surprise: the proof had been green since Phase 3 because it ran on a square frame where the anisotropy cancels algebraically, and the warning comment about this exact trap sat in aperture.ts, one file away from the function that fell into it.
- 2026-08-11, fix, dated stamps (remediation A3): README.md and STATE.md now carry the stamp that saved MODEL_CARD.md in the audit, and the rule is enforced, not remembered: when a stamped file changes its stamp must change with it, checked against the last commit that touched the file, with CI fetching full history because a depth-one checkout cannot answer who touched what. Surprise: while adding the stamp, STATE.md's "Last commit" header was found stale AGAIN, two merges after being corrected in A1, which is the whole argument for the stamp in one sentence.
- 2026-08-11, fix, third party attribution (remediation A4, Stage A complete): the published page redistributes Google's MediaPipe library, WASM and model under Apache-2.0 and the built output carried zero attribution; public/THIRD_PARTY_LICENSES.txt now ships in dist with the canonical licence text, unit tests hold its claims to the repository and an end to end test fetches it from the served site. Surprise: the first draft pointed readers at /LICENSE, a path the published page does not serve, a false claim inside the very notice meant to fix a compliance gap.
- 2026-08-11, fix, the frame counter that ran before the model existed (remediation B1): framesMeasured now increments only after the model call returns, so a cold start no longer writes thousands of phantom frames into the export header; a hidden probe and three end to end tests pin the counter from both sides, and a stepped run that measured nothing refuses instead of summarising. Surprise: the first repair replaced the false success with a false diagnosis, "stepped at the wrong interval, the exported file is correct", every clause wrong beside a disabled export button, and it took two independent review agents to see that a refusal needs the same scrutiny as a result; a third agent then showed the refusal could fire into a successor run's session, which the new source-run token now forbids.
- 2026-08-11, fix, the failure that had no state (remediation B2): a failed model load now enters modelFailed, a readable message with a "Retry loading the model" button; the camera stops with the session, the clip path refuses by name before the first seek, and retry re-enters through beginCamera so a meanwhile-dead camera gets its own honest state. Surprise: the new state immediately broke two older promises, B1's blocked-model test relied on the failure staying silent so its scenario became a stalled download instead of an aborted one, and the corpus runner knew only one failure state and would have parked forever on the second, the same trap it had escaped one increment earlier.
- 2026-08-12, fix, the loop that died in silence (remediation B3): a throw in any frame driver now reports once and stops for good, as the new measurementFailed state carrying the error's own words, with recorded data still exportable and a dead display loop refusing new camera sessions instead of freezing them; all four calibration storage operations are guarded, a failed profile store surfaces on the calibrate button. Surprise: the audit-prescribed test caught a real defect before it was ever mutated, the render pass force-disabled the export buttons the crash message had promised, and review then found the third frame driver, the stepped walker that is the clip default, still funnelling mid-run crashes into "this file is broken" while revoking the exports.
- 2026-08-12, fix, the window that remembered the wrong blink (remediation B4): the blink shape window now starts no earlier than the previous blink's end, read from the reducer's own pre-step memory, so a quick second blink can no longer be published with its predecessor's closing velocity; the audit's synthetic two-close-blinks trace is the fixture, with a counterfactual pinning the old contamination bit for bit. Surprise: the correction needed no new state, and my first version added some anyway, a module variable duplicating a field the blink reducer had carried since Phase 4 under the very same name; review deleted it and the fix got smaller.
- 2026-08-12, fix, the button that came back for nobody (remediation B5, Stage B complete): entering the running state now recomputes the heatmap button from the stored calibration profile, so a returning visitor reaches the heatmap and the scanpath replay again instead of being silently locked out of increments 5.9 and 5.10 by the off-duty force-off. Surprise: the review skeptic proved my negative test green against an always-enabled rot five runs out of five, because it sampled before the session was running; absence assertions mean nothing until the moment the thing would appear has provably arrived.
- 2026-08-12, fix, the constants nothing was holding (remediation C1): a mutation census found pitch, roll and yaw unpinned in both directions and the 30 second learning window cuttable to 1 second with the suite green, because the old tests derive their inputs from the constants and move with them; literal-valued tests now pin all three pose axes at their exact limits, the learning boundary at exactly 30,000 ms, and the missing learningSecondsLeft behaviour, and the whole 23-bend list is runnable as tools/mutationCheck.mjs. Surprise: the first draft of the checker itself committed the project's recurring sin, printing "all mutations caught" when the test runner was replaced with a nonexistent command, because any nonzero exit read as a red suite; it now proves the tree green first and tells a failing suite apart from a runner that could not run.
- 2026-08-12, fix, the control that could not testify (remediation C2): the DROZY shuffled-null control now has its own test, a planted near-perfect signal that the shuffle must destroy into a band bounded from both sides, and the starved branch that answered (0.0, 0.0) on fewer than three pairs now raises by name; both proven red by mutation, shuffle disabled and zero restored. Surprise: the skeptic stubbed the whole null to zero and watched the first version's ceiling-only assertions pass, chance reported as producing nothing at all, so the test gained floors, seed-scanned across 200 alternatives to sit far from flake.
- 2026-08-12, fix, the clock the pure functions never checked (remediation C3, closes #107, Stage C complete): the five reducers the issue names ignore a frame stamped earlier than the newest timestamp their own state carries, so a backwards reopen can no longer record a negative blink or closure duration, and ongoingClosureMs answers null rather than a negative; all five guards proven red by removal, the strict boundary pinned by an equal-stamp test. Surprise: the skeptic proved the guards weaker than their own comments claimed, an open state carries no timestamp so disorder among open frames is still accepted, and the honest repair was wording, a smaller promise kept and stated rather than a five-module shape change bought against a scenario the frame door already prevents.
- 2026-08-12, fix, the number that impersonated the camera (remediation D1, stage one of two): the frames-per-second readout is relabelled "Processing rate", because it is the animation pace live, where a 20 fps camera read as 70, and the label is mode-aware, saying "the instrument's pace, not the camera's" for the live camera and "on the clip's own clock" for clips; SPEC's fps field and MODEL_CARD's 25 fps sentence now carry the same honesty, and stage two, wiring a true camera rate into the blink gate, stays held for a quiet day on real hardware because it will refuse sessions that succeed today. Surprise: the first draft's disclaimer was exactly backwards for clips, where the number rides the media clock and IS the source's rate, so on a 15 fps DROZY recording the page would have disowned the one number it was actually measuring correctly.
- 2026-08-15, docs, the number that outlived the code that made it (NEEDS-REVIEW section 3): the DROZY result file and the README now name the commit the correlations were built from, bd2a98d on 9 August, and record that PR #225 changed the blink shape window three days later, so closing velocity, blink amplitude and amplitude over velocity were measured by superseded code while blink duration, long closures, blink rate and PERCLOS were not; tools/drozyGuard.mjs requires the caveat only while git says src/core/blinkShape.ts has moved since that commit, so re-measuring retires the warning without anyone remembering to. Surprise: the first guard searched the whole README and was therefore unkillable, because the feature list names "closing velocity" two hundred lines above the table, so deleting the entire caveat left the suite green; scoping it to the caveat block turned all three mutations red.
- 2026-08-15, fix, the data the page kept without ever mentioning it (remediation E3, Stage E closed but for the licence ruling): a "Stored on this device" box lists both localStorage keys from core/storedData.ts, what each holds and why, and erases them behind a two-click confirm; the erase clears the in-memory calibration profile too, so the heatmap returns to "calibrate first" in the same click, and the confirmation is built from a re-read of the storage rather than from removeItem not throwing. Surprise: my first draft labelled the disabled button "nothing stored" whenever the browser refused to be read, so a locked-down Safari would have been told its device was clean by a control whose whole job is that sentence; the two disabled states mean opposite things and now say so, and the end to end test had to start a camera before asserting the heatmap went dark, because off duty that assertion passes against a fix that never cleared the profile at all.
- 2026-08-15, fix, the document that could not be wrong (remediation F3): tools/uiGuard.mjs reads every box("...") heading out of src/main.ts and holds docs/UI.md to them in both directions, so an undocumented box and a documented box the page no longer builds are each a red build; the file had already drifted and all three faults are corrected, five boxes described where there are eight, Session in a tier of its own, and an "Instrument" section for what is now the footer of Live signals. Surprise: the guard nearly certified the drift it was written to catch, because box() is called both on one line and across several and my first pattern only matched the single-line form, finding five of the eight and reporting success.
- 2026-08-15, fix, the convention that lapsed because it was only a convention (remediation F1, closes #108): fifteen backfilled log lines for the two real gaps, increments 5.6 to 6.1 on 4 August and the whole of 9 August with its twenty pull requests, each marked with the date it was written rather than passed off as contemporaneous; and the Definition of Done becomes a mechanism, tools/checkLearningEntry.mjs failing any pull request that changes src/ without touching LEARNING.md or writing "No LEARNING entry: <reason>" in a commit message. Surprise: REMEDIATION.md had the gap in the wrong place, recording it as 8 to 10 August when both those days are fully logged, because the item was written from a memory of when things went quiet rather than from the file, which is the same failure as the one being fixed, one level up.
- 2026-08-15, 8.6 and 8.7, two numbers that can only move one way: a coverage floor on src/core (statements 98, branches 95, functions 100, lines 98, against a measured 98.61 / 95.52 / 100 / 98.57) running as its own CI step, and a 240 kB ceiling on the built JavaScript checked after the build against 217.6 kB actual; both proven able to fail, the floor by raising it to 99.9 and the ceiling by dropping it to 100 kB. Surprise: the two guards want opposite amounts of slack, the floor set tight at exactly today's 100 percent for functions because an uncalled pure function is an unchecked one, and the ceiling set deliberately loose because it is written against the single commit that bundles the 3.7 MB model rather than against creep, and a budget that fails on ordinary work gets raised without being read.
- 2026-08-15, 8.3, the changelog written seven releases late: CHANGELOG.md covers v0.1.0 through v0.7.0 plus an Unreleased section for everything since, which is the project's largest gap between releases because the August audit landed in it; every released version says out loud that it was summarised after the fact from its tag, its pull requests and docs/log.md. CONTRIBUTING.md lands alongside it. Surprise: the row asks for a v0.8.0 release too and that is the half deliberately NOT done, because 8.8 is still open and tagging a phase closed before it is would be precisely the kind of claim this project has spent a week withdrawing.
- 2026-08-15, the ladder closed out: the owner answered the two questions the audit had parked, permission for the Eyeblink8 derived table was sought and granted so E4 closes and the withholding rule is retired, both committed human-data fixtures are recorded as the owner's own, and every remaining item that was tidying rather than blocking is marked DECLINED in writing. Surprise: the fix for a backlog that felt endless was not doing the work, it was inventing a marker for "no", because nothing in this project had ever been formally declined and so every deferred item returned in the next audit wearing a new number.
- 2026-08-15, fix, a measurement without its conditions is not a measurement: the CSV export now carries the camera's name, negotiated resolution and declared frame rate, the facing mode, user agent, core count, viewport, screen and orientation, plus the session's own observed duration, record count, face-detected fraction, median iris width in pixels and visibility changes; a Mark button writes timestamped anchors so a validation protocol's ground truth can be located without asking the instrument where it is. deviceId is deliberately not collected, being a fingerprint rather than a measurement. Surprise: the end to end test found the device block missing from every camera export, because I read it before resetSession() and the reset wiped it, and the file looked entirely healthy while carrying none of the thing the increment was for.
- 2026-08-16, fix, the button that looked broken because it was polite: every export outcome now says what happened, from core/exportStatus.ts, and the sleepiness panel scrolls itself into view when it opens. The first Export CSV click on a camera session has always opened the sleepiness question and returned, writing the file only once it is answered; nothing on the page said so, and clicking again just reopened the same question, so from outside it was a dead button. Surprise: the increment before this one caused it, by inserting the new Marks line between the export button and the question and pushing the question further down a box that can already run past the fold, which is the second time in two days that adding a readout moved something load-bearing out of sight.
- 2026-08-16, fix, the pixel that did not say whose (before the six-person round): median_iris_width_px is measured against the video the model reads rather than the 640-wide display canvas, and travels with a measurement_frame line so the pair is unambiguous; startCamera now asks for 1920x1080 instead of 1280x720, falling back on its own wherever the camera cannot. Surprise: the field misled its own author within a day, because I read 10.1 canvas pixels as the real resolution, computed that the blink threshold lived in 2.6 pixels of eyelid travel, and had the owner move closer to the camera on the strength of a factor of two I had introduced myself; aperture in millimetres is a ratio and survives any display scale, so the canvas cap costs the measurement nothing.
