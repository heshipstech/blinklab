BLINK DETECTION vs Eyeblink8 ground truth

8 clips, 408 annotated blinks, 406 detected

  Recall     83.6%   (341 of 408 found, 95% interval 79.7 to 86.9)
  Precision  84.0%   (65 invented, 95% interval 80.1 to 87.2)
  F1         83.8%

Per clip
  clip                   gl   true  found  miss  false   recall     prec
  26122013_223310_cam      -    38     28    10      5    73.7%    84.8%
  26122013_224532_cam      -    88     80     8     10    90.9%    88.9%
  26122013_230103_cam      -    65     43    22      2    66.2%    95.6%
  26122013_230654_cam      -    31     23     8      0    74.2%   100.0%
  27122013_151644_cam      -    30     26     4     14    86.7%    65.0%
  27122013_152435_cam      -    41     36     5     19    87.8%    65.5%
  27122013_153916_cam      -    72     67     5     11    93.1%    85.9%
  27122013_154548_cam    yes    43     38     5      4    88.4%    90.5%

Split by glasses
  with glasses    1 clip(s), recall 88.4%, precision 90.5%
  without         7 clip(s), recall 83.0%, precision 83.2%

Coverage
  26122013_223310_cam    measured   15784, annotated   15783
  26122013_224532_cam    measured   11182, annotated   11182
  26122013_230103_cam    measured    9216, annotated    9216
  26122013_230654_cam    measured    5405, annotated    5404
  27122013_151644_cam    measured   10663, annotated   10663
  27122013_152435_cam    measured    5134, annotated    5134
  27122013_153916_cam    measured    9077, annotated    9077
  27122013_154548_cam    measured    4895, annotated    4895

--- END OF THE CURRENT RUN ---

Everything above this line is the block tools/evaluate_eyeblink8.py
generates, and it is the only part of this file tools/resultGuard.mjs
reads the published figures from. Everything below is history: how to
reproduce the run, a second machine's, superseded runs kept for the
record, and the measurement ratchet.

The line exists because four blocks in this file carry a headline in
the same shape, and the parser used to take the first one that
matched. That is correct only while the current block keeps matching:
roadmap 10.10c1 adds a confidence interval inside the parentheses of
the recall and precision lines, and at the next regeneration the old
pattern would have skipped the block above and read the 20 August one
instead, publishing precision 81.4% and 78 invented in place of 84.0%
and 65 while recall stayed right, because that superseded block
carries an identical recall line. Roadmap 10.0b2, found by widening
the detector ratchet in 10.1g4.

When a corpus run regenerates the block above, paste it in above this
line and leave the line where it is.

THE LEARNING WINDOW OF EVERY CLIP, A CONDITION OF THE NUMBERS ABOVE

Roadmap 10.13b, decided 9 September 2026 and kept deliberately: for
the first ~29 rows of every clip — about the first thirty seconds,
while the passive baseline is still learning — blinks are counted
against the fixed 4 mm line (BLINK_APERTURE_THRESHOLD_MM), a constant
from one face, and against the person's own line only after. The
run's own exports say so per row: blinkLineSource reads `fixed` for
those rows and `passive` after (docs/line-in-the-export.txt, the
9 September re-score). The counts above INCLUDE those windows; the
decision to keep them, the alternatives weighed, and the predicted
zero delta are in docs/learning-window.txt. The demo page states the
same condition while its own learning window runs.

HOW TO REPRODUCE THIS EXACTLY

    node tools/measure_corpus.mjs \
        "$DATASETS/eyeblink8-mp4" \
        "$DATASETS/eyeblink8-measured-2026-09-09"

    cd analysis
    PYTHONPATH="$PWD" uv run python tools/evaluate_eyeblink8.py \
        "$DATASETS/eyeblink8/eyeblink8" \
        "$DATASETS/eyeblink8-measured-2026-09-09"

Until 8 September 2026 this section's evaluation command read the
2026-08-21-rearm measured folder, and for one day it read the
2026-09-08 folder, whose run a transient made unrepresentative (the
dated section below); the correction notes that follow were written
about the earliest of those commands. One caution the transient
earned: a fresh measurement reproduced the block above in five runs
of six, and the sixth is the recorded transient, so a re-measure
that disagrees on one clip should be re-run before it is believed.

A CORRECTION TO "EXACTLY", 24 August 2026. The heading above
promised an exact reproduction and did not deliver one. The
evaluation command is complete, but the folders it reads were not
reproducible from this repository: the measured folder is generated
by tools/measure_corpus.mjs, which reads flat .mp4 files, and the
public dataset ships nested .avi files — the conversion between
them was committed nowhere, so the command reproduced only on the
one machine that already held the prepared folder, and that
machine's conversion settings are lost with it (issue #309). The
preparation step is now committed as docs/eyeblink8-preparation.txt.
The first reproduction attempted from a rebuilt folder is
docs/eyeblink8-m5max.txt, whose prediction was committed before its
run and names the cost of the loss: re-encoded pixels need not
match the original run's re-encoded pixels. The heading stays as
written, because wrong answers stay published here.

A SECOND MACHINE, 25 August 2026, AND WHAT IT DOES TO THE TABLE
ABOVE. The corpus was measured again on different hardware, against
a prediction committed before the run (docs/eyeblink8-m5max.txt).
The same code, the same committed face model, the same pinned
runtime and the same frames — coverage identical on all eight clips
— produced a different table:

                        old machine        M5 Max
  Recall                83.6% (341/408)    85.0% (347/408)
  Precision             84.0% (65 made)    96.4% (13 made)
  F1                    83.8%              90.4%

  with glasses    1 clip(s), recall 90.7%, precision 97.5%
  without         7 clip(s), recall 84.4%, precision 96.2%

  per clip, found and false alarms
  26122013_223310_cam    28 ->  29     5 ->  5
  26122013_224532_cam    80 ->  80    10 ->  1
  26122013_230103_cam    43 ->  43     2 ->  1
  26122013_230654_cam    23 ->  23     0 ->  1
  27122013_151644_cam    26 ->  29    14 ->  1
  27122013_152435_cam    36 ->  37    19 ->  3
  27122013_153916_cam    67 ->  67    11 ->  0
  27122013_154548_cam    38 ->  39     4 ->  1

    node tools/measure_corpus.mjs \
        "$DATASETS/eyeblink8/eyeblink8" \
        "$DATASETS/eyeblink8-measured-m5max"

THE CAUSE IS THE MACHINE, established by measurement rather than by
assumption. The code was cleared by running the published run's own
code on these files: it produced the M5 Max numbers, not these ones.
The files were cleared by a four-point transcode sweep from lossless
to visibly lossy, where the worst disagreement is ONE false alarm
against a gap of fifty-two. The face model is a committed file and
package-lock.json has not moved since the published run's commit, so
the model, its runtime and the browser build are pinned identical.
What is left is the processor, the operating system, and the browser
binary built for them.

THE REPRODUCIBILITY BOUND, which is the number this pair actually
buys. Across two machines, one corpus, identical frames:

  found blinks       341 vs 347, a spread of 6 in 408   (1.5%)
  false alarms        65 vs 13,  a spread of 52         (400%)
  recall             83.6% vs 85.0%,  1.4 points apart
  precision          84.0% vs 96.4%, 12.4 points apart

Recall travels. Precision does not. Stepping every frame removed the
hardware dependence it was designed to remove — which frames are
read — and did not touch the one that decides precision: a false
alarm is near-line flutter, and whether flutter crosses the line is
settled by micrometres of aperture that differ between machines.
Frame determinism was achieved and read as measurement determinism.

WHICH NUMBER THIS PROJECT PUBLISHES. Both, each named with its
machine, because neither is "the instrument's number" and printing
one alone would repeat the mistake this pair exposed. The headline
stays on the old machine's table for one narrow reason, stated
rather than dressed up: the analysis behind this file's miss
breakdown was run on that measurement and not on the new one.
Promoting the M5 Max table means re-running that analysis first,
not preferring the better-looking column. The better-looking column
is exactly the one to distrust.

A CORRECTION TO BOTH CORRECTIONS ABOVE, 26 August 2026. The 24
August note says the conversion between the public download and this
runner "was committed nowhere" and that its settings "are lost". Both
statements are false. analysis/tools/prepare_eyeblink8.py has carried
that step since #158, with its own dependency group in
analysis/pyproject.toml, and it REMUXES rather than re-encodes: the
H.264 bitstream copied byte for byte, the timeline normalised to
zero, the decoded frame count verified on both sides. Nobody looked
in analysis/tools. Issue #309 was filed on the false premise and
docs/eyeblink8-preparation.txt was written to it.

The consequence for the section above is specific and it is not
cosmetic. The second machine measured RE-ENCODED files; the published
run measured REMUXED ones. The transcode sweep offered as clearing
the files swept four re-encodes and never the remux, so "the cause is
the machine" rests on an incomplete elimination and is SUSPENDED
until the committed tool's own output is measured on the second
machine. The pair of tables above is unaffected — both were measured,
both are real — but the CAUSE printed between them is not yet
established. See docs/eyeblink8-m5max.txt for the command that
settles it.

THE SUSPENSION IS RESOLVED, 26 August 2026: THE CAUSE WAS THE
FILES, AND "THE MACHINE" IS REFUTED. The committed preparation tool
rebuilt the corpus by remux on the second machine, and one clip
answered the question by the rule stated before the number existed:
27122013_152435_cam measured found 36, miss 5, false 19 — the row in
the table at the top of this file, DIGIT FOR DIGIT, on different
hardware. The 12.4-point precision gap between the two tables above
was never machines. It was preparations: the second machine's table
was measured from RE-ENCODED derivatives, while this file's table
was measured from the committed remux, and re-encoding collapses
this clip's false alarms from 19 to 3 at every quality from lossless
to visibly lossy. The second table stays published as what it is — a
real measurement of the wrong files, and the discovery of a
preparation sensitivity nobody suspected — but it is NOT an
Eyeblink8 result and the machine-conditionality read of the pair is
withdrawn. The full-corpus remux run on the second machine, scoring
the prediction docs/eyeblink8-m5max.txt committed on 24 August, is
in progress as this is written; its result will be recorded here.

REPRODUCED IN FULL ON A SECOND MACHINE, 26 August 2026. The whole
corpus, prepared by analysis/tools/prepare_eyeblink8.py and measured
on the M5 Max, returns THIS FILE'S TABLE EXACTLY: recall 83.6% (341
of 408), precision 84.0% (65 invented), F1 83.8%, every per-clip
row, the glasses split and every coverage count, digit for digit.
The prediction committed in docs/eyeblink8-m5max.txt on 24 August is
confirmed on its own pre-registered terms. The reproduction spans a
different processor generation, operating system and WebKit binary,
and fifteen merged commits of instrument change — same 406 blinks,
same 341 matches, same 65 false alarms. The number at the top of
this file is now a property of the INSTRUMENT AND THE PREPARED
FILES, measured, on two machines; the caution that survives is that
it is NOT a property of arbitrarily transcoded copies of the same
recordings, which is why the preparation is part of the result.

TWO INSTRUMENT CHANGES IN ONE DAY, EACH MEASURED SEPARATELY

On 20 August 2026 the six-person validation round reported
(docs/validation-round.txt), and two of its findings became code the
same day, each with predictions committed before its own corpus run:

1. THE RULER FROZE (docs/baseline-freeze.txt). The baseline the blink
   line is half of used to rise during a session, and had risen on
   every one of the eight clips, so part of the previous 87.7% recall
   was earned by a line that grew more permissive as each clip went
   on. Frozen at its thirty-second calibration, recall fell to 83.6%,
   inside the predicted band. One prediction was wrong: precision
   FELL, because the frozen lower line exposed near-line flutter
   fragmenting into re-crossings, the very signature the round's P1
   produced live.

2. THE RE-ARM GATE (docs/blink-rearm.txt). After a counted blink, no
   new closure may arm until the aperture rises 10 percent above the
   line, symmetric to the arm line below it. The eyelid must actually
   reopen before it can blink again. Removed 13 false alarms at ZERO
   recall cost, eleven of them fragments sitting on real blinks. The
   refractory period stays at 150 ms: the re-crossings are removed by
   mechanism, not by timer.

This run repeats: measuring one clip three times produces identical
files, byte for byte, a property held since pull request #189.

FIVE PUBLISHED NUMBERS, ALL KEPT

  69.6% recall, F1 77.1   the export deleted its own rows (#172)
  82.8% recall, F1 84.6   export fixed, clock still wobbled (#173)
  87.7% recall, F1 85.4   clock fixed (#189), double counting cut
                          (#190), ruler still moving
  83.6% recall, F1 82.5   ruler frozen at calibration (2026-08-20)
  83.6% recall, F1 83.8   re-arm gate (2026-08-21), 13 false alarms
                          removed at zero recall cost

THE 65 FALSE ALARMS OF THIS RUN

  38 of 65 sit on a real blink under the strict rule, no widening.
  55 of 65 under the four frame tolerance rule.
  54 of 65 are three frames long or shorter.

Still fragments of real blinks, and fewer of them. What remains sits
further from the blink it belongs to than either the refractory
period or the re-arm gate reaches, and no constant will be moved to
chase it: a constant chosen to improve a score on a benchmark
already read is fitting, not measuring.

THE MISS TABLE OF THIS RUN

67 misses, of which 47 carry at least one frame
the human marked fully closed, 70.1%. The table is
docs/evidence/2026-08-21-rearm/eyeblink8_misses.csv. Identical to
the frozen run's misses: the gate removed false alarms and touched
nothing else. The share has barely moved across four runs (78.6%,
72.0%, 70.1%, 70.1%), so what is missed keeps its character: blinks
a human saw fully close, where this instrument's eyelid measurement
does not dip far enough. Issue #179, still the open question.

THE SUPERSEDED RUNS, all kept so every correction can be checked

Ruler frozen, before the re-arm gate (2026-08-20):

  Recall     83.6%   (341 of 408 found)
  Precision  81.4%   (78 invented)
  F1         82.5%

  Its 78 false alarms: 49 strict-rule, 66 four-frame, 64 short. Its
  miss table: 67 misses, 47 fully closed, 70.1%, at
  docs/evidence/2026-08-20-frozen/eyeblink8_misses.csv.

Ruler still moving (the 9 August run, superseded 20 August):

  Recall     87.7%   (358 of 408 found)
  Precision  83.3%   (72 invented)
  F1         85.4%

  clip                   gl   true  found  miss  false   recall     prec
  26122013_223310_cam      -    38     31     7      3    81.6%    91.2%
  26122013_224532_cam      -    88     80     8     12    90.9%    87.0%
  26122013_230103_cam      -    65     50    15      1    76.9%    98.0%
  26122013_230654_cam      -    31     25     6      0    80.6%   100.0%
  27122013_151644_cam      -    30     28     2     15    93.3%    65.1%
  27122013_152435_cam      -    41     37     4     21    90.2%    63.8%
  27122013_153916_cam      -    72     69     3     15    95.8%    82.1%
  27122013_154548_cam    yes    43     38     5      5    88.4%    88.4%

  with glasses    1 clip(s), recall 88.4%, precision 88.4%
  without         7 clip(s), recall 87.7%, precision 82.7%

  Its 72 false alarms: 45 strict-rule, 64 four-frame, 61 short. Its
  miss table: 50 misses, 36 fully closed, 72.0%, at
  docs/evidence/2026-08-09/tables-current-run/eyeblink8_misses.csv.

THE FIRST SUPERSEDED RESULT, kept so the original correction can be
checked:

  Recall     69.6%   (284 of 408 found)
  Precision  86.3%   (45 invented)
  F1         77.1%

  clip                   gl   true  found  miss  false   recall     prec
  26122013_223310_cam      -    38     28    10      6    73.7%    82.4%
  26122013_224532_cam      -    88     49    39      1    55.7%    98.0%
  26122013_230103_cam      -    65     44    21      1    67.7%    97.8%
  26122013_230654_cam      -    31     23     8      1    74.2%    95.8%
  27122013_151644_cam      -    30     26     4      7    86.7%    78.8%
  27122013_152435_cam      -    41     36     5     14    87.8%    72.0%
  27122013_153916_cam      -    72     42    30      8    58.3%    84.0%
  27122013_154548_cam    yes    43     36     7      7    83.7%    83.7%

  with glasses    1 clip(s), recall 83.7%, precision 83.7%
  without         7 clip(s), recall 67.9%, precision 86.7%


RE-MEASURED AT HEAD, 8 SEPTEMBER 2026: ONE CLIP COLLAPSED, AND THE
COLLAPSE PROVED TO BE A TRANSIENT
=============================================================

The regression run row 10.8a promised: the owner's machine, WebKit
through tools/measure_corpus.mjs (Playwright 1.62.1), all eight clips
stepped, at commit 68690bff63cbc3d0d2d7d754f85e219a3c28a4d5. Every
honesty check the run carries passed: measurement_mode "stepped" on
all eight exports, inexact_landings 0 on all eight, no refusals, and
the coverage block identical to the anchor's on every clip, down to
the same two "+1" rows. Seven of eight clips reproduced the anchor
DIGIT FOR DIGIT. The eighth, 27122013_154548_cam — the one clip
whose subject wears glasses — fell from 38 of 43 found to 6, and for
one day this file's current block was that run's table, kept here as
every superseded table is:

  8 clips, 408 annotated blinks, 371 detected

    Recall     75.7%   (309 of 408 found, 95% interval 71.3 to 79.6)
    Precision  83.3%   (62 invented, 95% interval 79.2 to 86.7)
    F1         79.3%

    clip                   gl   true  found  miss  false   recall     prec
    27122013_154548_cam    yes    43      6    37      1    14.0%    85.7%
    (the other seven rows are identical to the block at the top)

THE DIAGNOSIS, 9 SEPTEMBER 2026, with the tools rows 10.8a1-10.8a5
built for exactly this. The per-miss replay of the clip's 37 misses
(analysis/tools/write_miss_table.py, then npm run replay:run) returned
the same verdict on every one: no crossing, no reopen, no re-arm —
the detector was handed nothing. The frame trace says why: a blink
line exists from frame 1 to 1162 and never again, apertureMm is null
on 3,732 of 4,895 frames (a healthy clip has ~50), faceDetected goes
false at second 39 and never recovers, and inferenceMs falls from a
working ~7.3 ms to ~1 ms — the model answering instantly with
nothing, while sceneLum holds steady at 0.53. The face tracker lost
the face once, 39 seconds in, and rode that loss to the end of the
clip.

THE RESOLUTION: IT DOES NOT REPRODUCE. Five re-measurements at the
same commit, byte-identical build (git rev-parse confirmed
68690bff...): four of the clip alone in a fresh browser, and one of
the FULL corpus in the same eight-clips-one-session shape as the 8
September run. All five recover the clip completely — 42 raw
detections, the anchor's exact 38 found plus 4 false — and the full
re-run of 9 September reproduces the anchor table at the top of this
file digit for digit, every row, both splits, all eight coverage
counts. One failure in six runs of this clip at this commit. The 8
September table above is a real measurement of a transient
face-tracking failure, not of the instrument's reproducible
behaviour, and the current block is the anchor, re-confirmed at this
commit.

WHAT THE TRANSIENT COSTS THIS FILE'S OWN CLAIMS. Stepped replay is
not byte-identical run to run, as this project has claimed since pull
request #189. WHICH FRAMES are measured is deterministic — coverage
was identical in every one of the seven runs this clip has now had —
but WHAT THE FACE MODEL RETURNS on them is not: its video-mode
tracker carries internal state that can, rarely, diverge, and when it
loses a face it does not reacquire it for the remainder of the clip.
Recall moved 7.9 points on a one-in-six event. The determinism
sentence is qualified wherever it is published, the prediction
outcomes written on 8 September are re-scored in their own documents,
and the ride-a-lost-face-to-the-end defect is on the roadmap, blocked
on a reproduction nobody can currently force.

TO REPRODUCE THE CURRENT BLOCK, use the two commands under HOW TO
REPRODUCE THIS EXACTLY near the top of this file. The
eyeblink8-measured-2026-09-08 folder holds the transient run;
evaluating it gives the superseded table above, which is exactly what
makes the transient checkable.

THE MEASUREMENT RATCHET, 5 September 2026 (roadmap 10.1a)
=========================================================

Built from commit cc882c7643f4e9426bba73af48d35e81e25ce608 — the
corpus run of 10 September 2026 on the owner's machine, all eight
clips stepped, coverage identical down to the same two +1 rows,
which reproduced the block above DIGIT FOR DIGIT: every per-clip
found and false count, both confidence intervals, the same 341 of
408 and 65 invented. That identity scores and retires the four
caveats below, the ledger of every detector change since the
previous built-from
commit, 68690bff63cbc3d0d2d7d754f85e219a3c28a4d5 — the 9 September
2026 full re-run (WebKit driven by Playwright 1.62.1, inexact
landings 0 on every clip) which itself reproduced the 2 September
anchor 65e8e7ca2a6322c8a71f52caabcf2a189972f956 digit for digit.
Three commits, three runs, one table. The build stamps its commit
only in continuous integration, so the 10 September run's commit
was pinned the way the 9 September one was: git itself, read on the
machine that ran it, with the checkout unchanged since the build.
The 8 September run deviated on one clip through the transient
recorded above; the caveat ledger between the FIRST two anchors is
the retired block below, and the two early-September runs together
are the measurement that retired it.

From this line on, tools/detectorRatchet.mjs holds this file to the
code: a commit that touches a detector source after the built-from
commit turns the build red unless this file carries a dated caveat
naming it — and once a corpus run moves the built-from line, a caveat
still reading "not yet re-measured" is itself a false sentence and
turns the build red from the other side. Retiring a caveat means
editing its phrase to "re-measured on <date>", never deleting the
record.

DETECTOR CHANGED, re-measured on 8 and 9 September 2026 — the runs
recorded above score every entry in this block. All eight clips came
back digit for digit (the 8 September run's one-clip deviation
proved to be a transient of the face tracker, not of any change
listed here), so every "argued neutral" and "proved neutral" below
held. Originally declared 5 September 2026:

  1a6c88f feat: the detector reads a person's own guided blink line
  dce2b87 feat: guided calibration stored form and a validated parse
  bf5bfca feat: guided calibration session state machine, the two
          phases sequenced in core
  fab006c feat: guided blink-line calibration, the pure core

  The guided blink-line work landed after the anchor run. Argued
  corpus-neutral: the personal line engages only when a stored guided
  calibration exists, and a corpus clip run starts with none, so the
  detector should fall back to the learned-baseline path unchanged.
  But argued is not measured, and this caveat stands until the next
  corpus run moves the built-from line — row 10.8's regression run is
  already committed to do exactly that. The ratchet finding a real,
  undeclared staleness in its first hour is the reason it exists.

  6 September 2026, the stepper (ladder A1, docs/stepper-honesty.txt):
  the step is now the mean period across calibration gaps that must
  be whole multiples of the smallest, and a run whose inexact landings
  exceed two in a hundred is refused by name. NOT argued neutral: a
  clip with millisecond-quantised timestamps changes schedule, and the
  pre-registered prediction is a frame count that drops by the
  fabricated excess and a blink count that moves by at most a handful.
  The stepper and its rule were NOT on the ratchet's watch list when
  this paragraph was written, so it was the only record that the
  schedule had moved. Ladder item D4 has since added them, and the
  merged commit it could not name in advance is named below, under 7
  September 2026.

  7 September 2026, the velocity sampling bound (roadmap 10.10c4b,
  ladder B12, docs/velocity-sampling-bias.txt):

    1ab04ed feat: how much of a blink's speed the frame rate takes (roadmap 10.10c4b, ladder B12)

  NOT argued neutral, PROVED neutral, which is a different and much
  cheaper claim. That commit's whole diff to a detector source is 26
  added lines in src/core/blinkShape.ts and every one of them is a
  comment: `git show adbfcb0 -- src/core/blinkShape.ts` shows no
  executable line added, changed or removed. The instrument that
  measured the corpus and the instrument in the tree are the same
  bytes once comments are stripped, so no corpus number can have
  moved.

  The bound itself lives in a new module, src/core/velocityBias.ts,
  which nothing in the detection path imports: it reads blinkShape
  rather than being read by it. That module is deliberately NOT on the
  watch list, because a simulation that measures the detector is not
  part of it, and putting it there would make every future refinement
  of the bound look like a detector change.

  This caveat exists anyway, because the ratchet keys on the file and
  not on the diff, and a guard that can be talked out of firing is not
  a guard. The record is the point: a reader in six months sees that
  blinkShape moved on this date and can check in one command that the
  movement was words.

  That sha was written twice. The first version of this entry named
  adbfcb0, the sha the commit had on its branch, and the rebase merge
  gave it another one — so this file named a commit that does not
  exist, main went red, and the ratchet was right again. Roadmap
  10.10c4c widened the match to accept a commit's SUBJECT as well as
  its sha, which is why the entries below name subjects: a subject
  survives the rebase and a branch sha does not. The line above keeps
  the merged sha now that there is one.

  7 September 2026, the pose bound (roadmap 10.10c4c, ladder B12,
  docs/pose-aperture-bias.txt):

    feat: how much of a published millimetre is head angle (roadmap 10.10c4c, ladder B12)

  Named by subject rather than sha for the reason just given. The
  commit touches two detector sources, src/core/constants.ts and
  src/core/aperture.ts, and its diff to both is comments only: the
  pose limits and the aperture expression are unchanged to the byte,
  and `git show <that commit> -- src/core/constants.ts
  src/core/aperture.ts` shows no executable line added, changed or
  removed. Proved neutral, as above, rather than argued.

  The measurement itself lives in src/core/poseBias.ts, which nothing
  in the detection path imports and which is deliberately not on the
  watch list, for the same reason velocityBias.ts is not: a simulation
  that measures the instrument is not part of it.

  7 September 2026, the line in the export (roadmap 10.13a, ladder A8,
  docs/line-in-the-export.txt):

    feat: a stored line carries its conditions, and a clip cannot borrow one (roadmap 10.13a, ladder A8)

  Named by subject rather than sha, for the reason two entries up.
  This one is NOT comments-only, so the cheap proof is unavailable and
  the argument has to be made on what the executable change reaches.

  Its whole diff to a detector source is in
  src/core/guidedCalibration.ts, and it is three things: two counts
  added to what the resolver returns, a stamp type, and a parseStamp
  the stored-entry parser calls. `effectiveBlinkLineMm`, which is the
  only function in that file the per-frame detection path calls, is
  unchanged to the byte.

  A corpus clip cannot reach the other two. The resolver runs only
  when somebody clicks the calibrate button, and that button is now
  gated on a live camera, so a clip run cannot start a calibration at
  all; the added counts change no line value in any case. The parser
  runs only against something in localStorage, and a corpus clip runs
  in a fresh browser with nothing there, so a stricter parser has
  nothing to be stricter about.

  The same commit also refuses a stored line outright when the frame
  source is a file. That can only move a corpus run further from the
  guided path, never onto it, so it cannot raise a corpus number
  either. Every one of those steps is checkable from the diff, which
  is what separates this from "argued neutral" without quite reaching
  the comments-only proof above.

  The prediction that the corpus is byte-identical apart from the new
  trailing columns was committed BEFORE any of this code existed
  (docs/line-in-the-export.txt), and 10.8a's regression run scores it.
  That is the measurement this caveat is standing in for.

  7 September 2026, the whole chain comes under the watch (roadmap
  10.1g4, ladder D4):

  A published recall is made by four things in series: the detector
  decides where the blinks are, the stepper decides which frames the
  detector ever sees, the runner drives the clips, and the Python
  scorer turns detections and annotations into a percentage. Until
  today the ratchet watched the first and none of the other three, so
  the stepper could be rewritten and the scorer's arithmetic changed
  with this file staying green over a run that described neither.

  Widening the list means every commit since the anchor that touched a
  newly watched file has to be declared here. There are four, and they
  are not equal. Three are shown inert from their diffs. The fourth is
  the stepper and it can move the numbers, which is recorded as a fact
  rather than argued away.

  4e64877 feat: the stepper refuses what it cannot place on the clip's clock (ladder A1)

  THIS ONE CAN MOVE THE NUMBERS, and any other verdict would be the
  hand-wave this file exists to forbid. It touches three watched
  files. tools/measure_corpus.mjs is inert: its whole diff is one log
  line, `summary.slice(0, 62)` becoming `summary`, printed after the
  CSVs are written and read by nothing. The other two are the frame
  stepper, and the stepper decides which frames of a clip are measured
  at all.

  The schedule seeks `origin + (index + 0.5) * step` until the target
  passes the clip's end. The step was the smallest gap over the first
  six frame times landed on; it is now the mean period implied by
  gaps that must be whole multiples of the smallest, over up to
  twelve. On a clip whose container timestamps are exact the two are
  the same number and nothing moves. On a millisecond-quantised clip
  they differ: the schedule shifts, different frames reach the
  detector, and a step short of the period fits an extra target before
  the end. The same commit adds two refusals that end a run rather
  than report it.

  Eyeblink8's clips are 30 frames per second, and whether their
  container timestamps are exact or quantised is exactly what a
  re-measurement would show. The prediction was committed before the
  code, in docs/stepper-honesty.txt, and row 10.8a's regression run
  scores it. That run is the measurement this paragraph stands in for.

  9d8932b feat: every counted proportion carries its interval (roadmap 10.10c1, ladder B8 and B10)

  NOT argued neutral, argued from the whole diff, and then checked by
  running it. One watched file, analysis/tools/evaluate_eyeblink8.py,
  in three hunks: an import, a new `_interval` helper returning a
  string, and two f-strings in `report()`. Loading both versions and
  calling `report()` on the per-clip counts published above gives
  output that differs on exactly two lines, where "(341 of 408
  found)" and "(65 invented)" gain their intervals. Every figure is
  unchanged: 83.6%, 84.0%, 83.8%, all eight clip rows, both glasses
  rows, all eight coverage rows. So this commit changes the TEXT of
  the published block at the next regeneration and no number in it.

  It does, however, arm a trap for whoever pastes that regenerated
  block in, and finding it is the reason this widening was worth
  doing. tools/resultGuard.mjs reads the headline out of this file
  with expressions that end at `found)` and `invented)`. The new
  clause sits inside those parentheses, so after a regeneration
  neither expression matches the block at the top any more, and
  because this file keeps its superseded runs below and the parser
  takes the FIRST match, precision would be read from the 20 August
  block instead: 81.4% and 78 invented in place of 84.0% and 65.
  Recall would stay correct, because that superseded block happens to
  carry the same recall line, so the README would publish a current
  recall beside a superseded precision and every byte comparison
  would stay green, both sides reading the same wrong line. Nothing
  is wrong in the committed file today, which is why this is a
  caveat and not a correction; row 10.0b2 fixes the parser before
  10.8a's regenerated block arrives.

  394f0ff fix: four small pieces of arithmetic (roadmap 10.16, ladder A27)

  One watched file, analysis/blinklab/blink_log.py, in two hunks: an
  added import, and a `_read_metadata` body replaced by a call into a
  new shared module carrying the same loop, with one statement added
  so a repeated metadata key raises instead of the later line quietly
  winning. That metadata is genuinely on the published path, because
  the evaluator skips any clip whose measurement_mode is not
  "stepped". The difference is a refusal rather than a value: the
  anchor run's own files have no repeated keys, so the new raise
  cannot fire on them, and if it ever did the run would stop rather
  than publish a different number.

  b048251 feat: what each reader does when a key is not there (roadmap 10.1f4, ladder D6's reading half)

  NOT argued neutral, PROVED neutral. One watched file,
  analysis/blinklab/blink_log.py, at nine insertions and no deletions,
  all nine between the opening and closing triple quotes of a new
  docstring on the `frames_measured` property. No executable line is
  added, changed or removed. The scorer reads the same bytes it read
  at the anchor.

  7 September 2026, the probe step (roadmap 10.14c,
  docs/stepper-probe-rate.txt):

  feat: the probe step comes from the clip, not from a constant (roadmap 10.14c)

  NOT argued neutral. The stepper's calibration probe stops being a
  constant 10 ms and becomes a quarter of the smallest gap it has
  seen, floored at 2 ms and never above the old constant. That changes
  which instants calibration asks for on EVERY clip, including a 30
  frames per second one, so the cheap proof is unavailable.

  What it cannot change is the answer at 30 frames per second, and
  that is measured rather than argued: the fake-video harness drives
  the real stepThroughVideo over clips with explicit frame times, and
  the 30 and 60 frames per second cases measure the same counts and
  calibrate to the same intervals before and after. The schedule the
  run then walks is derived from the calibrated interval and the
  origin alone; the origin is the first landing, which is frame zero
  either way. So identical calibration means an identical schedule
  means identical frames measured.

  The change is visible only above 200 frames per second, where the
  old constant could not gather enough distinct landings and the run
  measured nothing at all. Eyeblink8's clips are 30, so this is
  expected to leave the numbers above untouched, and only 10.8a's
  regression run can say so. That run now scores TWO stepper changes:
  this one and ladder A1's whole-multiple rule.

  7 September 2026, the same row's comment (roadmap 10.14c):

  docs: the probe comment states what was measured, not what was assumed (roadmap 10.14c)

  ARGUED NEUTRAL, and the argument is short because the diff is. One
  block comment inside src/core/stepCalibration.ts is rewritten. No
  executable line is added, changed or removed, no constant moves, and
  the module exports exactly what it exported. The detector reads the
  same bytes it read one commit earlier.

  What changed is a claim. The comment said "Measured before the fix:
  200 frames per second calibrated as 100, 240 as 120, 300 as 100".
  None of those three numbers was ever measured; they are what the
  mechanism predicts, written in the grammar of a reading. The sweep
  in docs/stepper-probe-rate.txt says something milder: every rate up
  to 200 came out right with the constant step, and 240 and 300
  measured zero frames. The comment now says that.

  7 September 2026, the probe budget (roadmap 10.14d,
  docs/stepper-probe-budget.txt):

  feat: the probe step halves a measured period, and the budget covers the slow end (roadmap 10.14d)

  NOT argued neutral by reach, and PROVED neutral by measurement,
  which is the stronger of the two and is available here because the
  thing that could move is a number a test can read.

  Two changes to the stepper. The calibration probe, once the landings
  reduce under the whole-multiple rule, advances by half of that
  period rather than a quarter of the smallest raw gap. And the
  attempt budget rises from 60 to 120, because the sweep found the
  budget was exhausted at 20 frames per second and below: calibration
  gathered eleven frames at 20 and eight at 15 where the design asks
  for twelve.

  Both change which instants calibration asks for, on every clip, so
  no argument from reach is available. What is available is the
  calibrated interval itself. The fake-video harness drives the real
  stepThroughVideo over clips with explicit frame times at thirteen
  rates from 15 to 300 frames per second, and every one of them
  calibrates to the SAME interval before and after, to the last digit
  a double carries: 0.03333333333333333 at 30 frames per second on
  both sides. Only the number of probes spent reaching that answer
  moved, from 48 to 41 at 30 frames per second.

  Eyeblink8's clips are 30. The schedule a run walks is derived from
  the calibrated interval and the origin alone, and the origin is the
  first landing, which is frame zero either way. An identical interval
  and an identical origin give an identical schedule and identical
  frames measured. Only 10.8a's regression run can confirm it on the
  real clips, and that run now scores three stepper changes: ladder
  A1's whole-multiple rule, 10.14c's probe step, and this one.

DETECTOR CHANGED, re-measured on 10 September 2026, digit for digit by the run at the built-from line. Originally declared 9 September 2026:

  chore(deps): bump eslint, globals, typescript-eslint and vite (lockfile declared)

  One watched file, package-lock.json, moved by four development
  dependency bumps. Three of them — eslint 10.10.0, globals 17.12.0,
  typescript-eslint 8.69.0 — are lint-time tools: they never execute
  in the built page or anywhere on the measurement path, so they
  cannot move a corpus number. The fourth, vite 8.2.1 to 8.2.2, is
  the bundler, and the cheap proof is unavailable for a bundler; the
  argument is from what the diff holds still: every detector source's
  bytes are unchanged, and the two pinned instruments the model card
  reads out of this same lockfile — @mediapipe/tasks-vision 1.0.1
  with its integrity hash, and @playwright/test 1.62.1 — are
  untouched in the diff, checkable with `git show <that commit> --
  package-lock.json`. A bundler repackages the same sources; the
  9 September anchor stands on those sources. The next corpus run
  scores this the way every entry in the retired block above was
  scored, and retires this caveat with it.

DETECTOR CHANGED, re-measured on 10 September 2026, digit for digit by the run at the built-from line. Originally declared 9 September 2026:

  Row 10.10c4d build half: the iris constant carries its citation and spread, stated twice and pinned

  One watched file, src/core/constants.ts, moved — and the whole diff
  to it is a comment: the citation block written above
  IRIS_DIAMETER_MM by roadmap 10.10c4d. The constant's value, 11.7,
  and every other token of executable code in the file are unchanged
  byte for byte, checkable with `git show <that commit> --
  src/core/constants.ts`: the diff adds comment lines and nothing
  else. A comment is stripped before any measurement runs; a diff
  with no executable change has no mechanism by which to move a
  number. Predicted delta: zero, on every clip. The next corpus run
  scores this beside the lockfile entry above and retires both.

DETECTOR CHANGED, re-measured on 10 September 2026, digit for digit by the run at the built-from line. Originally declared 9 September 2026:

  chore(deps): bump vitest to 4.1.11 (lockfile declared)

  One watched file, package-lock.json, moved by one development
  dependency: vitest 4.1.10 to 4.1.11, the unit-test runner and its
  own sub-packages. The runner never executes in the built page or
  anywhere on the measurement path — it is the instrument that checks
  the code, not the instrument that measures the corpus — and the two
  pinned instruments the model card reads out of this same lockfile,
  @mediapipe/tasks-vision 1.0.1 with its integrity hash and
  @playwright/test 1.62.1, are untouched in the diff, checkable with
  `git show <that commit> -- package-lock.json`. Predicted delta:
  zero. The next corpus run scores this beside the two entries above
  and retires all three.

DETECTOR CHANGED, re-measured on 10 September 2026, digit for digit by the run at the built-from line. Originally declared 9 September 2026:

  Row 10.12c build: sample floors demand face-seconds, and duplicates cannot fill them

  Two watched files moved, src/core/baseline.ts and
  src/core/guidedCalibration.ts, by one kind of change in each: an
  ADDED floor condition — a span of trusted-face time derived from
  the existing tick floors at the slowest legal rate — beside floors
  that all stay. A condition only added can refuse births it used to
  allow; it cannot change any output it still allows. On the stepped
  corpus the added floor binds nowhere, argued in the prediction
  committed one commit earlier (docs/face-seconds.txt): every tick
  is a distinct photograph, the clips run near 30 frames per second
  so the credit cap never trims a gap, and every one of the eight
  learning windows held a face far past the 4000 ms span, which is
  how all eight baselines were born on the anchor run. Predicted
  delta: byte-identical. The next corpus run scores this beside the
  three entries above and retires all four.

DETECTOR CHANGED, not yet re-measured, declared 12 September 2026:

  The two eyes must agree before they average, by the measured margin

  One file joins the watch list in the same commit that creates it:
  src/core/crossEyeGate.ts, enrolled in DETECTOR_SOURCES at birth
  because a roster cannot see a source nobody added (the metadata
  guard's enrolment lesson, applied to this ratchet before it could
  repeat). The change it carries is behavioral on the measurement
  path once wired: the per-frame aperture has always been the plain
  mean of the two eyes, and it becomes a REFUSED merge when the eyes
  disagree by more than 1.72 mm — twice the cross-eye p95 the noise
  floor committed (docs/aperture-noise-floor.txt, 0.860 mm), a
  threshold derived rather than chosen and held to that document by
  test. Predicted corpus impact: at or near zero on clean clips,
  because honest disagreement ends at 0.86 mm by measurement and the
  line sits at twice that; any change concentrates on frames where
  ONE eye's landmarks degrade while the pose gate still admits the
  face, and the glasses clip is the named candidate. Whichever way
  it lands, the owner's next corpus run scores this entry and
  retires it the way the block above was retired.
