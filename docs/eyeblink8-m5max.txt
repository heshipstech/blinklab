EYEBLINK8 ON THE M5 MAX: THE PREDICTION, COMMITTED BEFORE THE RUN

Written 24 August 2026, before the corpus has been measured on the
new machine. The dataset was re-downloaded from its public source
(the previous copy is on a machine that is not physically
accessible); the measured outputs are generated fresh, as they
always are. The run this predicts:

    npm run build
    npm run preview -- --strictPort        (in its own terminal)
    node tools/measure_corpus.mjs \
        "$DATASETS/eyeblink8/eyeblink8" \
        "$DATASETS/eyeblink8-measured-m5max"

    cd analysis
    PYTHONPATH="$PWD" .venv/bin/python tools/evaluate_eyeblink8.py \
        "$DATASETS/eyeblink8/eyeblink8" \
        "$DATASETS/eyeblink8-measured-m5max"

THE PREDICTION. The published table reproduces: recall 83.6% (341 of
408), precision 84.0% (65 invented), F1 83.8%, and the per-clip
found/miss/false counts in docs/eyeblink8-result.txt, identically.
The design says machine speed cannot matter: every frame is SOUGHT
on the clip's own clock, the model clock is offset-stable, and the
whole delivered-rate question does not exist for a stepped clip.

THE STATED THREAT TO EXACTNESS, named before the numbers exist so a
deviation cannot be explained after the fact: the face model's
arithmetic may differ across CPU generations and browser builds in
low-order bits, which moves apertures by micrometres, which can flip
a detection that sits exactly on a threshold. So:

  - identical counts CONFIRM the design claim outright;
  - a difference of a FEW blinks on threshold-hugging clips, with
    coverage counts identical, indicates landmark-level numeric
    drift between machines, NOT an instrument change — it would be
    recorded as a bound on cross-machine reproducibility, a number
    this project has never had;
  - coverage counts differing AT ALL (the measured-frames column),
    or counts moving by tens, is a real defect and stops the line.

Whatever happens is appended below this line as a dated RESULT
section, and this prediction is not edited.

ADDENDUM, 24 August 2026, still before the measurement. The first
attempt printed "0 clips to measure": the runner wants flat .mp4
files and the public dataset ships nested .avi files, so the
published reproduce command depended on an undocumented conversion
whose settings are lost with the inaccessible machine (issue #309).
This run therefore RE-CONVERTS, with the settings recorded here:

    ffmpeg -i <clip>.avi -an -c:v libx264 -crf 10 -preset slow <clip>.mp4

one flat .mp4 per .avi, basenames preserved, audio stripped, frame
timing untouched. This adds a SECOND stated threat to exactness,
named before the numbers exist: the original run also measured
converted files, and two transcodes of the same source need not
produce identical pixels, so the "a few blinks on threshold-hugging
clips" outcome now covers transcode variance as well as CPU-level
numeric drift, and the two cannot be separated by this run. What
transcoding CANNOT change: the frame count the stepper visits, so
the coverage columns must still match the annotations exactly, and
a coverage mismatch still stops the line.

RESULT, 25 August 2026. The run happened. The prediction is WRONG,
and wrong in the direction that is hardest to be honest about: the
instrument scored BETTER than the table it was predicting.

  measured on the M5 Max          predicted (the published table)
  Recall     85.0% (347 of 408)   83.6% (341 of 408)
  Precision  96.4% (13 invented)  84.0% (65 invented)
  F1         90.4%                83.8%

Per clip, found and false, published then measured:

  26122013_223310_cam   28 ->  29  (+1)    5 ->  5   (0)
  26122013_224532_cam   80 ->  80   (0)   10 ->  1   (-9)
  26122013_230103_cam   43 ->  43   (0)    2 ->  1   (-1)
  26122013_230654_cam   23 ->  23   (0)    0 ->  1   (+1)
  27122013_151644_cam   26 ->  29  (+3)   14 ->  1  (-13)
  27122013_152435_cam   36 ->  37  (+1)   19 ->  3  (-16)
  27122013_153916_cam   67 ->  67   (0)   11 ->  0  (-11)
  27122013_154548_cam   38 ->  39  (+1)    4 ->  1   (-3)
  TOTAL                341 -> 347  (+6)   65 -> 13  (-52)

COVERAGE IS IDENTICAL. Every clip's measured-frames count matches
the published run exactly — 15784, 11182, 9216, 5405, 10663, 5134,
9077, 4895 — against the same annotated counts. The stepper visited
the same frames, so nothing here is about which frames were seen.
That clause of the pre-decision did not fire.

THE OTHER CLAUSE DID. "Counts moving by tens is a real defect and
stops the line." Fifty-two false alarms disappeared. That is not the
"few blinks on threshold-hugging clips" that landmark-level numeric
drift or transcode variance was allowed to explain, and the shape is
not noise either: the losses concentrate exactly on the clips that
had the most false alarms (19 -> 3, 14 -> 1, 11 -> 0), which is what
a MECHANISM change looks like, not a jitter.

So the pre-registered verdict is applied as written: THIS STOPS THE
LINE. The published table is not reproduced. The new numbers are NOT
published as a replacement, do not enter README, STATE or the model
card, and 83.6 / 84.0 / 83.8 remains the project's standing result
until this is explained. A better number arriving unexplained is
still an unexplained number, and this is precisely the moment a
project stops asking questions because it likes the answer.

WHAT IS ACTUALLY IN QUESTION. Two things changed between the
published run and this one, and only one of them can be the cause:

  1. THE CODE. Thirteen commits touched src/ after the published run
     (003184a, the re-arm gate). Among them, one rewrote every
     timestamp the face model receives (1761e33, the model-clock
     ratchet) and one moved the baseline birth arithmetic into its
     own module (89658cb). Both are supposed to be behaviour
     preserving on a stepped clip. Supposed to is not measured.

  2. THE FILES. These clips were re-converted, because the original
     conversion's settings are lost with the inaccessible machine,
     and the addendum above named transcode variance as a threat.

They are separable, and cheaply, by ONE experiment: check out
003184a, build it, and measure THESE files with THAT code.

  - old code, new files, gives ~65 false alarms -> the files are
    innocent and an increment changed the instrument's behaviour
    without anybody noticing. Whichever commit it is owes a test.
  - old code, new files, gives ~13 -> the code is innocent and two
    transcodes of one recording differ by fifty-two false alarms,
    which is a far larger transcode sensitivity than this project
    has ever assumed, and a finding in its own right.

Filed as an issue. Until it is answered, this document is the
record: a prediction committed before the run, and a measurement
that broke it.

THE ANSWER, 25 August 2026, and it is not the code. The experiment
above ran: the published run's own measurement code, carrying only
the two load-and-seek fixes without which it cannot open these files
on this machine at all, measured two of the clips. It reproduced
YESTERDAY'S numbers exactly, not the published ones.

  clip                    published    new code   published code
  27122013_152435_cam       36/5/19      37/4/3           37/4/3
  27122013_153916_cam       67/5/11      67/5/0           67/5/0
                                      (found / miss / false)

Coverage identical in every arm. Recall and precision identical to
the last decimal. So the thirteen commits merged after the published
run are EXONERATED: they change nothing about what this instrument
measures. That is also a determinism result the project did not
have — the same input, through thirteen commits of change, gives the
same answer digit for digit.

WHAT THIS DOES NOT EXONERATE, stated because the comparison cannot
see it: the two fixes present in BOTH arms. They are common to
every run here, so nothing above tests them. The reasoning, offered
as reasoning and not as measurement, is that neither can matter: one
changes only when loading finishes, and the other only where
stepping begins — and coverage is identical to the published run,
so the same frames were visited either way, and the same frames
carry the same pixels.

WHAT REMAINS, and it is now sharp. The published run differs from
this one in exactly two remaining ways: DIFFERENT FILES (its
conversion's settings are lost) and a DIFFERENT MACHINE. This
experiment removed the code from the list; it did not separate those
two from each other, and nothing available can, because the original
.mp4 files and the machine that made them are the same inaccessible
place.

They can, however, be told apart by a NEW measurement rather than an
old one: convert a clip from the same .avi a SECOND time with
different settings — losslessly, so the pixels are the decoder's own
— and measure that on this machine. Only the file differs.

  - counts move between two transcodes of one recording -> transcode
    sensitivity is real, large, and the likely explanation, which
    makes every published corpus number conditional on a conversion
    this project never recorded until yesterday;
  - counts identical across both transcodes -> the file is not the
    variable and the machine is, which makes the published table a
    property of the laptop it was measured on.

Either answer is a finding this project has to publish. Until one of
them is measured, 83.6 / 84.0 / 83.8 remains the standing result and
the numbers at the top of this section are not published as its
replacement.

THE TRANSCODE IS NOT THE VARIABLE, so far as two points can say it,
25 August 2026. The same clip converted a second time from the same
.avi, LOSSLESSLY this time (-crf 0, so the decoded pixels are the
decoder's own), measured on this machine:

  27122013_152435_cam   found 37, miss 4, false 3, coverage 5134

Identical to the -crf 10 conversion, to the last decimal of recall
and precision, and identical to what the published run's own code
produced on that conversion. Two transcodes of one recording, one of
them lossless, are indistinguishable to this instrument.

WHAT THAT DOES AND DOES NOT ESTABLISH. It establishes that the
detector is insensitive to pixel differences ACROSS THE NEAR-
LOSSLESS RANGE. It does not yet exonerate transcoding, because the
published run's conversion settings are unknown and could sit well
outside that range: the likeliest lost setting of all is x264's
DEFAULT, -crf 23, which is visibly lossier than either point
measured here. A sensitivity claim tested only between crf 0 and crf
10 cannot speak for crf 23.

So the sweep is finished rather than declared: the same clip at crf
23 and at crf 28. Three points spanning lossless to visibly lossy,
all landing on 37/4/3, would exonerate transcoding across the whole
practical range and leave the MACHINE as the only remaining
explanation for the published table — which would make that table a
property of the laptop it was measured on, and a finding this
project has to publish. Counts that move at crf 23 would instead say
the lost conversion is a live suspect after all, and that every
published corpus number is conditional on settings nobody recorded.

83.6 / 84.0 / 83.8 remains the standing result.

THE SWEEP IS FINISHED, AND IT IS THE MACHINE, 25 August 2026. One
clip, 27122013_152435_cam, converted from the same .avi at four
qualities spanning lossless to visibly lossy, each measured on this
machine:

  conversion            found  miss  false   coverage
  -crf 0 (lossless)        37     4      3       5134
  -crf 10                  37     4      3       5134
  -crf 23 (x264 default)   37     4      3       5134
  -crf 28                  37     4      2       5134
  ---
  the published run        36     5     19       5134

Found and missed never move. Across the entire practical range of
transcoding, the worst disagreement is ONE false alarm, at the
lossiest setting. The published run differs by SIXTEEN on this clip.
Transcoding cannot explain a gap of that size, and the likeliest
lost setting of all, x264's default crf 23, sits exactly on the
lossless answer.

THE ENUMERATION, checked rather than assumed, because an elimination
is only as good as its list. The code: exonerated by measurement
above. The files: exonerated by this sweep. The face model:
public/models/face_landmarker.task is a committed file, unchanged
since 2026. The model runtime and the browser: package-lock.json is
tracked and has not moved since the published run's commit —
@mediapipe/tasks-vision 1.0.1 and @playwright/test 1.62.1, and a
pinned Playwright version pins the WebKit build with it.

What is left is the MACHINE: its processor, its operating system,
and the browser binary built for them. Same code, same model, same
pinned runtime, same frames, materially different false alarms.

THE IRONY IS WORTH STATING PLAINLY, because it is the finding.
src/io/videoStepper.ts opens by saying that watching a clip measures
it "at whatever rate the model manages on that machine, so the same
file yields different data on different hardware. For a benchmark
that is fatal: a result that depends on the reviewer's laptop is not
a result." Stepping was built to remove exactly that dependence, and
IT DOES remove it where it was aimed: coverage is identical to the
frame, and the found-blink counts barely move (341 to 347 across the
corpus). But threshold-hugging events are not protected by reading
every frame. Near-line flutter — which is what the published run's
false alarms are, as docs/eyeblink8-result.txt itself says — is
decided by micrometres of aperture, and those are machine-dependent
in a way frame scheduling is not. The instrument achieved frame
determinism and was read as though it had achieved measurement
determinism.

ONE HONEST GAP, unmeasurable now: the old machine's installed
node_modules is asserted to match its lock file, not verified. If
that tree had drifted from the lock, the runtime would be a suspect
again. The machine is inaccessible, so this cannot be closed, only
declared.

WHAT THIS DOES TO THE PUBLISHED NUMBER. 83.6 / 84.0 / 83.8 is not
wrong, and it is not the instrument's number either: it is what this
instrument measured ON THAT MACHINE. The same is true of
85.0 / 96.4 / 90.4 on this one. Neither is "the" result until the
project decides what a machine-conditional benchmark publishes, and
that decision is the owner's, not this document's.
